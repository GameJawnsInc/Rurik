# LANE B3-1 -- naming the path that re-issues destinations 11x
> **TRIMMED 2026-08-27.** This is a raw recon transcript. Where it SURVEYED a
> module by listing its asserts, the expression text has been cut and only the
> address and `File:line` locator kept — a census of assert strings is the bulk
> shape `CLAUDE.md`'s provenance gate refuses, and `test_provlint.py` caught it
> at 19 against a ceiling of 10. Every ADDRESS is kept (those are permitted in
> bulk), and every assert this lane's CLAIMS actually rest on is kept in full,
> here and in [FINDINGS.md](../FINDINGS.md) §1d.


Client: vault/client/2026-07-29_221c13772c7a/Gw.exe (build 38797, PRISTINE)
Tree: C:/gd/Rurik (verified)

## Q1 -- 0x005FC7A0

OBSERVED. Module = `P:\Code\Engine\Agent\AgApi.cpp` (asserts at lines 1041/1043/1069
fall inside 0x005FC7A0..0x005FC8F9). Also one `Array.h:587` bound check.

Asserts inside the body (asserts.py --at 0x005FC7A0 --span 0x160):
  0x005fc7cd  AgApi:1041   targetPoint.position != AGENT_INVALID_POSITION
  0x005fc81d  AgApi:1043   moveSpeed <= AGENT_MAX_MOVE_SPEED
  0x005fc8a1  AgApi:1069   ptr
  0x005fc880  Array [expression trimmed]
  (one more site at 0x005fc7f9, push 0x412 = line 1042, expression UNREADABLE --
   an `fstp st(0)` at 0x005FC803 is scheduled into the pattern. Its test is
   moveSpeed vs float [0x941148].)

=> ArenaNet's own parameter names for this function: `targetPoint` and `moveSpeed`.

Signature (RECONSTRUCTION from the frame, cdecl, 5 stack args, no `this`):
  ebp+0x08  esi  agentId        (indexes agent array at [ctx+0x14C], bound [ctx+0x154])
  ebp+0x0c  edi  targetPoint*   (float[2]+, `fld [edi]` / `fld [edi+4]`, x/y)
  ebp+0x10       moveSpeed      (float; asserted <= AGENT_MAX_MOVE_SPEED)
  ebp+0x14       flags          (`and dword [ebp+0x14],0xf` -- 4 bits kept)
  ebp+0x18       value stored straight to agent+0x50

Body flow (VAs):
  005FC82C  call 0x47F660  -> eax; ebx = [eax+8]   (agent-module context)
  005FC83C..005FC868  hash-mix of [ebx+0xC8] with agentId (shift/xor/lea*8 mixer),
                      then `call 0x5FCEC0` (ecx=ebx) -- looks like a change stamp
  005FC873  cmp esi,[ebx+0x154] / Array [expression trimmed]
  005FC895  esi = [[ebx+0x14C] + agentId*4]      -> agent object; AgApi:1069 `ptr`
  005FC8B9  lea ecx,[ebx+0x1CC]; push agentId; call 0x605F10
  005FC8CB  call 0x5FF540 (ecx=agent, moveSpeed, flags)  -- if returns 0:
  005FC8DE  call 0x602990 (same args)
  005FC8ED  mov [esi+0x50], [ebp+0x18]
  005FC8F0  call 0x602A40  <-- THE SHARED DESTINATION SETTER, 525 of the 586
            args: push -1 (arg3 plane = "do not change"), push 0 (arg2 = hard
            arrival), push edi (targetPoint), ecx = agent

=> 0x005FC7A0 is an AgApi.cpp entry point that sets an agent's move destination
   from a caller-supplied point + speed. arg3=-1 is HARDCODED at the call site,
   which is why all 525 carry it.

## Q2 -- the three callers (in progress)

OBSERVED. --xrefs 0x005FC7A0 = exactly 3 direct rel32 callers, no data words:
  0x0081AC0F, 0x0081B04F, 0x0081B69A.

Asserts in 0x0081A8F0..0x0081ADB0:
  0x0081ac3d  ChCliBase:154   index < arrsize(m_path)
  0x0081ad27  ChCliBase:164   this == context->playerControlledChar
=> module is `P:\Code\Gw\Char\Cli\ChCliBase.cpp`, and one of these functions is
   gated on the PLAYER-CONTROLLED CHARACTER, walking an `m_path` array.

## Q2 -- 0x0081A8F0 FULLY READ (OBSERVED)

__fastcall/thiscall, ecx = `this` (a ChCliBase char object), `ret 0x10` = 4 stack args:
  ebp+0x08 edi  -> destination POINT {float x, float y, int plane}  (edi+8 compared
                   against waypoint[0]+8, so +8 is the plane/layer id)
  ebp+0x0c      -> float[2] DIRECTION (dotted with the normalized path vector later)
  ebp+0x10      -> passed to the pathfinder
  ebp+0x14 esi  -> movement TYPE enum, values 1..8 tested explicitly

Body:
  0081A91D  call 0x47F660 ; esi=[eax+0x2c]      global context
  0081A931  early-out to 0x81AD0F if [this+0x10c]&0x100, or [this+0x64]&1,
            or bit4 of [this+0x10c] clear -> `push [this+0x14]; call 0x5FCA80`
            ([this+0x14] is the agentId -- same value passed as arg1 to 0x5FC7A0)
  0081A96C  call 0x709D30(destPoint, arg3, 0, 9, &outCount, &outArray[9*16])
            == THE PATHFINDER. Returns up to 9 waypoints, 16 bytes each.
            count clamped to 9; count==0 -> return 0.
  0081A999  PRUNE LOOP: call 0x709990(&wp[0], destPoint, 1.0f, 1) -> float;
            exits when result > 1.0 or wp[0].plane != dest.plane; otherwise
            memmove (0x46DBB0) shifts wp[1..] down over wp[0] and repeats.
            == leading-waypoint pruning / string-pulling.
  0081AA73  caches [this+0x54]=dir.x, [this+0x58]=dir.y, [this+0x5c]=moveType and
            calls 0x5FC6E0(agentId, &dir, moveType)   (the AgApi fn immediately
            before 0x5FC7A0) -- only when dir moved > eps or type changed.
  0081AA92  moveType 1,2,3 -> 1.0f ; 4,5,6 -> [0xA95590] ; 7,8 -> [0x946248]
            stored to [this+0x6c]  == a per-move-type SPEED MULTIPLIER
  0081AACD  normalizes (wp[0] - destPoint), 0x46E530 = rsqrt/normalize, then
            DOT with the cached direction [this+0x54],[this+0x58], + const,
            clamped/NaN-guarded  -> an angle-derived speed factor
  0081ABD7  inc [this+0x68]                      <-- MONOTONIC MOVE SEQUENCE
  0081ABF5  [this+0x60] = 0
  0081AC0F  call 0x5FC7A0(agentId=[this+0x14], targetPoint=&wp[0],
                          moveSpeed=[this+0x6c]*angleFactor, flags=0,
                          seq=[this+0x68])
            -> seq lands in agent+0x50 (the `mov [esi+0x50],eax` in 0x5FC7A0)
  0081AC2A  copies wp[1..count-1] (16 bytes each) into [this+0x70]
            guarded by `ChCliBase:154 index < arrsize(m_path)` with `cmp ecx,8`
            => **[this+0x70] IS `m_path`, 8 slots x 16 bytes**
  0081AC8A  pads the unused m_path slots with [0x948654]
            == the SAME constant AgApi:1041 calls AGENT_INVALID_POSITION
  0081AD1A  `ChCliBase:164  this == context->playerControlledChar`
            (ctx+0x680 == playerControlledChar) -- on BOTH the success and the
            early-out path. **This function only ever runs for the local player.**

=> 0x0081A8F0 = ChCliBase.cpp ~lines 100-170: BEGIN A MOVE. Pathfind to a point,
   prune, issue waypoint[0] to AgApi, stash the remaining waypoints in m_path.

## The other two callers -- module + role (OBSERVED)

0x0081ADB0 asserts:
  0x0081b07d  ChCliBase:239  index < arrsize(m_path)
  0x0081b16d  ChCliBase:248  this == context->playerControlledChar
  => same file, same two guards, ~85 lines later. Sibling of 0x0081A8F0.

0x0081B220 asserts:
  0x0081b237  ChCliBase:264  [expression trimmed]
  0x0081b258  ChCliBase:265  [expression trimmed]
  0x0081b2f8  ChCliBase:326  [expression trimmed]
  0x0081b3a3  ChCliBase:353  [expression trimmed]
  0x0081b32b  Array [expression trimmed]
  => a NOTIFY handler (`notify.code`, `notify.targetAgent`).

Xrefs (codescan --xrefs):
  0x0081A8F0 <- 1 caller: 0x00816470 (call)
  0x0081ADB0 <- 1 caller: 0x00816534 (call)
  0x0081B220 <- 0 direct calls; 1 data word: `0x00824606 mov dword ptr [ecx], 0x81b220`
               == INSTALLED AS A CALLBACK / vtable-or-table slot.

## CORRECTION to Q2 above (important)

0x0081A8F0's ebp+0x08 is NOT the destination -- it is the agent's CURRENT
POSITION. Proof: its only caller fills that buffer immediately before the call:
  008163D1  push [esi+0x14]            (agentId)
  008163D7  push lea eax,[ebp-0x28]
  008163D8  call 0x5FC400              == AgApi "get agent position" -> [ebp-0x28]
  ...
  0081646F  push eax  (= &[ebp-0x28])  == arg1 to 0x81A8F0
So 0x709D30(currentPos, scaledDirection, 0, 9, &count, &wp[9]) is a
**"trace the navmesh from here along this direction" walk**, not a point-to-point
solve. The dot product at 0x81AACD is (wp[0] - currentPos) . requestedDirection,
i.e. how well the first waypoint agrees with the requested heading -> speed factor.

## Q2 -- the chain, all the way out (OBSERVED)

0x005FC7A0  AgApi.cpp  SetAgentDestination(agentId, targetPoint, moveSpeed, flags, seq)
   ^-- 0x0081AC0F in 0x0081A8F0   ChCliBase.cpp ~L100-170  DIRECTIONAL move
   ^-- 0x0081B04F in 0x0081ADB0   ChCliBase.cpp ~L200-250  POINT move
   ^-- 0x0081B69A in 0x0081B220   ChCliBase.cpp ~L260-360  NOTIFY handler

0x0081A8F0 <- 0x00816470, inside fn 0x008163A0   (ChCliApi.cpp; assert
              `ChCliApi:5562 (rotation >= -1.0f) && (rotation <= 1.0f)` at
              0x008165EB is in this same block)
   0x008163A0 body: ctx=0x47F660; player = [[ctx+0x2c]+0x680]
              (== context->playerControlledChar, same +0x680 ChCliBase:164 names);
              bail if null or if bit4 of [player+0x10c] is clear;
              read current pos via 0x5FC400; scale the direction by arg2 (float);
              then a 3x3 SIGN TABLE over two signed ints ([ebp+0x10], [ebp+0x14])
              producing 1..8 -- **an 8-way direction octant**, which is exactly the
              1..8 enum 0x0081A8F0 switches on at 0x0081AA92.
   0x008163A0 <- 0x0053547B, inside fn 0x00535380

0x0081ADB0 <- 0x00816534, inside fn 0x008164C0
   0x008164C0 body: 0x70A5C0 -> map bounds; rejects a point outside them;
              player = [[ctx+0x2c]+0x680]; bit4 of [player+0x10c]; then
              `push edi; mov ecx,esi; call 0x81ADB0`.  == MOVE TO POINT.
   0x008164C0 <- 0x005354E6, a TAIL `jmp` from fn 0x00535490

**Fn 0x00535380 (direction) and fn 0x00535490 (point) are the client's own
movement issue layer, and both are DEDUPED against one global last-issued
destination:**
   0xC07C2C = last x, 0xC07C30 = last y, 0xC07C34 = last plane
   0x00535440  `cmp [0xC07C34], -1` ; if x,y match and plane == -1 -> RETURN, no call
   0x005354B9  `mov eax,[0xC07C34]; cmp eax,[ecx+8]` ; if x,y,plane match -> RETURN
   directional issues STORE plane = 0xFFFFFFFF (0x00535462)
   point issues STORE the point's own plane (0x005354D5)
=> the -1 in the capture's arg3 is the SAME -1: a "no plane" marker on the
   direction path. (The -1 reaching 0x602A40 is hardcoded at 0x005FC8E8 though.)

0x00535380 <- 5 callers, all local: 0x00535710, 0x005357C5, 0x00535DEE,
              0x00535E9F, 0x00535F48; plus 1 .rdata word at 0x00BE5D74 (a table).
0x0081B220 <- installed as a callback by the factory at 0x008245E0:
              `0x008245F1 call 0x47F490(size=0x130, tag 0xa95d40/0x29)`,
              `0x008245FE call 0x81A4B0(ctor)`, `0x00824606 mov [ecx], 0x81b220`.

## *** THE 11x MULTIPLIER -- FOUND (OBSERVED) ***

0x0081B69A is NOT inside 0x0081B220. It is inside **0x0081B580**, a separate
function whose ONE caller is 0x0081B548 -- which is **jump-table case 5 of the
notify handler 0x0081B220**:
    0081B267  mov eax,[edi]        ; notify.code
    0081B269  cmp eax,5 / ja       ; 6 codes
    0081B273  jmp dword ptr [eax*4 + 0x81B568]
  table bytes at 0x0081B568 (24 bytes, read out of the --dis byte column):
    0 -> 0x0081B295   1 -> 0x0081B3E5   2 -> 0x0081B554
    3 -> 0x0081B27A   4 -> 0x0081B52E   5 -> 0x0081B543
    0081B543  push dword ptr [edi+0x1c]   ; notify.<seq>
    0081B546  mov ecx, esi                ; targetChar
    0081B548  call 0x81B580

0x0081B580  __thiscall (this = ChCliBase*, arg = seq):
  0081B58D  `cmp eax,[esi+0x68]` -- IGNORE the notify unless its seq equals the
            character's CURRENT move sequence (the counter 0x81A8F0/0x81ADB0
            increment and 0x5FC7A0 stamps into agent+0x50)
  0081B592  if m_path[0].x and m_path[0].y are both AGENT_INVALID_POSITION
            ([0x948654]) -> path exhausted
  0081B5B5  either of those -> `call 0x81A6F0; call 0x81C090` == STOP
  else:
  0081B5CB  speed = [esi+0x6c]  (the move-type multiplier)
  0081B5D1  if direction ([esi+0x54],[esi+0x58]) == (0,0) -> skip straight to
            0081B685 and use the multiplier unscaled
            (0x0081ADB0 zeroes that direction, 0x0081A8F0 sets it)
  0081B5F5  else current pos via 0x5FC400, d = m_path[0] - pos, normalize
            (0x5351D0), DOT with the stored direction, clamp <=1.0, NaN-guard,
            speed *= it
  0081B685  inc [esi+0x68]                        <-- NEW SEQUENCE
  0081B69A  call 0x5FC7A0(agentId=[esi+0x14], targetPoint=edi=&m_path[0],
                          moveSpeed, flags=[esi+0x60], seq=[esi+0x68])
  0081B6A2  `lea edx,[esi+0x70]` then a loop copying [edx+0x10]->[edx] 16 bytes at
            a time == **POP m_path[0], SHIFT THE REST DOWN**

=> ONE gesture pathfinds up to 9 waypoints; the client then issues them ONE AT A
   TIME, each on an arrival notification carrying the sequence it stamped.
   That is the 11x. 9 waypoints max per solve x ~1 begin-move each is the right
   order of magnitude for 525 / 49.

[this+0x60] therefore selects the AgApi `flags` byte (arg4, masked `and ...,0xf`):
   0 from the directional path (0x0081ABF5), 1 from the point path (0x0081B048),
   and the advance path forwards whichever is current.

## The notify bus (OBSERVED)

0x00603D40(code, agent, x, seq) with ecx = [agentModuleCtx + 0x94] is the notify
raiser. 12 call sites, ALL inside the Agent engine (0x005FD3B8 .. 0x00602E49):
  code 1 @0x005FF7A6   code 2 @0x005FD3B6   code 4 @0x00600A64, 0x00601868, 0x00602570
  code 5 @0x006017E3, 0x00600342          code 6 @0x00602616   code 7 @0x00600428
Codes 4 and 5 push `[esi+0x50]` -- the SEQUENCE 0x005FC7A0 stamped there
(0x005FC8ED `mov [esi+0x50], eax`) -- and clear/re-arm it right after
(0x0060034F, 0x00600A71, 0x006017F0, 0x00601875, 0x00602577).
ChCliBase's handler 0x0081B220 dispatches codes 0..5 (`cmp eax,5; ja`), so 6 and
7 fall through to its default. Code 5 -> case 0x0081B543 -> 0x0081B580 -> re-issue.

The function containing the code-5 raiser at 0x00600339 starts at 0x00600140 and
is a VIRTUAL method: its only reference is the 2-entry vftable at 0x00A52F60
(slot0 = 0x005FE920, slot1 = 0x00600140), installed by
`0x005FDE8E mov [ebx],0xa52f60` and `0x005FE689 mov [edi],0xa52f60`.
=> --xrefs cannot see its callers. **NOT DETERMINED: whether that tick is driven
by the frame loop or can also be re-entered from a server update.**

## The pathfinder (OBSERVED)

0x00709D30 and 0x00709E90 are in `P:\Code\Engine\Map\Map*.cpp` -- asserts
  0x00709d45 Map:1195  [expression trimmed]
  0x00709d7b Map:1197  [expression trimmed]
Callers: 0x00709D30 <- 0x006007ED (AgAgent) and 0x0081A96C (ChCliBase)
         0x00709E90 <- 0x00605802 (AgAgent) and 0x0081AF51 (ChCliBase)
=> the client solves paths itself, in two independent places.

Other named helpers:
  0x005FC400  AgApi (assert `AgApi [expression trimmed]` at 0x005FC43F) -- fills a point from
              an agentId == "get agent position"
  0x00602A40  the shared destination setter -- assert
              `AgAgent:2334 m_flags & INTERNAL_FLAG_IN_WORLD` at 0x00602A56
  0x005FCA80  no readable assert inside; called on the ChCliBase early-out (stop)

## Q4 -- 0x00602448 (the 12) (OBSERVED for structure)

Containing function starts **0x006022B0** (`push ebp` right after the previous
function's `ret 0x10` + int3 at 0x006022AC/AF). Module = AgAgent.cpp; asserts in
its body:
  0x00602350  AgAgent [expression trimmed]
  0x006023AF  AgAgent:2366  [expression trimmed]
  0x006023D8  AgAgent:2367  [expression trimmed]
  0x006023F5  AgAgent:2368  [expression trimmed]
  0x00602412  AgAgent:2369  [expression trimmed]
__thiscall(this=esi=agent, arg1=[ebp+8]=edi=another agent, arg2=a bool).
It COPIES move state from edi onto esi and then calls the setter:
  00602429  or [esi+0x20], 0x80000        (the same bit 0x00600140 tests at entry)
  00602432  mov [esi+0xc4], eax           (eax = [edi+0xc4])
  0060243E  fstp [esi+0x60]               (= [edi+0x60], the moveSpeed)
  00602441  push [edi+0x98]               -> arg2 of 0x602A40
  00602447  push lea eax,[edi+0x9c]       -> arg1, the destination POINT
  00602430  push -1                       -> arg3, hardcoded
  00602448  call 0x602A40   (ecx = esi)
Its 2 callers, 0x00605EF1 and 0x006060E2, are inside the AGENT UPDATE DISPATCHER
-- asserts in that block name `AgTrack:680 m_agentCount` (0x00605DE6) and
`AgUpdate:26 m_dispatchedOffset <= m_updateArray.Count()` (0x00606132) -- and
both call it as `push ebx(bool); push [agentArray + i*4]; call 0x6022B0`.

Differences from the 525 path: no ChCliBase, no m_path, no sequence counter, no
playerControlledChar gate, arg2 forwarded from the SOURCE AGENT ([edi+0x98])
instead of a literal 0 -- the capture's arg2=0 for all 12 means those source
agents happened to carry 0.

## Retraction

The `.rdata` word at 0x00BE5D74 holding 0x00535380 is NOT a dispatch table: the
words around it are 0x00535354, 0x0053535C, 0x00535362, 0x00535368, 0x0053536D,
0x00535380, 0x00535386, ... -- consecutive code addresses 5-19 bytes apart, i.e.
an unwind/state map, not function pointers. Do not cite it as a registration.

## *** Q3 -- THE CLINCHER: the issue layer is GmWalk.cpp ***

asserts.py --at 0x00534000 --span 0x3000 puts exactly one named site in the
0x535xxx block:
    0x0053513E   GmWalk:106   evt.code < KEYSTATES
file `P:\Code\Gw\Ui\Game\GmWalk.cpp`. The nearest named sites either side are
GmUnlockBlurb:603 at 0x00534D0B (below) and GmWeaponBar:171 at 0x00536CE1
(above). Everything I walked in between -- 0x00535380 (direction issue),
0x00535490 (point issue), and their callers 0x00535710, 0x005357C5, 0x00535DEE,
0x00535E9F, 0x00535F48, 0x005365B8, 0x00536A7C -- lies in that gap.
OBSERVED: the assert and its VA. RECONSTRUCTION: that those functions are
GmWalk.cpp (interval ordering off a single assert; GmWalk has only one readable
site, so this is a placement argument, not a bound).

`evt.code < KEYSTATES` is a KEY-EVENT assert. The whole 525 chain therefore
starts in the UI walk module, at a keyboard/mouse event, and never touches the
network stack:

  GmWalk.cpp (key/click event, dedupe vs 0xC07C2C/30/34)
    -> ChCliApi.cpp 0x008163A0 (8-way octant) / 0x008164C0 (bounds-checked point)
      -> ChCliBase.cpp 0x0081A8F0 / 0x0081ADB0  [assert this == playerControlledChar]
        -> Map*.cpp pathfinder 0x709D30 / 0x709E90  (<=9 waypoints)
          -> AgApi.cpp 0x005FC7A0  -> 0x00602A40   (waypoint 0, seq stamped in agent+0x50)
        -> the rest parked in ChCliBase::m_path (8 x 16 bytes at this+0x70)
  AgAgent.cpp raises notify code 5 with that seq (0x0060034A / 0x006017EB)
    -> ChCliBase.cpp 0x0081B220 case 5 -> 0x0081B580
      -> pop m_path[0], inc seq, AgApi 0x005FC7A0 again ... until m_path is
         AGENT_INVALID_POSITION, then STOP (0x0081A6F0 / 0x0081C090)

vs the server path, which is disjoint:
  wire 0x0029 (AgMsg.cpp per msghandler --map) -> 0x005FD913 -> 0x00602A40 direct.
  It does NOT pass through 0x005FC7A0 and never touches m_path.

## What static analysis CANNOT split (be honest about this)

The capture counted the shared inner site 0x005FC8F0, so the 525 is
begins + advances pooled. To split them, tap the three ENTRIES:
  0x0081A8F0  directional begin      (flags 0, [this+0x60]=0)
  0x0081ADB0  point begin            (flags 1, [this+0x60]=1)
  0x0081B580  waypoint advance       (reads m_path[0], shifts m_path)
movehook already taps function entries -- these are four-byte-alignable entries
with standard prologues.
