# LANE B3-3 -- +inf target and the stuck character (build 38797, pinned pristine)

Tree: C:/gd/Rurik (verified). Client: vault/client/2026-07-29_221c13772c7a/Gw.exe

## Q1 -- AGENT_INVALID_POSITION == 0x7F800000 (+inf)  -- CONFIRMED / OBSERVED

Two independent chains, both landing on the same constant slot 0x00948654.

(a) inside the teleport fn 0x006020B0:
  0x006020B6  fld dword ptr [ebp+8]          ; arg0 = point.x
  0x006020B9  fld dword ptr [0x948654]       ; the constant K
  0x006020BF  fucom st(1) / 0x006020CD test ah,0x44 / jp   ; !=  -> skip
  0x006020D2  fld dword ptr [ebp+0xc] / fucompp / jp       ; !=  -> skip
  0x006020DE  push 0x812 (=2066) ... call 0x487BC0   <- THE ASSERT
  asserts.py: 0x006020e3  AgAgent:2066  "point.position != AGENT_INVALID_POSITION"
  => assert fires iff arg0==K AND arg1==K, so K == AGENT_INVALID_POSITION.

(b) the lane's own anchor, the AgAgent:1144 guard:
  0x0060025B  fld dword ptr [0x948654]       ; K
  0x00600261  fld dword ptr [esi+0x9c]       ; m_targetPoint.position.x
  0x00600267  lea ebx, [esi+0x9c]
  0x0060026D  fucomp st(1) / jp 0x600298     ; != -> skip
  0x00600276  fld dword ptr [ebx+4]          ; +0xA0 = .y
  0x00600279  fucompp / jp 0x60029a
  0x00600282  push 0x478 (=1144) -> 0x00600287 mov edx,0xa52f68 -> call 0x487bc0
  asserts.py: 0x00600287  AgAgent:1144 "m_targetPoint.position != AGENT_INVALID_POSITION"
  => same constant; and this also OBSERVES that +0x9C/+0xA0 == m_targetPoint.position.

Raw bytes at 0x00948654 (codescan --dis renders the hex column):
  00948654  0000     add byte ptr [eax], al
  00948656  807f74.. cmp byte ptr [edi+0x74], ...
  => bytes 00 00 80 7F => dword 0x7F800000 = IEEE +infinity.

VERDICT: CONFIRMED. AGENT_INVALID_POSITION.x == .y == 0x7F800000 == +inf.
Compared component-wise as two float compares, not as a struct memcmp.
(There is also an immediately-preceding assert at 0x00600245 push 0x477=1143.)

## Q2 -- 0x006020B0 teleports to its ARGUMENTS. The PLAN.md claim is WRONG
   and BACKWARDS: the fn writes the SENTINEL into +0x9C, it does not read it.

Signature (RECONSTRUCTION): __thiscall f(this=ecx, Point pt /*16 bytes by value,
[ebp+8..0x14]*/) -- the recursive call at 0x0060221E rebuilds exactly 16 bytes of
stack args, and the epilogue is `ret 0x10` (see 0x006022xx tail, to confirm).

ebx = this (0x006020C5 mov ebx,ecx); esi = ebx+0x78 (0x006020FF lea esi,[ebx+0x78]).
Stores:
  0x0060210E  fstp [ebx+0xb0] <- 0.0        (fldz at 0x006020FD)  ; velocity x
  0x00602117  fstp [ebx+0xb4] <- 0.0                              ; velocity y
  0x00602132  mov [esi+0x00] <- [ebp+0x08]  ; +0x78 = arg.x
  0x0060213A  mov [esi+0x04] <- [ebp+0x0c]  ; +0x7C = arg.y
  0x00602143  mov [esi+0x08] <- [ebp+0x10]  ; +0x80 = arg.plane
  0x0060214F  mov [esi+0x0c] <- [ebp+0x14]  ; +0x84 = arg.w
  0x00602155  mov [ebx+0x88] <- K (+inf)    (via ebp-0x1c; fld [0x948654] @0x0060211D)
  0x00602164  mov [ebx+0x8c] <- K (+inf)
  0x0060216D  mov [ebx+0x9c] <- K (+inf)    <<-- WRITES THE SENTINEL INTO m_targetPoint.x
  0x00602179  mov [ebx+0xa0] <- K (+inf)    <<-- ...and .y
  0x0060217F  [ebx+0x90]=0  0x00602189 [ebx+0xa4]=0  0x00602194 [ebx+0x94]=0
  0x0060219E  [ebx+0x98]=0  0x006021A8 [ebx+0xa8]=0
  0x006021B2  push esi ; call 0x70a150 ; add esp,4   (one cdecl arg = &m_point)
  0x006021BC  je 0x6021d5 -- only if nonzero:
     0x006021BE..0x006021D2 copy [esi+0..0xc] -> [ebx+0x68..0x74]
  0x006021D8 [ebx+0x3c]=0  0x006021DF [ebx+0x40]=0  0x006021E6 [ebx+0x48]=0
  0x006021ED..0x006021F5 walk array base [ebx+0x28], count [ebx+0x30], stride 4
  0x0060221E  call 0x6020b0  RECURSIVE, same 16-byte point, this = [edi]
              (bound/attached agents -- cf. m_bindSourceArray / m_bindTarget,
               AgAgent:300/301/804)
  0x00602231  call 0x6011f0 (esi=[ecx+0x58], push &[ebp+8], push 1, push 0)
  0x00602239  call 0x601f70

So: +0x78 (m_point) <- ARGUMENT. +0x9C (m_targetPoint) <- +inf sentinel.
The direction claimed in the arc PLAN.md is inverted.

## Q3 -- who writes +0x9C.  YES, there is a deliberate "no destination" write,
   and it is the teleport itself.

codescan --field 0x9c --in AgAgent (range 0x005FE0C3..0x00602CE2): 21 insns,
5 "stores" of which one (0x0060060A) is absolute address 0x9C, not a field.
Real stores, each cross-checked against the +0xA0 (.y) store that follows it:
  1. 0x005FE507 / 0x005FE510  init/construct: copies *[ebp+0x58] into +0x9C/+0xA0,
     after copying *[ebp+0x60] into +0x88.. and *[ebp+0x54] into +0xB0/+0xB4.
     Caller-supplied; can carry the sentinel if the caller passes it.
  2. 0x005FEC52 / 0x005FEC5E  bulk copy +0x88..+0xA0 from esi (one agent) to
     ecx=[edi] (another) -- propagates whatever the source holds, sentinel included.
  3. 0x0060216D / 0x00602179  THE TELEPORT -- writes K=+inf unconditionally.
     <<-- the deliberate "no destination" write.
  4. 0x00602AB0 / 0x00602AB9  in fn 0x00602A40 = the SET-DESTINATION path:
     0x00602A48 test [ebx+0x20],0x20000 / assert AgAgent:2334
                "m_flags & INTERNAL_FLAG_IN_WORLD"   => 0x20000 == INTERNAL_FLAG_IN_WORLD
     0x00602A65 and [ebx+0x20], 0xFFF7FFFF  (clears bit 0x80000)
     0x00602A6C if [ebp+0x10] != -1 -> [ebx+0x80] = it (plane)
     copies *[ebp+8] into +0x88..+0x94, [ebp+0xc] into +0x98, *[ebp+8] into +0x9C/+0xA0
     0x00602B74 also calls the teleport 0x006020B0.

Caveat (tool's own): --field cannot see a field reached through a biased `this`
or an address built in two steps. Stores from OUTSIDE AgAgent's assert range were
not scanned here.

## Q4 -- 0x006025A6 sits in fn 0x00602540. It teleports the agent TO ITS OWN
   CURRENT POSITION. Purpose: RE-ANCHOR / RE-RESOLVE IN PLACE.

  0x00602540  push ebp; mov ebp,esp; ... mov esi,ecx      (__thiscall, no args)
  0x00602549  call 0x47f660 ; ebx = [eax+8]               (the usual context getter)
  0x0060254E  test [esi+0x20], 0x20000 ; je 0x6025e2      <- IN_WORLD or do nothing
  0x0060255E  cmp [esi+0x24], 1 ; jne -> skip
  0x00602564  push [esi+0x50]; lea ecx,[ebx+0x94]; push 0; push esi; push 4;
              call 0x603d40 ; then [esi+0x50] = 0
  0x00602580  call 0x5ff880                    (same helper the teleport calls first)
  0x00602585  edi = esi+0x78 ; copy the 16 bytes AT +0x78 onto the stack
  0x006025A4  mov ecx, esi
  0x006025A6  call 0x6020b0                    <- TELEPORT(this, m_point)
  0x006025AB  imul eax,[esi+0x24],0x64 ; cmp [esi+0x58], [eax+ebx+0x148]
  0x006025BE  push 0x8a3 (=2211) -> assert  (text not yet read)
  0x006025D5  push 0; push 1; push edi(&m_point); push ecx; call 0x600b70
  0x006025E8  ret            (no ret n -> takes no stack args)

So the "jump" this path produces is NOT to a new coordinate supplied by anyone --
the destination is the agent's own +0x78. Any observed displacement across this
call is what the teleport's own resolve step (call 0x70a150 -> +0x68..0x74) or the
following 0x600b70 did, or the difference between the previous sampled tick and
+0x78 at entry.

## Q5 -- collision: there IS a point-resolve call, and it is obstacle-related
  0x006021B2 call 0x0070A150 -- one cdecl arg, &m_point:
    0070A150  push ebp; mov ebp,esp
    0070A153  call 0x47f660
    0070A158  push [ebp+8]                  ; the point
    0070A15B  mov eax,[eax+0x14]
    0070A15E  mov ecx,[eax+0x74]
    0070A161  add eax,4 ; push eax ; push ecx
    0070A166  call 0x722b90 ; add esp,0xc ; ret
  0x00722B90's neighborhood carries PathObstacle:176 "radius >= 0" at 0x00722C4D.
  => the resolve consults the PathObstacle/path map, and the result only feeds
     +0x68..+0x74 (and only when it returns nonzero). +0x78 is written BEFORE the
     call and is NOT corrected by it.

## THE MECHANISM (added after reading the extrapolator)

### 0x005FFB40 -- the dead-reckoning extrapolator. __thiscall(this=esi, Point* out=[ebp+8], int time=[ebp+0xc])
  0x005FFB50 fld [esi+0x78] / fld [0x948654] / fucom ... 0x005FFB73 push 0x3ce(=974)
            -> assert AgAgent:974 "m_point.position != AGENT_INVALID_POSITION"
            (THIRD independent confirmation that 0x948654 is AGENT_INVALID_POSITION)
  0x005FFB8B mov eax,[esi+0x48] ; test eax,eax ; je skip ; sub eax,time ; jns skip
  0x005FFB99 push 0x3d2 (=978) -> assert AgAgent:978
            "!m_timeStopMovement || ((int)(m_timeStopMovement - time) >= 0)"
            => +0x48 IS m_timeStopMovement (matched to the assert's own operand)
            *** THE ASSERT CALL FALLS THROUGH TO THE EXTRAPOLATION AT 0x005FFBAD. ***
            An over-run past the stop time is DIAGNOSED and then PERFORMED anyway.
  0x005FFBAD  vx = [esi+0xb0] ; vy = [esi+0xb4]
  0x005FFBB3  dt = time - [esi+0x58]          (+0x58 = timestamp of m_point)
  0x005FFBD1  fmul qword [0xa53748]           bytes fc a9 f1 d2 4d 62 50 3f
                                              = 0x3F50624DD2F1A9FC = 0.001 (ms -> s)
  0x005FFBD7  out->plane ([ecx+8]) = [esi+0x80]   <- plane COPIED, never re-resolved
  0x005FFBE0  out[0xc] = 0
  0x005FFC19  out.x = [esi+0x78] + vx*dt ;  0x005FFC21 out.y = [esi+0x7c] + vy*dt
  0x005FFC24..0x005FFC5x  clamp x against [edi+0x1b0] and [edi+0x1b8] (edi = ctx+8)
              -- a WORLD-BOUNDS clamp only. No navmesh, no collision, no obstacle test.

### 0x005FF880 -- "bring this agent's m_point up to `now`"
  0x005FF89E  time = ctx8[m_world*0x64 + 0x148]   (world timer)
  0x005FF89A  if [esi+0x48] (m_timeStopMovement) != 0:
     0x005FF8B1  call 0x5ffb40(&tmp, time)
     0x005FF8B9..0x005FF8CE  copy the 4 dwords straight into [esi+0x78]  <-- m_point WRITE
     0x005FF8D9  call 0x603040 (ecx = ctx8 + m_world*0x64 + 0x10c, arg &m_point)
                 -> module AgMap (asserts AgMap:34 "point.position.y <= m_dims.y1 * CELL_SIZE",
                    AgMap:50..53 at 0x006031e6..0x00603231) = spatial-grid re-bucket,
                    NOT a collision correction.
  0x005FF8E1  if [esi+0x4c] != 0: call 0x5ff9f0 -> [esi+0xb8]; sin/cos (0x5bc0f0/0x5bc4f0)
              -> [esi+0xbc].. (facing)
  Callers: 005FE9EA 006003CF 006020F8(teleport) 00602490 00602580(the halt fn) 00602626
           00602938 00602D18

### Why the character stays pinned -- the teleport is a FULL STOP
  0x0060210E/0x00602117  velocity +0xB0/+0xB4 = 0.0
  0x006021E6             [ebx+0x48] = 0   == m_timeStopMovement = 0
  0x0060216D/0x00602179  m_targetPoint +0x9C/+0xA0 = +inf (AGENT_INVALID_POSITION)
  => 0x005FF880's `cmp [esi+0x48],0 ; je` now skips extrapolation forever.
  The agent cannot move again on the client's own initiative. Exactly the observed
  "pinned at one coordinate, movement events stop" state.

### 0x00602540 -- what the 0x006025A6 path IS (RECONSTRUCTION of the name; behaviour OBSERVED)
  A "halt the agent where it is now" method:
   - 0x0060254E gated on INTERNAL_FLAG_IN_WORLD (0x20000, named by AgAgent:2334)
   - 0x0060255E..0x00602577 cancels a queued timer: handle [esi+0x50], queue lea [ebx+0x94],
     call 0x603d40, then [esi+0x50] = 0
   - 0x00602580 call 0x5ff880  (extrapolate m_point to now)
   - 0x006025A6 call 0x6020b0(this, m_point)   <-- destination IS the agent's own point
   - 0x006025C3 assert AgAgent:2211 "time == context->world[m_world].timerQueue.GetTime()"
   - 0x006025DD call 0x600b70(time, &m_point, 1, 0)
  Its most telling caller: 0x006022D8 `cmp [esi+0x48],0 ; je skip ; 0x006022E7 call 0x602540`
  -- i.e. called exactly when m_timeStopMovement is set (agent mid-move): "if it is
  moving, stop it here first", before a sync that then checks
  AgAgent:2147 "syncPoint.position != AGENT_INVALID_POSITION" (0x00602350).

### The "main path" for comparison
  0x00602B74 (fn starting ~0x00602B00, takes Point* [ebp+8]):
    if m_timeStopMovement != 0 -> full teleport(this, *pt)
    else -> 0x00602B7B writes *pt straight into +0x78, THEN 0x00602B90 call 0x70a150.
  Same shape: the position write is never gated on the spatial query.

### 0x0070A150 / 0x00722B90 -- the spatial query, and what it is NOT
  0x00722B90 returns 1 iff:
    0x00722BA4 call 0x72b840(edi, pt, &out) != 0, AND
    0x00722BB0/0x00722BBE out.x == pt.x AND out.y == pt.y   (resolve changed nothing), AND
    0x00722BD6 call 0x7234c0([edi]+0x24, [ebp+0xc], pt) == 0
  Neighborhood assert: PathObstacle:176 "radius >= 0" at 0x00722C4D.
  In the teleport its ONLY consumer is the +0x68..+0x74 copy at 0x006021BE (gated by
  0x006021BC je). It never vetoes, corrects, or aborts the +0x78 write.

### Answer to Q5, stated with its limits
  SUPPORTED: there is no collision check on the teleport destination; the destination
  itself is produced by uncollided linear extrapolation; the plane rides along
  unchanged; the only clamp is the world rectangle; an extrapolation that over-runs
  m_timeStopMovement asserts and proceeds. All the machinery needed to land inside
  geometry is present and none of the machinery to prevent it is.
  NOT SUPPORTED FROM THE IMAGE: that this is what happened in the capture at t=243.5s,
  or what set plane to 29. That needs the movetap trace, not static analysis.
