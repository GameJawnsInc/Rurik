# LANE B3-2 -- MapFindPath 0x00709E90 signature  [COMPLETE]

Client: vault/client/2026-07-29_221c13772c7a/Gw.exe, build 38797, PRISTINE.
Tree: C:/gd/Rurik (verified). READ-ONLY, nothing launched, no repo file edited.

## HEADLINE
The record's `MapFindPath(from, to, range, maxCount) -> pathCount, P[]` is RIGHT about
the argument ORDER and MEANING and WRONG about the return. There are SIX args, not
four; pathCount and P[] are OUT-PARAMETERS (arg5, arg6); and eax is not a result at
all. => AN ENTRY-ONLY HOOK CANNOT SCORE AGREEMENT. It can capture the QUERY only.

## Q1 -- SIGNATURE

EXTENT (OBSERVED): 0x00709E90 .. 0x0070A0D4. True function entry:
  0x00709E8A `ret`, 0x00709E8B..0x00709E8F int3, 0x00709E90 `push ebp`.
  0x0070A0D5..0x0070A0DF int3; a sibling with the same arg shape starts 0x0070A0E0.
  (`--dis`'s "nearest earlier int3 padding: 0x00709D30" note is its own heuristic and
   is WRONG here; `--upto 0x00709E90` settles it.)

CONVENTION (OBSERVED): __cdecl. NOT thiscall. NOT stdcall.
  - All four exits are bare `ret`, no immediate:
      0x00709F0F, 0x00709F44, 0x0070A0AD, 0x0070A0D4
  - Caller confirms: 0x00605807 `add esp, 0x18` = 24 = 6 dwords popped by the CALLER.
  - ecx is never READ at entry. First ecx use is 0x00709EE7 `mov ecx,[ebp+0x1c]` (a
    write from arg6). The `push ecx` at 0x00709F2B / 0x00709FA1 / 0x0070A0BB is the
    MSVC reserve-then-`fstp dword [esp]` float-push idiom, not an argument.
  => hook reads [esp+4..esp+0x18] at entry; ecx is GARBAGE, do not record it as `this`.

SIX ARGS (OBSERVED) -- [ebp+8]..[ebp+0x1c]; nothing at [ebp+0x20] or beyond.

 # slot     type            role        evidence
 1 [ebp+08] point*          FROM        edi @0x00709EA1; `fsub [edi+4]` @0x00709EB5,
                                        `fsub [edi]` @0x00709EC4
 2 [ebp+0c] point* (16B)    TO          ebx @0x00709E9E; [ebx],[ebx+4] float
                                        (@0x00709EA7/0x00709EAC); [ebx+8] INT
                                        (`cmp eax,[ebx+8]` @0x0070A032 -- cmp, not
                                        fcomp); [ebx+0xc] dword @0x00709EFC
 3 [ebp+10] float           range       only ever `fld dword ptr [ebp+0x10]`
                                        (@0x00709F22, @0x0070A0AE); forwarded as float
 4 [ebp+14] int             maxCount    never deref'd, never fld'd here; both callers
                                        pass an IMMEDIATE (see Q4 buffer arithmetic)
 5 [ebp+18] int*            OUT count   written @0x00709F06, @0x0070A0A1
 6 [ebp+1c] point*          OUT path[]  written @0x00709EE7..0x00709EFF

TRAP: the stack SLOT [ebp+0xc] is REUSED as a float scratch local once ebx holds arg2
(`fstp dword ptr [ebp+0xc]` @0x00709EBB and 8 more sites). Entry hook is safe; a hook
placed anywhere later reads scratch, not `to`.

FROM/TO ORDER -- three independent proofs arg1=FROM, arg2=TO:
  a) 0x00709F50-0x00709F64: dx = arg2.x - arg1.x, dy = arg2.y - arg1.y. The unit
     direction points arg1 -> arg2, and arg1 ([ebp+8]) is passed to backend 0x7219d0
     as the ORIGIN alongside that direction (@0x00709FC4).
  b) the degenerate early-out emits arg2 as the single path point (@0x00709EEA..EFF)
     -- a path's one point is its destination.
  c) 0x0070A032 compares the LAST output point's +8 against arg2's +8, and on success
     0x0070A04E-0x0070A06D overwrites that last point with all 16 bytes of arg2 --
     snapping the path end exactly onto the requested destination.

## Q2 -- WHERE THE ANSWER COMES OUT   *** THE LANE-CRITICAL ANSWER ***

ANSWER IS ENTIRELY IN CALLER-SUPPLIED OUT-BUFFERS. eax IS NOT A RESULT.

arg5 = int* pathCount (OBSERVED):
  0x00709F02 mov eax,[ebp+0x18] / 0x00709F06 mov dword ptr [eax], 1   (*count = 1)
  0x0070A09E mov eax,[ebp+0x18] / 0x0070A0A1 dec dword ptr [eax]      (*count--)
  forwarded to every backend (@0x00709F25, 0x00709F93, 0x0070A0B5)
  read straight back: 0x00709FCF `mov edi, dword ptr [edi]` (edi was [ebp+0x18])

arg6 = point* outPath (OBSERVED):
  0x00709EE7 mov ecx,[ebp+0x1c]; 0x00709EEA..0x00709EFF copy 16 bytes of *arg2
  forwarded to every backend (@0x00709F1F, 0x00709F92, and via esi @0x0070A0B4)

STRIDE = 16 BYTES (OBSERVED):
  0x00709FDF `shl edi, 4` (edi = count) then
  0x00709FE2 fld [edi+esi-0x10] .x ; 0x00709FE9 fld [edi+esi-0xc] .y ;
  0x0070A02E mov eax,[edi+esi-8] (INT)
  layout { float x; float y; int32 plane; int32 }  -- identical shape to m_point (Q5)

eax IS MEANINGLESS (OBSERVED), per exit:
  0x00709F0F  eax = arg5, the count POINTER, left over from 0x00709F02
  0x0070A0AD  eax = last point's +8 field, left over from 0x0070A094
  0x00709F44 / 0x0070A0D4  eax = raw backend return, never normalised
  => no consistent return value; effectively void.

CONSEQUENCE FOR MOVECODE-B3:
  * entry hook CAN capture the query: from, to, range, maxCount (+ the two out
    POINTER VALUES, which must be stashed).
  * entry hook CANNOT capture the answer -- at entry arg5/arg6 point at
    uninitialised caller stack.
  * to score agreement, ALSO break at all four rets (0x00709F0F, 0x00709F44,
    0x0070A0AD, 0x0070A0D4) and read [stashed arg5] and [stashed arg6], OR
    hook the sibling/caller instead.

## Q3 -- ASSERTS

INSIDE 0x00709E90..0x0070A0D4: NONE. (Map.cpp assert VAs in the neighbourhood are
0x00709e75 -- previous function -- then 0x0070a563, past the end.)
`asserts.py --at 0x00709E90` REPORTS 0x0070a563/0x0070a590/0x0070a62e; those are
OUTSIDE the measured extent -- the tool's function heuristic overshoots. Do not
attribute them to this function.
CAVEAT: asserts.py is a FLOOR by its own banner (3 unreadable + 370 more sites
image-wide). "No assert names it" is not a census.

MODULE (OBSERVED, ArenaNet's own path): the function sits inside the assert range of
  P:\Code\Engine\Map\Map.cpp   (0x00707434..0x0070A62E)
so the `Map` prefix in our reconstructed name is CORROBORATED. ArenaNet's own name
for the FUNCTION is NOT DETERMINED -- no assert inside it to carry one.

Nearby ArenaNet name that IS load-bearing (Q5): AgAgent:974 `m_point.position !=
AGENT_INVALID_POSITION` @0x005ffb78.
Callers' module named by its own asserts: AgTrack:457 `state->clientControlled`
@0x0060560b -- so caller 1 lives in P:\Code\Engine\Agent\AgTrack.cpp.

## Q4 -- CALLERS

`--xrefs 0x00709E90`: exactly 2 direct rel32 callers, 0 data words holding the VA.
(Indirect/vtable dispatch is NOT covered by that scan -- absence there is not proof.)

CALLER 1 -- 0x00605802, inside the function entered at 0x006055E0.
*** YES: THE SNAP'S GATE 2 IS THIS CALLER, AND THE pathCount==0 TEST IS AT
    0x0060580A / 0x0060580D. ***
  0x006057EC lea eax,[ebp-0x44]  push   ; arg6 outPath
  0x006057F0 lea eax,[ebp-0x60]  push   ; arg5 &count
  0x006057F4 push 4                     ; arg4 maxCount = 4   (IMMEDIATE INT)
  0x006057F6 push ecx / fstp [esp]      ; arg3 range = 300.0f  (from [0x946564])
  0x006057FD push eax (= [ebp-0x80])    ; arg2 to
  0x00605801 push eax (= [ebp-0x5c])    ; arg1 from
  0x00605802 call 0x709e90
  0x00605807 add esp, 0x18              ; 6 dwords, cdecl
  0x0060580A cmp dword ptr [ebp-0x60], edi
  0x0060580D je  0x60582b               ; <-- pathCount == 0 -> bail
  edi == 0 here (OBSERVED): zeroed 0x006056B3 `xor edi,edi` and again 0x00605756
  `xor edi,edi`; no write to edi on the reaching path 0x00605756..0x00605802
  (checked instruction by instruction); the only `mov edi,1` is at 0x00605822, AFTER.
  On success it goes on to 0x00605819 call 0x5fef70(&[ebp-0x5c], &[ebp-0x44]).
  Function owner: AgTrack.cpp, args named by its own asserts --
  [ebp+8] = `state` (field +0 is `clientControlled`, AgTrack:457 @0x0060560b),
  [ebp+0xc] = `source`, an agent (AgTrack:458 `source.GetWorld() == WORLD_SYNC`
  @0x00605625 reads [ebx+0x24]; so WORLD_SYNC == 0).

CALLER 2 -- 0x0081AF51.
  0x0081AF25 fld [0x946280]             ; arg3 range = 10000.0f
  0x0081AF2B lea eax,[ebp-0x94]  push   ; arg6 outPath
  0x0081AF32 lea eax,[ebp-0x98]
  0x0081AF38 mov dword ptr [ebp-0x98], 9 ; PRE-SEEDS *count = 9 before the call
  0x0081AF42 push eax                   ; arg5 &count
  0x0081AF43 push 9                     ; arg4 maxCount = 9   (IMMEDIATE INT)
  0x0081AF45 push ecx / fstp [esp]      ; arg3
  0x0081AF4F push edi                   ; arg2 to
  0x0081AF50 push eax (= [ebp-0xd0])    ; arg1 from
  (caller 1 does NOT seed *count, so seeding is defensive, not required.)

*** arg4 = maxCount CONFIRMED BY BUFFER ARITHMETIC, two independent callers ***
  caller 1: buf [ebp-0x44], 4 * 16 = 0x40 -> ends exactly at [ebp-0x04]
  caller 2: buf [ebp-0x94], 9 * 16 = 0x90 -> ends exactly at [ebp-0x04]
  [ebp-0x04] is the stack-cookie slot in both frames (caller 1 sets it at 0x006055F0).
  Two different capacities, two different frame sizes, both closing to the byte.

## Q5 -- UNITS AND COORDINATE FRAME

SAME WORLD FLOATS AS m_point (+0x78). OBSERVED, via the chain that produces the args:

  caller 1 fills BOTH endpoints with the same helper, 0x005FF820 (__thiscall, `ret 8`,
  ecx = agent):
    0x006057AD call 0x5ff820(&[ebp-0x5c], time)  with ecx = ebx = `source` -> arg1 FROM
    0x006057BA call 0x5ff820(&[ebp-0x80], esi)   with ecx = [ebp-0x48]     -> arg2 TO
  0x005FF820 copies 16 bytes from agent+0x88/+0x8c/+0x90/+0x94 when the cache at
  agent+0x48 is fresh (0x005FF829 `mov ecx,[edx+0x48]`; 0x005FF83F..0x005FF85A), else
  falls through to 0x005FFB40 to recompute.
  0x005FFB40 reads the position as FLOATS from agent+0x78 / +0x7c:
    0x005FFB50 fld dword ptr [esi+0x78]
    0x005FFB67 fld dword ptr [esi+0x7c]
  and guards them with ArenaNet's own assert:
    0x005ffb78  AgAgent:974   m_point.position != AGENT_INVALID_POSITION
  => the value handed to arg1/arg2 is agent m_point.position, or a cache extrapolated
     from it. SAME FRAME. Not tiles, not normalised, not model-local.

  Corroboration in the gate-2 caller itself: 0x00605643..0x00605669 copies
  [ebx+0x78],[ebx+0x7c],[ebx+0x80],[ebx+0x84] as one 16-byte block and `fld`s the
  first as a float (0x0060564C) -- m_point is 16 bytes shaped {float x, float y,
  int32, int32}, exactly the outPath element stride from Q2.

  AGENT_INVALID_POSITION (OBSERVED) = +inf. [0x00948654] raw 0x7f800000, the operand
  of the fucom guarded by AgAgent:974.

CONSTANTS READ OUT OF .rdata (imagebase 0x00400000; values are clean and named, which
is itself the parse check):
  [0x00a545b0] = 1.1920928955078125e-07 = FLT_EPSILON (2^-23)
        the early-out at 0x00709EDD is `distSquared <= FLT_EPSILON`, i.e. from == to.
        It is a DEGENERATE-INPUT test, NOT a range gate. Falls through to
        "emit `to`, count = 1".
  [0x00945b40] = 0.5 (double)  added to the path length at 0x00709FB1; the backend
        0x7219d0 is asked for len + 0.5.
  [0x00948654] = +inf          AGENT_INVALID_POSITION
  [0x00946564] = 300.0         caller 1's arg3 range (world units)
  [0x00946560] = 100.0         AgTrack threshold
  [0x00946280] = 10000.0       caller 2's arg3 range (world units)
  ranges of 300.0 and 10000.0 in the same float frame as m_point are ordinary GW world
  units -- consistent with the scale of a compass/aggro radius, not tiles.

arg3 "range": the NAME is a RECONSTRUCTION. Inside 0x00709E90 arg3 is never compared
against anything -- it is only forwarded as a float to the three backends. The two
caller values (300.0, 10000.0) are what make "range/max search distance" the natural
reading; this lane did not prove it.

## BODY STRUCTURE (context for whoever hooks it)
ctx = *( [call 0x47f660 -> eax] + 0x14 ), @0x00709E99/0x00709EA4, cached at [ebp-4].
  A. 0x00709EE5 not-taken: |to-from|^2 <= FLT_EPSILON -> outPath[0] = *to, *count = 1
  B. 0x00709F1D: if 0x7372a0(ctx+0x78) -> tail 0x737350(ctx78, from, to, 0, range,
     maxCount, outCount, outPath) @0x00709F36
  C. else 0x7219d0(ctx->0x74, from, &unitDir, len+0.5, 1, maxCount, outCount, outPath)
     @0x00709FCA; then if *count != 0 AND dist(last, from) >= len AND
     last.plane == to.plane AND 0x722b90(ctx->0x74, ctx+4, to) -> overwrite last point
     with *to and run a backward dedupe loop that `dec`s *count on each duplicate
     (0x0070A075..0x0070A0A5); otherwise tail 0x721a30(ctx->0x74, from, to, range,
     maxCount, outCount, outPath) @0x0070A0C6.
