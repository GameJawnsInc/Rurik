# The minimap ladder — from static reads to an authored-map compass

**Arc:** `studies/minimap/` · **Companion:** [FINDINGS.md](FINDINGS.md) · **Date:** 2026-08-14

This is the ordered work list, cheapest first. Every rung states its **prediction before its procedure** (house rule, CLAUDE.md "A probe states its prediction first") and every rung is falsifiable — the refutation is written into the rung. Client rungs are marked **NEEDS OWNER GO-AHEAD**: parallel sessions share one harness and one `main`, and a client run against ArenaNet is never in scope here (all runs are loopback, our DH, caged).

**What is already settled** (see FINDINGS): the compass/mission-map/world-map ground image is a per-continent ATEX atlas in the archive, cropped by the area row's footprint rect at **one texel per terrain cell**, the footprint selected between two by the `0x0199` map-type byte our server sends, and the *continent* selected by the identity map from that same message's map-id word; the compass draw/ping is the opcode **triple** `0x002B` (c2s draw) / `0x0091` (s2c broadcast) / `0x0092` (s2c ping), and our server drops `0x002B` unnamed; the fog stream is `0x008B` declare + `0x008A` payload with `0x008C` as the incremental mark.

**TIER 1 COMPLETE (S1–S13, 2026-08-14). TIER 2's C1, C2 and C3 have all RUN (2026-08-15). Only C4 is left, and Tier 3 is an owner decision. FINDINGS §6d, §6e, §6f.**

**Where the arc stands in one paragraph.** The compass draws the ATEX atlas; the fallback was never a property of our server but of an archive whose atlas tile rows were replacement-armed (§6e). We hold the lever: flipping the `0x0199` map-type byte changes the picture on a row whose two footprints differ and changes nothing where they are equal (§6f.1). The picture follows the **slot**, not the terrain — the identical map file at two slots gives two continents' art (§6f.2) — while the terrain supplies the **addressing**, setting both how far the crop reaches and where within the footprint it sits. On an authored 64-cell map that costs **~40% of the disc to fallback**, which prices Tier 3: A1 cannot fix it, only A2 can. *This paragraph used to read: "every compass frame in the vault is the client's NULL-image fallback tile … So the ladder no longer starts at 'compare our crop to a compass'; it starts at 'find out why the compass image is NULL' … C1 is re-scoped into that diagnosis and C2/C3 are gated behind it."*

**The compass draws the atlas.** On build **38833 against its own archive generation**, retail map 148, loopback and caged, the compass renders a recognisable crop of Ascalon City: χ **+0.250 … +0.279** against rung S5's offline prediction of **+0.286** for that map's crop, with the previously-classified fallback frames re-measured at **+1.154 … +1.160** by the same sampler as a control. **The whole-disc fallback was never a property of our server**, C2 and C3 are ungated, and risk 7 is retired.

**What is open is now an ATTRIBUTION question, not a diagnosis** — client build, archive generation and the `0x0199` id form all changed together and C1 separates none of them. That is rung **S13**, whose first leg is static and free. Everything below about "why is it NULL" is retained as the record of hypotheses killed while the premise held; the static work stands, the framing does not.

---

## Tier 1 — static, no client, no harness contention (hours each)

### S1. Correct `s_worldData` in `consttable.py` — **DONE 2026-08-14**
**Prediction (a regression claim, since the measurement is in hand):** stride **48**, count **10**, `stride_from` = the accessor's own `lea/shl` at `0x005A93CD`, corroborated by `WORLDS = 10` and by `count == chunkCX × chunkCY` on 7 of 7 worlds. The current `stride=24, "UNSETTLED"` at `toolkit/clientscan/consttable.py:539` (printing `20 × 24`) is wrong; 48 is already in its own rival-stride list.
**Procedure:** edit the row; `test_consttable.py` re-derives closure from the anchor, so a wrong stride reddens it. Keep the rival-stride print — 48's divisors still close, and the module's blind-spot doctrine says report that, not hide it.
**Cost:** ~30 min. **Do first — it is a correction to a committed claim.**

**Outcome — prediction met, and the re-verification made it stronger than predicted.** The row is `10 × 48` at file `0x635210` (VA `0x00A36210`), `pad +4`, and `test_consttable.py` is green at **69 checks** (floor 61 → 69) with a new section 7b. Three things the prediction did not contain:

1. **The count is ArenaNet's own `arrsize`, not our `480 / 48`.** The accessor bounds *itself* — `0x005A93B7 cmp esi,0xa`, guarding `index < arrsize(s_worldData)` at `ConstWorld.cpp:41`. The prediction cited `ConstWorldMap:1313 world < WORLDS`, which is a **different translation unit and a different function** (the chunk-tile getter at `0x005A93E0`). Both bound at 10, so they corroborate; the accessor's is the direct witness and it names the symbol. The row now *declares* its count, the `s_missionClientData` shape.
2. **That assert's `__FILE__` string IS the row's anchor** (`P:\Code\Gw\Const\ConstWorld.cpp` at VA `0x00A363F0`), so `base + 10 × 48` lands on it — the assert and the table are one measurement from two directions.
3. **The 7-byte accessor pattern is unique in `.text`**, so section 7b finds it by SHAPE rather than at a remembered address. The `lea`+`shl` pair alone occurs 22 times; the trailing absolute `add` is what makes it a witness. Read as bytes — carve-out (1) scopes capstone to two named files and this is neither.

**The rival-stride print was kept, as instructed, and it earned its keep:** 24 is still listed, because every divisor of 48 closes on the same base. What *can* refute 24 is a column — `+0x14` is `512` (`CONST_WORLD_CHUNK_SIZE`) on all ten 48-byte records and ragged on the twenty 24-byte ones — and that is now recorded as a stated **limit of `rival_strides`**, which tests closure and the index column and never column coherence. Four sabotages built and run (3, 5, 2, 1 red). The one that does *not* redden under the pre-fix row is the useful one: the left-edge corroboration passes at `20 × 24`, because 24 divides 480 and two readings of the **same 480 bytes** cannot see a divisor stride. Only the code witness can — which is the general lesson for S3, where the tile tables get walked.

### S2. Pin the footprint selector into `maprows` §9 item 4 — **DONE 2026-08-14**
**Prediction:** the branch at `0x008C276B-0x008C2782` is the only place the compass reads either footprint; `MissionCliGetMap()==0 → A`, non-zero → B. `GmMapView` will have its own read and may or may not use the same rule.
**Procedure:** `codescan --field 0x48/0x58` in `CompassMap` then `GmMapView`; note `--field`'s declared blind spots.

**Outcome — one half CONFIRMED, two halves REFUTED, and the procedure itself was the trap.**
1. **The selector is CONFIRMED, 6 of 6 readers image-wide, and now NAMED**: `MISSION_MAP_OUTPOST == 0` / `MISSION_MAP_GAME == 1` / `MISSION_MAPS == 2`, all three read out of compiled comparisons. `+0x48` is the OUTPOST rect, `+0x58` the GAME one.
2. **"The only place" is REFUTED.** The compass reads the footprint at **three** sites and the branch named in the prediction is the *narrowest* — it takes the origin only and is a re-fetch of a rect the same function already obtained from a helper 22 instructions earlier. Six readers exist image-wide, closable because the table base `0x0096DE38` occurs **exactly once** in the whole image.
3. **`GmMapView` is REFUTED as the map side's reader** — 0 instructions at both offsets over its whole span, no accessor call, no selector call. The map read is `GmMapHelpers.cpp`, and it has a **zero-fallback the compass does not**.
4. **The 172 became 20.** 136 of the 172 have A all-zero and 16 have B all-zero; only **20 rows carry two different non-zero rects**. That is C3's real population, and it was the difference between a falsifiable rung and one that flips a byte on a row where the client falls back or blanks.

**The rung's own prescribed procedure would have missed the branch it was run to find.** `codescan --in <module>` bounds from `asserts.py`, and `--bounds CompassMap` ends **0x1D0 bytes before** `0x008C2774`; `--field 0x48 --in CompassMap` returns three rows and none of them is it. `--in GmMapView` answered a confident, wrong-shaped zero the same way. **Use the directory prefix.** Recorded in FINDINGS §3.3 as the same class of failure `test_codescan.py` §8/§9 records for `PrApi`, from the other direction: there a name was too *wide*, here an assert-derived range is too *narrow*, and the tool cannot warn because it does not know where the module ends.

**Independent cross-corroboration the rung could have failed:** reader #4 of the six is `ChCliApi.cpp` `0x00811C64`, which rung S8 reached from the *opcode* side without seeing S2's list — the fog mark's own footprint read, `shr`'d by 5 into the block grid. Two sweeps sharing no premise landing on the same site.

### S3. Commit the atlas reader as `toolkit/clientscan/worldmap.py` — **DONE 2026-08-14, then AUDITED**
**Prediction (stated before re-running on a third archive):** 409 tile records over 7 worlds; trailing u32 = 0 on 409/409; ≥401 resolve to `ATEX`; the 8 that do not are exactly {387674, 387678, 387680, 387682, 387684, 388093, 388095, 388097}, all in worlds 0/1.

**Outcome — every clause MET, and two corrections found while measuring.** All four predictions plus PLAN's worlds-0/1 clause (3 and 5) and "worlds 7/8/9 have no record". The corrections: there are **three** tiers, not two (the shape search found the satellite getter without being asked, retiring a FINDINGS UNVERIFIED), and **not every tile is 512×512** — 15 of 484 are 256×256, four of them chunk-tier and none at a grid edge, which the four-tile sample the old row rested on could never have shown.

**The tables are located STRUCTURALLY and it paid.** The task allowed pinning by VA as a stated limit; that was not needed. The getter's loop plus tail is a unique byte shape carrying four facts that must agree, and the module refuses on any disagreement — including the CX-offset-used-by-the-bound versus CX-offset-used-by-the-index check, which is **invisible everywhere else because the two reads agree on the real image**, and whose sabotage reddens exactly one check. Without it the module accepts a build whose two reads disagree and indexes with whichever it parsed.

**AUDIT (independent, same day) — three sabotages scored `ALL CHECKS PASSED, exit 0` against the file as delivered, and all three are shapes this repo has a scar for**: a `main()` that never calls the write guard (the AST checks parsed the file on disk, not the module under `--module`, so every syntax-tree check was exempt from the one mode the sabotage table is measured in); a provenance row stamping `build: 38797` as a constant (condition 2 satisfied in form while recording a literal); and a reader that never reads the trailing u32, invisible because all 492 real records carry zero — the very fact the prediction asserted. A fourth check **could not fail** and was deleted. **Floor 69 → 78, MEASURED, zero headroom; 65 client-only; 40 bare. 19 sabotages built and run, all 19 redden.** Report the 78, not the 69.

**Uncommitted in the tree:** `toolkit/clientscan/worldmap.py`, `toolkit/clientscan/test_worldmap.py`, and the `CLAUDE.md` suite entry (already carrying the post-audit floor). `test_srclint.py` §7 is green in both directions at 83 files.

### S4. Find the producer of the compass's world index (`[ebx+0x84]`) — **DONE 2026-08-14**
**Prediction:** it is `s_missionClientData[map].continent` (`+0x04`, UPSTREAM offset) directly or through a small mapping — the continent histogram over 888 rows has exactly the 7 values that carry tile arrays.

**Outcome — the conclusion is MET and the supporting argument is REFUTED, which is the more useful half.** The map is the **IDENTITY**: `0x0084DE50` is four instructions and is exactly `s_missionClientData[missionContext+0x230] + 0x04`, its return going straight into `[CompassMap+0x84]` at `0x008C1C78` and from there, unmodified, into the tier getter that bounds it by `WORLDS`. No table, no offset, no remap. **Two agents reached it independently** (this rung from the field side, rung S5 from the constructor). It also promotes `areatable.OFF_CONTINENT` from UPSTREAM to CORROBORATED — while leaving the *name* "continent" ArenaNet-unattested (`asserts.py --grep '(?i)continent'` → 0 sites; the client's word is `world`).

**But `{0,1,2,3,4,5,9}` ≠ `{0,1,2,3,4,5,6}`** — the "7 = 7" was a coincidence of cardinality. **World 6 has 47 non-NULL tiles no area row asks for; world 9 has five area rows (883-887) and no tile record in any tier**, so those five maps take the tiled fallback unconditionally. And the containment test that "failed as a discriminator" now has a cause rather than a shrug: `s_worldData` records **0 and 7 are byte-identical across all 48 bytes**, as are 3 and 8. It was a method applied to two copies of one record. **`GmMapHelpers:28` was NOT a clearer path** — its two callers pass a *view state* field, not an area row — and that negative is reported rather than dressed up.

**Consequence carried into S5 and C2:** worlds 2 and 4 have identical grids and different arrays, so **Kamadan must be rendered from world 4, array `0x00BF2A88`** — a renderer picking by grid shape gets Cantha's art for an Elonian map and nothing would catch it.

### S5. Render the atlas crop offline — **OFFLINE HALF DONE 2026-08-14; the on-screen half is REFUTED AS FRAMED and becomes C1**
**Prediction:** decoding Kamadan's covering world tiles and cropping at 1 px/cell produces a picture recognisably matching an existing vault screenshot of Kamadan's compass; map 143's crop must NOT match.

**Outcome, three results:**
1. **The scale is CLOSED at one texel per terrain cell — OBSERVED, was RECONSTRUCTION.** From the client's own arithmetic (the footprint origin is *added* to the crop's local coordinate, then `& ~0x1FF` and `>> 9`) and from a five-scale population test: at 1.0, five continents' touched-tile sets **equal** a world's tile set exactly and 629 of 631 rows are fully covered; at 0.25/0.5/2/4, **zero** continents match and coverage collapses. Kamadan alone reproduces it — 100% at 1.0, **0.0% at both 0.5 and 2.0**, where the crop lands entirely on NULL slots and renders nothing.
2. **The crop is a recognisable picture.** Kamadan renders as a tropical island city on turquoise water with docks and a walled compound; area 148 as Pre-Searing Ascalon with walls, roads, rivers and portal glyphs.
3. **THE COMPARISON COULD NOT BE RUN, AND THAT IS THE RESULT.** Every compass in the vault is the client's **NULL-image fallback**, positively identified as archive file **9153** (64×64 DXT1, loaded at `0x008C1C96`, tiled by `0x008C1E00`) — six frames, three maps, four sessions, chromatic ratio +1.081..+1.110 against the fallback's +1.164 and the four atlas crops' −1.718..+0.286, with **0 of 7,948** atlas windows in band, no translation improving the fit (best at 0,0 across a walk) and no rotation improving it (best at 0° while the bezel visibly rotates). **It fires on retail map 148 too, with all four of that map's tiles present in the archive the client opened.** A failed metric is kept as a negative: a normalised-texture template match does not discriminate and points the wrong way, because z-scoring removes the one channel carrying the signal and the search spaces are wildly asymmetric.

**Two premises of the rung as written were also wrong and are corrected:** the pipeline has no `atexlevel.decode_level` (the level codec is `toolkit/mapdata/atex.py:550`, reached with `dxt1` through `blocks_to_rgba`), and **no Kamadan frame exists anywhere in the vault** — map 449 was never served in any captured session (rung S7).

### S6. Confirm the compass draw/ping opcode from the sender — **DONE 2026-08-14, run TWICE: once as confirmation and once as an adversarial attack on the same four claims, with each agent blind to the other**
**Prediction (stated as confirmation):** exactly one sender reaches `CharMsg:8129`'s bound at `0x00920262`, writing GAME_CMSG `0x002B`; its s2c partner is `0x0091`; knots low-half-first; a single knot is a click, a run is a drag.

**Adjudication, claim by claim, with the label that SURVIVED the attack:**

| claim | verdict | label |
|---|---|---|
| **`CompassCanvas.cpp` is the sole producer of `0x002B` image-wide** | **SURVIVES, strengthened.** The attack found the earlier sweep genuinely incomplete — there are **two** send entries, not one (`0x007DCB10` adds 40 call sites, total **214**) — and the claim held anyway under a value-first sweep that cannot desync: of **134** materialisations of `0x2b` in `.text` across six encodings, **exactly one instruction** sits inside any send-call body, at `0x0092024B`. Controls on 0x78/0x39/0x3d/0x2c/0x40 find each one's producer and show the method *can* surface a second candidate (0x40 surfaces six and they separate on which slot the buffer's `lea` names) | **OBSERVED** |
| **thunk `0x00816110`: 1 caller, 0 raw dwords ⟹ reachable only from the compass** | **facts SURVIVE and are stronger than stated** (0 occurrences in *all five sections* means a runtime-built table could not hold it either, since the store's immediate would be in `.text`); **the inference is a NON-SEQUITUR and is WEAKENED as written** — those two facts bound the thunk, not its caller. Re-established a different way: reverse closure over 135,455 direct call edges from `0x008C0840` converges at depth 25 on **nine functions, all inside `0x008BA79D..0x008C0840`**, no `Gm*` module, and the only raw-dword hit along it is an MSVC switch table holding three compass VAs | **OBSERVED** for the closure over direct calls; the "therefore" must be rewritten to cite it |
| **s2c handler `0x0091EA20` feeds the compass polyline** | **SURVIVES, and the weak last hop closed from a new direction:** frame msgId `0x1000009E` has **exactly one poster and exactly one subscriber** (`Compass.cpp` @ `0x008BB5D7`), against controls that are not degenerate (`0x10000090` → 1 poster + 3 subscribers; `0x1000014E` → 2 + 7); and the polyline renderer `0x008C10D8`'s **only caller is `0x008C0840`** — the same function that flushes knots to the server. One function draws the ink and sends it | **OBSERVED**, except subscription → switch arm → point array, which stays **RECONSTRUCTION** and neither dive had said so |
| **the semantic NAME "draw/ping"** | **WEAKENED.** `DRAW` is ArenaNet's word but appears **only on the s2c side** (`CHAR_MAX_DRAW_MINIMAP_KNOT_COUNT`); the c2s sender names only `knotCount` and `message.knotData`. **`PING` occurs zero times** in all 19,758 readable asserts — it is GWW's player-facing word, not the binary's. The c2s name is *inherited* through the descriptor pairing (`0x2b`'s SEND and `0x91`'s RECV differ by exactly one leading `u16`) | s2c **high**; c2s **medium**; and any `COMPASS_PING` label is **medium** with its `why` saying the word comes from GWW |

**Two rival readings taken seriously and refuted**: hero-flag placement (`CompassCanvas.cpp` really does carry `:416` and `:902 aiFlag < AI_COMMAND_FLAGS`, but that path reaches no send entry, and a flag order is one point where this payload is 1-16 packed pairs gated on `knotCount >= 2`) and target call (no agent id anywhere; the u32 comes out of a free list). Ping-vs-draw are correctly **conflated** — one message carries both.

**And `0x2b` really is the opcode, which was the question worth opening.** Inside `SendMessage` the buffer's first dword is named `messageId` by the client's own `MsgChannel:392 messageId < channel->m_sendMsgs.Count()`, indexing an 8-byte-per-id descriptor array, and `0x007DCFCA or ax, cx` is where the `0x8000` game-channel stamp is applied. It is both the wire opcode and the descriptor index, which is what an opcode is in this protocol.

**Beyond the prediction, five results the ladder uses:** the 250 is **milliseconds** on three witnesses including the wire (Δ = 0.251988 s and 0.249950 s between consecutive flushes of one stroke); a second **25 ms** knot sampler nobody had reported; the knot space is **absolute world / 96.0**, discriminated against three rivals by the client's own 4,500-unit click bound; the u32 is a **client-allocated stroke handle** from a free list, corroborated by an id that *decreases* (7 then 6) and by two flushes of one stroke **sharing** id 6; and the **over-range hazard is VOID off the wire** (`MsgConn.cpp`'s unpacker refuses `count > 16` before dispatch, bound tight). **s2c `0x0092` is a third member of the family — the ping** — reaching the same widget via `0x1000009F`, and a 1-knot `0x0091` on an unknown key draws the same ripple, which is the cheapest possible server-side demonstration.

### S7. Transcription-only: recover the hostile compass-dot colour — **DONE 2026-08-14**
**Prediction:** the answer is already on disk in the `npc_allegiance` run artifacts or its frames.

**Outcome — the prediction is REFUTED for the probe and MET incidentally from elsewhere, and the negative is now hard rather than soft.** The probe's **wire** capture exists and is citable for the first time (`vault/captures/authsrv/authsrv-20260806T103830-c2.jsonl` and `-20260806T104329-c2.jsonl`: complete four-step runs five minutes apart, `map_id 148`, build 38797, all four tokens verbatim, found only by grepping the plaintext rather than the decoded-integer form). **The visual answer was never captured**: no harness session directory exists for either timestamp — the probe was driven against a hand-launched client — and a modification-time sweep of *every* image under `vault/` finds **zero** frames in a two-hour window bracketing both runs. This is the first kind of NOT FOUND (the frames do not exist), which is the cheap kind to state and the expensive kind to fix.

**Separately, rung S5 answered the general question from a frame that does exist:** `vault/captures/harness/20260811T161925/hold007.png` shows the player's green dot at the exact centre of the Danger Zone circle and the hostile's dot **red** outside it. The four-token allegiance question stays open and needs the run S7 costed.

**Two by-products the arc should carry:**
- **No Kamadan frame exists anywhere.** Map 449 was never served in any captured session — zero hits for `map_id 449` across ~1,500 gamesrv/authsrv captures and every harness `gamesrv.log`; every session requests `map_id=148`. The live captures hold no images at all. **This is what retargets S5's and C1's Kamadan framing.**
- **The nine `frames-2026081*` directories are NOT harness output** — nothing in `toolkit/` generates that name; they are an ad-hoc external screen-capture tool run in parallel. `frames-20260811T134036` is **contaminated** (its later frames are a Discord window, not the client) and must not be used past that point. Best clean candidates: `frames-20260811T144516/0059_1786473961.660.jpg`, `frames-20260811T135754/0071_1786471129.777.jpg`, `frames-20260811T160449/0032_1786478736.258.jpg` (the last carries an on-screen "Lakeside County / Explorable Area" banner, so its map identity is self-contained). **Caution:** maps 148 and 146 share one archive file but differ in `explorable`, so frames from the two are not guaranteed to be cropping the same footprint rect.

---

### S8. Settle the fog/exploration opcode partition — **DONE 2026-08-14, run TWICE from opposite ends with the rival answers withheld**
**Prediction (this rung existed only as FINDINGS §4.2's CONTESTED item):** one of two readings is right — (a) two mechanisms, each dive naming one, or (b) the dives disagree on which handler reaches `ChCliApi:201 @ 0x00811D12`.

**Outcome — reading (a) is right, deep dive A's opcode LABELS were wrong, and the two routes agree on every VA, every partition and all five live counts.** `0x008C` owns `0x00811D12` as the **sole** direct reference to `0x00811BE0` (1 call, 0 words holding the VA in any section); the init stream is **`0x008B` declare + `0x008A` payload**, sharing the RLE expander `0x00817550`, whose callers are exactly those two and nothing else; `ChCliApi:1641` belongs to `0x008A`; **`0x0089` is an AgentView mannequin message and not in this subsystem at all** (`AvMannequin.cpp:225 index < 64`, and the neighbouring `AvFlag.cpp:86` bounds a *different* array at 8, so a hero-flag reading is refuted as a certainty). Live: 0 / 8 / 8 / 47 / 28.

**The cause of the wrong reading is a tool-shaped trap worth more than the answer.** `asserts.py --at X` is not a function bound — it is `near(va, span=2000)`, a fixed 2,000-byte window, and the four functions of this family occupy **998 bytes** total, so the windows for `0x0089`'s 5-byte thunk, `0x008A`'s callee and `0x008B`'s callee all three swallow `0x00811D12`. Both halves of the wrong attribution are exactly what the default span produces. The fix is not a tighter span; it is the two questions the window cannot answer and `codescan` can: where the function *ends*, and who *calls* it.

**Beyond the partition, three results C4 needs**: `0x008C` is **inert** until the init pair has run (`0x00811BEE cmp [eax+0x5ac],0; je`), so `smsgsweep`'s 108 prior sends of it measured nothing; the init path posts **no** frame message, so an init-only probe may see nothing on screen even on a perfect payload; and `0x008C`'s bit writes are gated by a **per-map explorable mask** from `Engine\Map\Map.cpp`, which decides whether it can unfog anything on our own geometry.

**The RLE expander was re-derived and then run on ArenaNet's own payloads: 8 of 8 close exactly at the declared byte count, four rival framings 0 of 8.** Five further wire checks are in FINDINGS §4.2, of which the strongest is 47/47 marks inside the area row's own footprint at `>>5` with three shift rivals at 0/47 and a measured non-vacuity bound (only 3-8 of 888 rows contain a given mark set).

### S9. Why is the compass image NULL under our server? — the static half — **DONE 2026-08-14, run as TWO agents, one per hypothesis, each told to refute its own. BOTH SUCCEEDED.**
**Prediction:** the `CompassMap` object is constructed *before* `missionContext+0x230` holds our map id, so `[compassMap+0x84]` latches row 0's `+0x04` (continent 3, all-zero footprint) and `ConstWorldMapGetChunkFile(3, {0,0})` returns NULL, taking the tiled fallback. **Refutation:** if the construction site is provably downstream of the `0x0199` handler — or if a destructor/rebuild exists that re-runs the constructor per instance — the prediction is wrong and the cause is the texture load at `0x008C21C0` instead.

**Outcome — the rung's refutation clause fired on BOTH sides, which is the most useful outcome it could have had.** The prediction is refuted and so is its own stated alternative. Full record in FINDINGS §6.

1. **Hypothesis 1 REFUTED, four ways.** The pre-message value of `missionContext+0x230` is **888**, not 0 — `0x00855E3E mov dword [esi+0x230], 0x378` in the `MsCliApi` context ctor, re-written by the teardown at `0x00856119` — so **row 0 is unreachable**. The sentinel that *is* reachable is not silent and not a fallback: `0x0084DE50` routes through the asserting accessor (`0x005A858F`, `ConstMission.cpp:3005`), row 888 lands on that file's own `__FILE__` string so `+0x04` reads `0x5C65646F`, `0x005A93E0` asserts that too and then hands it to `0x005A93B0`, whose `×48 + 0xA36210` is dereferenced at `0x005A9415` far outside the image — **two assert dialogs and an access violation**, and `0x00487BC0`/`0x00488210` have no early gate. The HUD builder is structurally downstream (`0x004E7E00` reads map id, map type and the area row as its first three acts; `0x004E1254` branches on the observer bit only the `0x0199`-class handlers set). And on the wire `0x0199` precedes every character/agent message by **0.56–0.89 s, 17 of 17 instances** across ArenaNet's server and ours.
2. **Hypothesis 2 REFUTED to the limit of static evidence, and the decisive argument is not the log.** The fallback tile the compass is drawing was loaded by **the same loader** `0x00679A90` from the same archive in the same process on the same frame (`0x008C1C96`). Beneath it: 484/484 atlas tiles already DXT1 on four archives; every tile our six slots touch fully decompressing and passing `atex.parse`, byte-identical across all four; 0 of 888 footprints able to reach a 256×256 chunk tile (with 18 other world-3 cells covered as the non-vacuity control). Residual: a size/id-dependent refusal in `0x00470F50`/`0x00470B60`, **UNDECIDABLE STATICALLY**, ~1 h to close.
3. **THE `Gw.log` DISCRIMINATOR IS VOID AND MUST BE STRUCK FROM C1.** `0x008C21EB cmp dword [ebp-4], 0xf; jne` puts the line on the **conversion** arm alone — a failed load returns 0 at `0x008C21E4` with no log, an already-DXT1 surface returns at `0x008C21F8` with no log — so with 484/484 tiles DXT1 the line is unreachable and its absence from all 26 vault logs is exactly what a perfect load produces. `0x008C2203` is the **only** logger call in all nine compass translation units. **The compass writes nothing to `Gw.log` under any condition on this build.** The sink itself is proven by a positive control in the same files: same function, same severity 1, as the `AuthSrv (%S)` banner at `0x0049498D` seen in 25 of 26 logs.
4. **The destructor is FOUND** — `0x008C22A0`, 1 direct reference, at `0x008BB6FF`, with `0x008BB73F mov dword [esi+0x4c], 0` — closing half of a FINDINGS §7 open item. Per-*instance* rebuild stays RECONSTRUCTION (the create trigger is reached through a vtable store at `0x00824606`).
5. **"The fallback" is SEVEN routes, four of them ordinary edge fill** (`--xrefs 0x008C1E00`), so fallback *pixels* are normal and only a **whole-disc** fallback is a fault. `CompassMap:472` cannot fire as a symptom — `0x008C202B je 0x8c2052` jumps over it when the surface is NULL. And map 148 touches **two** tiles, not four, under the half-open loop at `0x008C2807`.

**THE THIRD HYPOTHESIS, opened by the adjudication and the reason this rung was worth running: the compass ground layer is bounded by a LATCH-ONCE copy of the LOADED MAP's own cell dimensions.** `0x008C28D0` (one caller, guarded `0x008C28DE cmp dword [ebx],0; jne` exactly the way `+0x84` is) calls `0x0070A5C0` — a four-dword copy of `Engine\Map`'s rect with no validity check — stores it at `[CompassMap+0x60..0x6c]`, divides by the 96.0 at `0x0094DE10`, and writes `(0, 0, mapCellsX, mapCellsY)` to `[CompassMap+0x50..0x5c]`. The crop function `0x008C2450` **clamps every request against that rect before adding the footprint origin** (`0x008C2782`/`0x008C2792`), and an empty intersection goes to the fill block via `0x008C24E8` / `0x008C24F4`. Mechanism **OBSERVED**; cause **UNVERIFIED**. Two sub-forms: **H3a (timing)** — an unpopulated rect at first paint latches zero and every map draws the fallback forever, which is hypothesis 1's shape on a field the wire-ordering argument cannot reach (12 of 26 vault logs carry `Map file … Attempting to re-bloat.`); **H3b (dims)** — an authored map's 32/64/96 cells against a borrowed 416×512 footprint crops only the top-left corner, which does **not** explain retail 148 but is a live hazard for C2. **This also corrects FINDINGS §0's headline: the map file supplies no pixel and does decide the addressing.**

**Consequence for the ladder:** the surviving values of `[CompassMap+0x84]` on map 148 are `{0, 2, 4, 5, 7, 8, 9, ≥10}` — neither 1 (a picture) nor 3 (Elona's art, which S5 would have caught). C1 is re-scoped again below: **not a log read, a memory read.**

**Trap this rung adds to §8 of the risks:** `[CompassMap+0x68]` (map rect x1, `0x008C294C`) and `s_missionClientData[map]+0x68` (per-area texture, `0x008C2513`) sit forty instructions apart in one function on different base registers, and `--field 0x68` returns both.

### S10. Read `s_missionClientData +0x40/+0x44` — **DONE 2026-08-14**
**Prediction:** it is a third rect origin or a validity/extent pair, and `GmMapWorld.cpp`'s five-pair compare at `0x0054DB42` against two BSS globals is a "does this area have a real footprint at all" test rather than a selector. **Refutation:** if the two globals are ever non-zero at that site, or if the pair correlates with a field `maprows.py` already reads, the reading is wrong.

**Outcome — CONFIRMED structurally and by population, sharpened on the naming, and NEITHER refutation clause fired.** The pair is the area's **world-map LOCATION-TAG ANCHOR**, a third `Point` in the same terrain-cell units as the rects, **with the rect's CENTRE as its fallback** — `0x0054BD80`'s two arms, the 0.5 at `qword [0x00945B40]`, the same unsigned `fild` fixup on both. Named by its consumer, not by us: `0x0054DC78 push 0x555de0` creates a child frame bracketed both ways by `GmMapCtlLocationTag.cpp`, and two of the getter's five callers sit beside `GmMapWorld:1052 locationTagFrameId`.

1. **The five-pair compare is CONFIRMED and sharpened**: not "a real footprint" but *"any position at all"* — anchor OR either rect — and its effect is to **destroy / never create** the tag (`0x0054DBA1` → `0x00631C90` find-child → `0x00631220` destroy). **189 of 888 rows fail it.**
2. **Both globals are one 8-byte BSS `Point` with ZERO writers image-wide** (six occurrences, all reads, each hand-disassembled rather than trusted to the scanner's backwards decode), past the raw image, so the compare is against `(0,0)`. Corroborated by two sites that need no global at all (`0x0054E91A`, `0x0055AD9C`) writing the same predicate against a literal zero.
3. **Containment: 254 of 299 (84.95%) inside the row's own `FOOTPRINT_A`**, against **1.19%** for the load-bearing control — a within-continent shuffle that **excludes every pairing whose rect equals the row's own**, which is exactly the S4 trap (`s_worldData` 0 and 7 byte-identical) designed out rather than re-run into. Neighbour pair 0.67%, global derangement 0.89%. ~71× separation.
4. **0 of 486 anchor-carrying rows duplicate any field `maprows.py` reads.** The second refutation clause is dead.
5. **The 137 misses are structured and reported rather than trimmed**: all 71 type-18 rows (private-strip footprints, anchors on the landmass) and all 20 continent-5 rows (disjoint y ranges, and *not* a rigid translation — the median-offset correction lifts 0/20 → 3/20 and *lowers* continent 0 from 130 to 97). **UNVERIFIED** why.
6. **Two by-products worth more than they cost.** `+0x3C` is **`successor`, a row index bounded by `MISSIONS = 888`** (`0x0054C944` guarding `GmMapWorld:864`) — so it is not a coordinate, which honestly bounds what the neighbour-pair control proves, and it re-derives 888 from a second witness while naming the record type `MissionData`. And `s_worldData +0x20..+0x2C` **is a rect**, closing half of a FINDINGS §7 item. **A test that FAILED is kept as a negative:** anchor-inside-`s_worldData`-rect scores 99.18% and is worthless — its own neighbour control scores 97.94% and world 0's rect holds 100% of all 486 anchors.
7. **The result the arc actually needed: NO reader of `+0x40/+0x44` exists in `P:\Code\Gw\Ui\Game\Compass\`.** All 31 reader instructions across 10 functions are in `P:\Code\Gw\Ui\Game\Map\`, established by intersecting an exhaustive `--field` sweep with a forward window from all 108 accessor call sites and hand-classifying every survivor (the `MsCliApi` `[eax+0x44]` family is the *mission context* pointer chain, not a row read; the `0x0084E150` struct copy never calls the accessor). **S10 does not bear on the leading question, and that is the useful half.**

### S12. When is `Engine\Map`'s rect populated, relative to the compass's first paint? — **DONE 2026-08-14 (was: NEW, opened by the S9 adjudication, the cheapest rung on the board — and it over-delivered; outcome below).**
**Prediction:** `[globalCtx+0x14] + 4..0x10` is written by the synchronous map parse that must precede any render, so `0x008C28D0`'s latch always sees a real rect and **H3a is dead**, leaving H3b (authored-map dims) and the blit. **Refutation:** if the rect is written asynchronously — after the archive read completes, on a separate completion path, or after the frame that first paints the compass — H3a survives and is the leading explanation for every fallback frame in the vault, including retail map 148.
**Procedure:** `codescan --field 0x14` for the context slot, then `--field 0x4/0x8/0xc/0x10 --in 'P:\Code\Gw\Engine\Map\'` — a **directory** prefix, never a bare module name (`--in Map` pulls the whole geometry tree; risk 8). Bound every writer with `--dis` to its `ret` + `int3` run and take ownership with `--xrefs`, never `asserts.py --at`. Cross-check against `0x0070A5C0`'s sibling `0x0070A5F0` and against the re-bloat path, since 12 of the 26 vault `Gw.log` carry `Map file '0x0287d3' failed to load.  Attempting to re-bloat.` and that is a different load schedule from retail's.
**Cost:** 1-2 h, **static, no client, no go-ahead.** It can kill H3a for free, which is the whole reason it goes before C1.

**Outcome — DONE 2026-08-14. The prediction is MET, H3a is DEAD three independent ways, and the measurement kept going and produced the arc's first complete candidate explanation for every fallback frame in the vault.** Full record: FINDINGS §6c. The short form:
1. **`[ctx+0x14]` has exactly two writers image-wide** (a 1,089-call-site getter-window sweep, the S10 method): the install at `0x00707CC4` — which runs strictly *after* the build driver `0x00713360` returns the finished object — and the clear at `0x0070984C`. The context is per-thread TLS (`0x0047F660`), not a global.
2. **The rect writer is chunk `0x2000000C` — `mapbuild.py`'s own `MAP_PARAMS_CHUNK`, met from the client side**: handler `0x007129D0` (three instructions) → `0x0070D780` (MapParams.cpp), which rounds the chunk's dims to whole 3072.0-unit chunks and writes `[map+4..0x10]` + flags at `[map+0x14]`. One caller image-wide; sig/version/offsets match `mapbuild.py` byte for byte. The write happens **inside the synchronous chunk loop**, before install; terrain (chunk 2) then *derives its grid from the rect*, so any frame showing terrain is also evidence the rect ran.
3. **Ordering closed without the writer**: `0x0070A5C0` has **no NULL path** — a paint before any install crashes at address 4, and 26 sessions show no crash while §23's frame is `CompassMap` itself painting. Build+install run inside the **synchronous** dispatch of frame msg `0x10000098` (sender passes stack-local out-slots — a queue would be use-after-return), posted only by the MsCliApi instance-load path; the HUD/compass is a later message (S9's 17/17 wire ordering). Re-bloat moves *when* the build succeeds, never the order.
4. **The second half, INCLUDING A WRONG CLAIM THIS RUNG COMMITTED AND THEN CAUGHT — read FINDINGS §6c.4 before citing anything here.** The rung first exported `0x0287d3` (the id in this rung's own procedure line above, taken from a `Gw.log` re-bloat quote), got **64×64 cells, rect ±3072**, and built a whole hypothesis on it: that map 148 carries a region-sized footprint over a small file, and that our spawn at `(9826, 8077)` falls outside the map's rect and is the cause of the fallback. **`0x0287d3` is the authored SCULPT map** — which is precisely why re-bloat names it, since retail maps do not re-bloat — and map 148's id sat in `content/maps.toml` the whole time (`0x8001B97D` → `0x1B97D`). **Corrected: map 148 is 416×512 cells, rect (−18432, −24576, 21504, 24576) — exactly the footprint** (it *is* one of `maprows`' 319 join rows), with our spawn at cell **(294, 340)**, well inside. **So the latch covers the whole footprint and H3 is dead for retail 148 too** — which is C1's own third branch, reached statically: all three named hypotheses are refuted on that map, and the fault is **downstream of the crop** (the per-slot load at `0x008C21C0`, the blit at `0x008C2077`, or rung S5's own metric). H3b stands for AUTHORED maps and is now quantified — the sculpt map really is 64×64 against a borrowed 416×512 footprint.
5. **Three traps for the next reader**: the module prefix is **`P:\Code\Engine\Map\`** — this rung's own procedure line above says `P:\Code\Gw\Engine\Map\`, and `codescan --bounds` answers that spelling with *"no asserts, so no bounds"*, a silence every `--in` then converts into confident zeros. A four-float rect writer is invisible to a `--field` triple sweep when it writes through `&rect` — the `+0x0` store has no displacement byte to anchor on (codescan's own stated blind spot); the writer here was found by chasing the *value* (3072.0) and the chunk magic instead. And **a file id quoted in a log line is a claim, not an identity** — the id that identifies a map is the one in its content row, one `grep` away.
6. **One lead closed in passing, so nobody chases it:** `[CompassMap+0x80]` (FINDINGS §7's "UNREAD") is **not** an input to the tier getter — it is written into the per-slot cache record at `[ebx+esi*8+0x14]` while the getter takes `[+0x84]` and the chunk pair; the constructor zeroes it (`0x008C1C6B`) and `0x008C2A9E` bumps it. A **cache generation stamp**. — OBSERVED.

### S11. Name the two remaining opcode families in `overrides.json` — **DONE 2026-08-14. TIER 1 IS NOW COMPLETE.**

**Outcome — the prediction held, and half the work turned out to be already done.** The **draw/ping family was already filed** from §4.1's own adjudication (`0x002B` COMPASS_DRAW medium, `0x0091` COMPASS_DRAW_BROADCAST high, `0x0092` COMPASS_PING medium), and `0x002B`'s `DROPPED_ON_PURPOSE` row was already in `test_dispatch.py`. So the rung's real remaining work was the **fog family**, and all three are now named: `0x008A` **MAP_EXPLORATION_INIT_DATA** (high), `0x008B` **MAP_EXPLORATION_INIT_BEGIN** (high), `0x008C` **MAP_EXPLORATION_MARK** (**medium**).

**The one thing the rung did not predict, and it is the useful half.** Applying §4.1's own naming test rather than inheriting §4.2's `CORROBORATED` label: **`asserts.py --grep '(?i)explor'` returns 0 of 19,758 readable sites, and `'(?i)fog'` returns 0.** The client never says either word — the same shape that put `COMPASS_PING` at medium. ArenaNet's tokens are `accumMapInit*` and `mapBits`, so `0x008A`/`0x008B` keep **high** (their operative token is ArenaNet's; only the prefix is ours) while `0x008C` drops to **medium** (both `MARK` and `EXPLORATION` are ours). CORROBORATED describes the *mechanism*; `name_confidence` describes the *name*, and the two are not the same claim.

**The refutation clause fired as designed, twice.** `test_dispatch.py` stayed green because `0x002B`'s allowlist row already existed. `test_codec.py` went **red on `('GAME_SMSG','140')`** the moment the name landed — a named entry whose layout differs must be a declared exception — and adding it to `LAYOUT_FIXED` is the rung working, not a workaround. That exemption is the strongest of the three there: `0x0092`'s rests on the binary alone (0 live), `0x008C` has 47 live occurrences with its fields read back off the wire.

**A second red this rung inherited rather than caused, and fixed:** `test_codec.py` asserted every captured opening frame announces build **38797** literally, which went red on the two 38833 captures C1's runs wrote — perfectly-decoding frames called a failure. Now validated against `pinned.BUILDS` (`{38519, 38797, 38833}`), the same fix `test_origin.py` took: **793 frames green**, and a sabotage that drops the builds from the pin list reddens it, so the check is still refutable.

**Counts:** `messages.json` unmoved at **487** GAME_SMSG layouts; named GAME_SMSG **40 → 43**; `0x008A` keeps its imported 2-field layout and `0x008C` its corrected 4, so the name-only merge did what it claims. `test_codec` 29, `test_catalog` 13, `test_dispatch` 45, `test_provlint` 19, `test_srclint` 15 — all green.

---

### S11-old. The rung as written
**Prediction:** the three fog opcodes and the three draw/ping opcodes can be named without a client, at the confidences rung S6's attack established. **Refutation:** `test_dispatch.py`'s `unlisted_named` goes red the moment `0x002B` is named without an arm or a `DROPPED_ON_PURPOSE` row — that check firing IS the rung working, and the row must land in the same commit.
**Procedure:** the six entries and the `DROPPED_ON_PURPOSE` row are drafted; run `test_dispatch.py`, `test_catalog.py` (477/477 must be unmoved — the entries are purely additive and copy `messages.json`'s fields verbatim, so a moved count means the copy was wrong), `test_codec.py`, `test_provlint.py`.
**Cost:** ~1 h. **Do it in one commit with the study docs.**

---

## Tier 2 — one client run each, cheap arms (NEEDS OWNER GO-AHEAD; batch one go-ahead over the set)

**UNGATED 2026-08-14 by rung C1 (FINDINGS §6d). Read this before any arm below.** *This preamble read: "Until that is explained, **C2 and C3 are unfalsifiable** — an arm whose two halves both draw the same 64×64 tiled fallback shows no change… So C1 stops being a picture comparison and becomes a **diagnosis**, and it gates the rest."* **C1 ran, and the compass draws the atlas** — retail map 148 on build 38833 against its own archive generation, χ +0.250..+0.279 against the crop's predicted +0.286, with the old fallback frames re-measured at +1.154..+1.160 by the same sampler as the control. **So the instrument exists and C2/C3 are falsifiable again.**

**Two things every arm below inherits from that run, and both are cheap to get wrong:**
- **Client build and server archive must be the SAME GENERATION, and `contentids` will not save you** — it resolves through `file_id_table()`'s dual registration and cleared the exact pair that then failed (FINDINGS §6d.3). Check the **raw** table for the id form `content/maps.toml` will actually send.
- **Give the server its OWN COPY of the archive.** Pointing `RURIK_DAT` at the client's run directory cost this run its navmesh (`Permission denied`, the client holds it open) — the run still passed and the compass was unaffected, but nothing about pathing was validated.

### C1. Why is the compass image NULL? — **DONE 2026-08-14, and the question expired instead of being answered. FINDINGS §6d.**

**Outcome — the compass DRAWS THE ATLAS, and the rung's whole diagnostic premise is dead.** On build **38833 against its own archive generation** (`vault/run/2026-08-13_64fae3b1369b`, ours-DH, caged, loopback, synthetic account, no probe, no `--enemy`), retail map 148, the run passed the full ladder to `body is in the map` and the compass rendered a recognisable crop of Ascalon City — fields, walls, roads, water, player dot in the Danger Zone circle.

**χ, with the control run first** (same code, same disc `28 ≤ r ≤ 72` about `(1808, 153)`, both frame sets 1936×1040):

| frames | median χ |
|---|---|
| `20260811T161925/hold001..004` — the fallback set §6 measured | **+1.154 … +1.160** |
| `20260814T225013/hold001..005` — this run | **+0.250 … +0.279** |
| S5's offline prediction for map 148's crop | **+0.286** |

The control is what makes it evidence: the sampler reproduces the fallback band on frames already classified, *then* the new run lands on a crop value computed offline and never fitted to a frame.

**Three things this rung did NOT establish, and the first is the important one:**
1. **WHY.** Client build, archive generation and the `0x0199` id form all changed together. Attribution is a new rung (S13 below), not this one.
2. The `ReadProcessMemory` read of `[+0x84]`/`[+0x58..0x5c]` was **not performed** — it became unnecessary for diagnosis and is demoted to an optional confirmation.
3. **The server ran without a navmesh** (`Permission denied` on the archive the client holds open), so nothing about pathing was validated. Use a **separate copy** for `RURIK_DAT`.

**Two by-products worth more than the rung's own cost:**
- **The 38797 arm failed at Code=007 and the cause is measured**: `content/maps.toml`'s corrected plain `0x1B97D` does not bind in the 38797/`dat_study` archives at all (only `0x8001B97D` does), and the client's lookup is an exact 32-bit compare with no retry. The server sent `0x0199` and the client hung up immediately.
- **`toolkit/contentids.py` cleared that pair anyway** — it resolves through `file_id_table()`, which dual-registers both forms, so it compared row 7982 to row 7982 and never tested the form actually sent. The guard validates our reader, not the client. **UNFIXED.**

**Consequence: C2 and C3 are UNGATED, and PLAN risk 7 is RETIRED.**

---

### S13. Which of the three changes un-NULLed the compass? — **DONE 2026-08-14, same day it was opened. FINDINGS §6e. The attribution was right and the mechanism was wrong.**

**Outcome — the cause is the ARCHIVE, via the ATLAS TILE ROWS rather than the map file.** In every archive the fallback sessions played from, map 148's world-map tiles carry bit 31 (replacement pending), so the **plain** id the client's own tile table holds does not bind; the client's exact 32-bit compare misses, the tile getter returns NULL, and the fallback is tiled over the whole disc. Over the full atlas: **470 of 492 tiles addressable in `run/38797`, 22 missing — 18 in world 1 and 4 in world 0 — against 492 of 492 in `run/38833`.** Maps 143, 146 and 148 are all **world 1**, and they are the entire fallback corpus. The premise was a survey of one broken continent.

**Every clause of the rung's own prediction was tested and three of the four died:**
- **map file — REFUTED.** Height field byte-identical (sha256 `f9bd7848…`), `MAP_PARAMS`'s first 25 bytes (the whole rect) identical, differing only in a trailing v4 GUID; grid 416×512 and the same rect in both. The latch always had the same input.
- **client build — REFUTED.** All 12 `CompassMap.cpp` asserts identical, uniformly displaced +0xA0; instruction streams for the ctor, crop, latch and fallback tiler have identical counts and **zero** mnemonic mismatches; the footprint table, `s_worldData` and the tile arrays are byte-identical.
- **map-type byte — REFUTED** (not in the original prediction, added when the first two died). `FOOTPRINT_A == FOOTPRINT_B` for map 148, and a `--explorable` run **still drew the atlas**.
- **archive generation — CONFIRMED**, by the tile rows.

**THE REFUTATION CLAUSE DID FIRE, and it improved the result — this line first read "The refutation clause did NOT fire: no 38833 client drew the atlas."** A population sweep of all 620 frame-carrying sessions (scored on the 320 with a hold frame) puts **4 in the atlas band, three of them build 38797**: `20260813T012916`, `T013033`, `T103437`. **And `20260813T103437` (atlas) and `20260813T105432` (fallback) ran twenty minutes apart from the SAME run directory** — same exe, same archive path, opposite compass. So the cause is not any "generation": it is **the archive's ARMED STATE, which changes during one copy's life** (`FcArchive` arms a row when it requests a replacement, and the client writes its own `Gw.dat`). That pair is a natural experiment holding the client fixed, so it exonerates the build more cleanly than the static diff does. The tile-addressability mechanism is unchanged and stronger; only the 38797-vs-38833 framing is withdrawn. **Sweep limit:** 188 sessions scored χ ≈ +0.529 from `final.png`, which is the character screen and not a compass — excluded rather than binned.

**Two by-products:**
- **The ground layer arrives LATE.** `final.png` at the map verdict reads χ **+1.182** (fallback band) and converges to **+0.250** by ~t+26 s. A single early frame is not evidence of a NULL image. *(It does not rescue the old corpus — 8 flat frames at +1.154 there.)*
- **`file_id_table()` has now hidden the same class of failure three times**: the map file id (caught by crossbuild), `contentids.py` (§6d.3, UNFIXED), and rung S9's tile-presence check — which cost S9, S12, C1 and S13 to unwind. **The fix is one predicate: does the id bind in the form it is sent?**

**Still NOT FOUND:** why those 22 rows were replacement-armed in the owner's archives. That is an ArenaNet-side `FcArchive`/`DnArchive` question, not a server one.

---

### S13-old. The rung as written, kept for the record
**Prediction:** the archive generation is the cause — the compass ground layer is drawn from the client's own archive, and map 148's file genuinely changed (row 7982, 1,300,036 B, crc `0xA0AE500A` → row 177262, 1,300,044 B, crc `0x33F1A289`). The client build is the weakest candidate, because §6's static model has no build-dependent step in the crop path. **Refutation:** if a 38833 client can be made to draw the fallback, or a 38797 client the atlas, the archive reading is wrong.
**Procedure, cheapest first, and the first leg is STATIC and free:** diff the two map files' chunk tables with `mapexport.py` — if the new file's rect or grid differs from `416 × 512 / (−18432, −24576, 21504, 24576)`, the latch changes and the mechanism is named without a run. Only then consider runs; note the id form is **not** freely variable (each archive binds exactly one form), so client-build and archive-generation cannot be fully crossed — say so rather than pretending to a 2×2.
**Cost:** ~1 h static, then optional runs. **Do the static leg before spending a go-ahead.**

---

### C1-old. The original diagnostic framing, kept for the record (was: baseline compass frame vs the S5 crop; **re-scoped a second time 2026-08-14 by the S9 adjudication**)
**THE `Gw.log` DISCRIMINATOR IS STRUCK. It was never a discriminator.** *This rung used to bill it as "a second, free discriminator … at zero extra cost".* Rung S9 measured `0x008C21EB cmp dword [ebp-4], 0xf; jne` putting `"Converting world minimap texture to DXT1."` on the **conversion** arm only, and 484 of 484 atlas tiles are already DXT1 — so the line is unreachable for every world-map tile that exists, and `0x008C2203` is the **only** logger call in all nine compass translation units. Absence of the line is what a *perfect* load produces. **Keep `Gw.log` on the run — it is free and carries the re-bloat and archive errors — but budget nothing on it.**

**Prediction, and it is now a MEMORY read rather than a picture — RE-SCOPED A THIRD TIME by rung S12, whose static half turned "most likely (0, 0)" into exact numbers, then CORRECTED THOSE NUMBERS (FINDINGS §6c.4 — the first export read the authored sculpt map, not map 148):** on **retail map 148** under our server, caged, our DH, the live `CompassMap` reads `[+0x84] == 1`, **`[+0x58]/[+0x5c] == (416, 512)`** and **`[+0x60..0x6c] == (−18432, −24576, 21504, 24576)`** as floats — measured from map 148's own file, `mapexport --file-id 0x1B97D`. H3a is DEAD, and so is H3 for this map: the latch covers the **whole** footprint. The read discriminates:
- `[+0x84] != 1` → the hypothesis-1 *family* is alive after all despite S9's refutation of its mechanism, and FINDINGS §6's world table names which world it is
- `[+0x58]/[+0x5c] == (416, 512)` → **everything upstream of the crop is confirmed healthy**, all three named hypotheses are dead, and the whole remaining question is downstream: the per-slot tile load at `0x008C21C0`, the blit at `0x008C2077`, or rung S5's own metric. This is the *predicted* outcome — the run's value is that it closes the upstream half by measurement rather than by disassembly
- `[+0x58]/[+0x5c] == (0, 0)` or anything else → S12's static chain missed a path, and finding which is the next rung

**The spawn arm proposed by the first draft of S12 is VOID and must not be run.** It rested on the sculpt map's ±3072 rect; map 148's real rect is 40k × 49k units and the spawn `(9826, 8077)` is at cell (294, 340), comfortably inside. There is nothing to move. Two runs, one content-row edit, and the arc's leading question goes to OBSERVED either way.

Also read `[+0x60..0x6c]` (the raw map rect) in the same pass — it is four dwords and it separates "the rect was zero" from "the rect was right and the divide went wrong".
**Take the χ measurement in the same run as the CONTROL**, unchanged: `χ = (R−G)/(G−B)` should read ≈ +1.10 (the fallback band) rather than ≈ +0.29 (map 148's atlas crop). If it reads ≈ +0.29 the fallback is *not* firing on a live-served retail map and every vault frame was taken under a condition we have not identified — which would itself be the result.
**Procedure:** `harness/session.py --keep-open --shots N`, map 148, no probe, no `--enemy`. Capture **after** the load overlay clears (the §22 lesson — a frame caught at 100% crossfade shows a curtain, not a compass) and record the wall clock so `shotlabel`'s join rules apply. For the memory half, extend the pure-`ctypes` cross-process reader `toolkit/harness/keytap.py` already proves (ASLR-correct module bases, `ReadProcessMemory`); the `CompassMap` instance is `[compass+0x4C]` and the lazy create is `0x008BC426`. Score χ against the band and the crop, not by template match (rung S5 measured that metric failing and pointing the wrong way).
**Cost:** ~1 run, ~5 min, plus ~1 h to extend the reader. **The control every later arm is read against, and still the only rung that can unblock C2/C3.**
**Run S12 FIRST — it is static, needs no go-ahead, and can kill H3a for free.**
**Note:** the original prediction named Kamadan (map 449). **No Kamadan frame exists in the vault and map 449 has never been served** (rung S7), so there is nothing to compare against; map 148 now has eight frames, an atlas crop, and a `maps.toml` row.

### C2. The discriminating arm: same terrain, two map slots — **DONE 2026-08-15. The load-bearing half CONFIRMED; the second clause REFUTED by the model's own arithmetic. FINDINGS §6f.2.**

**Run WITHOUT the archive write the procedure called for.** A new `authsrv --file-id` serves a chosen map **file** at whatever **slot** `--map` selects, which is the only way to hold one still while moving the other — and `deploy.py --install` twice would have changed the archive as well as the slot, confounding the arm it was meant to run.

| arm | slot | terrain | fallback | median RGB |
|---|---|---|---|---|
| A | 143 → world 1 | sculpt `0x287D3` 64×64 | 39.6% | (89,61,25) |
| B | 144 → world 2 | sculpt `0x287D3` 64×64 | 40.7% | (77,55,30) |
| C | 143 → world 1 | Ascalon `0x1B97D` 416×512 | 5.3% | (89,84,35) |

- **SAME terrain, different slot (A vs B): L1 = 0.618** against a 0.000 within-arm floor. Same-sized patch, different continent's art, disjoint tile sets. **The identical file at two slots gives two different pictures — the ground image is not the terrain's.** C2's real question, answered.
- **"Must NOT change when the terrain changes at a fixed slot" is REFUTED (A vs C: L1 = 0.916)**, for two reasons this document already contained: the latch makes the terrain set the **extent** (fallback 39.6% → 5.3%), and — the larger effect the clause missed — the player's **map-local cell** differs between terrains at one world position, so the window **translates** within the footprint: `(1536,1536)` is cell (48,48) of the sculpt map but (208,271) of Ascalon, atlas ≈ (1008,496) vs (1168,719), **223 cells apart**.
- **§3's model stands.** The refutation condition was "if the compass tracks the terrain" — meaning if it drew OUR geometry. It never does; every arm draws atlas art. The terrain supplies the **addressing**, which is what §0 was corrected to say after S9, and the clause predates that correction.

**By-product: H3b photographed.** The square patch in arms A and B is the latch, and it prices Tier 3 in one number — **~40% of the disc is fallback on a 64-cell map**, which A1 cannot fix and only A2 can.

---

### C2-old. The rung as written
**Prediction:** deploy the identical authored terrain at `map_id = 143` (continent 1, footprint 320×544) and at a slot on a different continent with a very different footprint (e.g. row 144: continent 2, `(4736,2272)..(5088,2912)`, 352×640). The compass image must **change between the two arms** and must **not** change when the terrain changes at a fixed slot. **If the compass instead tracks the terrain, FINDINGS §3's whole model is refuted and the ladder stops here.**
**Procedure:** `deploy.py --area sculpt --install --launch --dat <copy>` twice with the row's `map_id` swapped; `--serve` on a second launch for the navmesh readback (`studies/customarea/FINDINGS.md` §59.3: the run that installs cannot serve — `--install` arms the head to zero and the client then locks the archive). Screenshots through the hold; `capture_error_dialog` fires on every run.
**Cost:** ~4 runs (2 arms × install+serve), ~30 min harness. The E1-collision shape — two builds differing in one field — and the cheapest thing that can refute the model.
**UNGATED 2026-08-14 — C1 established that the atlas draws (FINDINGS §6d). The arm is specified in worlds rather than continents.** *This paragraph read "GATED BEHIND C1 … so C1 must first establish that the atlas draws at all."* It did. Map 143 is **world 1** (4×8 grid, tile array `0x00BF2688`) and map 144 is **world 2** (16×12, `0x00BF2708`) — two different arrays, so the arm discriminates. **Do not pick a second slot by grid shape**: worlds 2 and 4 have identical grids and different arrays, and a renderer or an experimenter choosing that way gets the wrong continent's art with nothing to catch it (rung S4).

**A second hazard, added 2026-08-14 by the S9 adjudication, and it is specific to an AUTHORED map: the compass crops only as much of the footprint as OUR OWN MAP FILE covers.** `0x008C28D0` latches `(0, 0, mapCellsX, mapCellsY)` from `Engine\Map`'s rect ÷ 96.0 into `[CompassMap+0x50..0x5c]`, and `0x008C2450` clamps every draw request against it **before** adding the footprint origin (`0x008C2782`/`0x008C2792`). Our sculpt map is 32/64/96 cells; map 143's footprint is 320×544 and map 144's 352×640. **So the expected picture on both arms is a small correct corner of the crop with the fallback tiled over the rest — and the two arms' corners come from two different worlds, so the arm still discriminates, but "the compass image changed" must be scored on that corner and not on the disc.** Predict the corner size from the deployed dims before the run, so a run that produces a full disc or a full fallback is itself a result.

### C3. Flip the map-type byte on a slot where A ≠ B — **DONE 2026-08-15. CONFIRMED, with the negative control at exactly zero. FINDINGS §6f.**

**Outcome — the lever is REAL and it is ours.** Four runs, build 38833 against its own archive, loopback, caged. On **map 474** (world 5, the two rects differing in *both* origin and size — A `(1536,2176,1984,2784)` 448×608, B `(800,2720,1312,3232)` 512×512) flipping the `0x0199` map-type byte changed the compass picture completely; on **map 143** (A == B) the same flip changed **nothing**.

| arm | map-type byte | rect | tiles touched | result |
|---|---|---|---|---|
| 474 default | 1 `MISSION_MAP_GAME` | B | (1,5)(1,6)(2,5)(2,6) | median RGB (21,21,27) |
| 474 `--outpost` | 0 `MISSION_MAP_OUTPOST` | A | (3,4)(3,5) | median RGB (61,59,46) |
| 143 default | 0 | A == B | — | median RGB (89,61,25) |
| 143 `--explorable` | 1 | A == B | — | median RGB (89,61,25) |

**Scored rotation-invariantly, because the operator moved the camera during both 474 arms and the compass disc turns with it.** A rotation about the disc centre permutes pixels within a **centred annulus** without changing the multiset of values, so a colour histogram over that annulus cannot see rotation at all. Coarse 4×4×4 histogram, L1 distance: **between the 474 arms 0.915**, against a within-arm noise floor of **0.027 and 0.109** (frames that themselves span the camera movement) — 8-34×. **Between the 143 arms: 0.000, and within each arm 0.000 too.** The tile sets the two 474 rects touch are **disjoint**, which is why the arm was chosen: there is no shared art for the two arms to agree on.

**The control is not vacuous, which is the trap this rung wrote itself against.** Map 143's `content/maps.toml` row points at `0x287D3` — the authored SCULPT map — so its compass could have been pure fallback, and "no change" would then prove nothing. It is not: its median RGB gives **χ = +0.778**, outside the fallback band (+1.081..+1.164), and the frame plainly shows a **square patch of real atlas art surrounded by the tiled fallback**. Real atlas pixels are present, so a footprint change would have been visible had one occurred.

**Two by-products, one of them a first.**
- **H3b is now OBSERVED rather than predicted.** That square patch *is* the latch: an authored map crops only as many cells as the authored map itself has (`[CompassMap+0x50..0x5c]`, S9/S12) and `0x008C1E00` tiles the fallback over the rest. This is the first direct visual confirmation of the mechanism, and it is on **our own** map.
- **The ground art is the SLOT's, not the terrain's.** Map 143 draws *world 1 atlas art* over *our authored sculpt geometry*. The map file supplies no pixel of it — §0's headline, seen rather than derived.

**A flag this rung had to add, because the toggle as specified had no off position.** `--explorable` can only force the byte ON, and map 474 is `explorable = true` in content, so its byte-0 arm was unreachable. `authsrv --outpost` is the counterpart (forces 0, refuses to combine with `--explorable`); the log line reads `[is_explorable=0, FORCED OUTPOST]`.

**A harness note paid for twice:** one run failed at *"client asked for a game instance"* — login fine, then nothing — because the play click needs the client foregrounded, the same condition that skips `--shots`. It is a retry, not a diagnosis.

---

### C3-old. The rung as written
**Prediction:** on one of the **20 rows that carry two different NON-ZERO footprints**, toggling `0x0199` field 3 between 0 and 1 (`authsrv --explorable`) shifts the compass ground image to a different crop of the same continent, and nothing else changes. **Negative control:** on map 143 (A == B) the same flip produces no compass change — without it the experiment is unfalsifiable. **UNGATED 2026-08-14** — *this read "GATED BEHIND C1 (a fallback compass cannot show a crop shift)"*, and C1 showed the compass drawing a real crop (FINDINGS §6d), so a crop shift is now observable.
**The population was corrected 2026-08-14 by rung S2 and the old "172 rows" is the wrong one**: 136 of the 172 have A all-zero and 16 have B all-zero, and on those the flip either changes nothing or blanks the compass — the `GmMapHelpers` readers fall back to the other rect while the compass has no fallback at all. The 20 usable ids are **135, 254, 255, 424, 426, 428, 433, 435, 451, 474, 476, 478, 480, 491, 492, 493, 494, 495, 545, 554**. **Prefer a row whose two rects differ in SIZE as well as origin** — 424, 426, 433, 476, 478, 491, 492, 545, 554 pair 192×192 against something much larger — because a pure translation is far harder to score off a frame than a change of scale. Note the selector is now NAMED: 0 = `MISSION_MAP_OUTPOST` → `+0x48`, 1 = `MISSION_MAP_GAME` → `+0x58`.
**Procedure:** two probe-free harness runs per arm; strip-score against the pre-flip idle window; register a `probes.py` step so the flip is timestamped in the capture.
**Cost:** ~4 runs. **Turns FINDINGS §3.3 from a disassembly reading into an OBSERVED fact — the answer to the brief's "do we hold the lever" question.** It does not make our map look right; it proves we hold the lever. (Note: map 143's A == B, so a live-retail row must be borrowed for the positive arm — the `maps.toml` map.144 caution about unknown live rows applies.)

### C4. Targeted opcode probes with screen readout — **DONE 2026-08-15. TIER 2 IS COMPLETE. FINDINGS §6f.**

**Five arms, four probes, and the two that closed last are a matched null/positive pair.**

- **`0x008C` UNFOGS — OBSERVED.** Marking a block the init payload left CLEAR (26, 22) changes **1,059** world-map pixels in one compact 88×101 box, visibly lifting the fog; marking a block it had ALREADY SET (30, 24) changes **0 of 2,013,440**. Same probe, one coordinate different. So the mark works, is confined to the blocks it names, and re-marking a set block is a **no-op rather than a refusal** — which is why the first isolation attempt returned byte-identical and was not evidence about the opcode.
- **Which blocks are clear is COMPUTABLE.** Decoding the replayed payload's bands consumes exactly its declared 38 bytes, reproducing rung S8 outside S8's tooling; 68 of map 148's footprint blocks are clear. Both coordinates live in `probes.py` with the derivation. *(The tail parse is 20 bits short of the band's 1,024 — do not trust blocks near a band's end from it.)*
- **`0x008D` is the predicted NULL — MET.** Compass byte-static across the entire run (green 23 / red 441 / pale 62 on every frame), the same zero-variance baseline the draw arm used.
- **`0x0049` is HALF REFUTED.** The quest registers — a **"?" icon** appears under the level bar, 23.8 % of that slot differing from a run without it — but **no green starburst on the compass**. Why is NOT FOUND: field semantics, a tracked-quest requirement and a world-map-only marker are all live.

**Still open after C4:** the per-map explorable mask (`0x0070A120` → `0x00721D00`) is unread, so `0x008C` is proven on **retail** map 148 and not on our own authored geometry.

---

### C4-partial. The first pass, superseded above (2026-08-15)

**Three results, and the biggest was not the one this rung asked for.** A new probe (`probes.py`, `compass_draw`) sent five messages on build 38833, map 148, loopback and caged, every payload hexdump-verified before the run.

1. **`0x0091` RENDERS, both arms — OBSERVED.** `knotCount = 1` with a non-zero owner tag draws a **red ping ripple** (806 and 744 red-excess px at t=3 s and 6 s); `knotCount = 5` draws a **polyline** (83 and 75 pale px at t=10 s and 13 s). Baseline: **eleven consecutive frames at exactly 441 and 62, zero variance.** Both appear on the frames their steps predict and vanish two frames later. The ripple needs no `0x0092`, confirming `0x008BE6B3 cmp edi,1`.
2. **THE FOG INIT PAIR IS ACCEPTED, and a CRASHED CONTROL is what proves it.** With `0x008B` + `0x008A` (ArenaNet's own RLE, replayed verbatim) the world map opens and shows revealed terrain; **without them the same M press kills the client** on `GmMapView:1731 worldMapDims.x == mapDims.x * DXT_BLOCK_SIZE`. The binary closes it: `mapDims` comes from `charContext + 0x5B4` (the field §4.2's expander writes) and `shl eax, 2` fixes `DXT_BLOCK_SIZE == 4`. **The world map is unopenable on this server until that pair is sent.**
3. **The compass ground is NOT fog-masked** — it drew normally in both runs, including the control whose `mapDims` was zero. Closes a §7 open question for free.

**What is left of C4, and it is real work rather than bookkeeping:** the `0x008C` mark's own contribution is **unmeasured** (the probe sent init *and* mark, and the revealed region is most likely the init's — separating them needs an init-only arm); **`0x008D`** (the predicted null control) and **`0x0049`** (QUEST_ADD, the green starburst) were never sent; and the per-map explorable mask is still unread, so whether `0x008C` can unfog **our own** geometry is still NOT FOUND.

**A harness fact this rung paid for:** `--shots` only fires during the `--keep-open` hold, and the hold starts *after* `--actions` finishes — so `--actions "0:play 45:key:M"` put the first frame at t=46.9 s while the draw steps fired at t≈20–39 s, and arm A was invisible for reasons unrelated to the opcodes. Sample the window under test.

---

### C4-old. The rung as written
**Prediction (per candidate, before sending):** `0x0049` (QUEST_ADD) with a vec2 inside our map's rect and `dest_map_id` = our slot draws a **green starburst on the compass** and a quest-log entry; `0x008D` draws **nothing** (static catalogue).
**`0x008C` (MAP_EXPLORATION_MARK) sent ALONE draws nothing, and that is a prediction rather than a caution** — its handler returns immediately when `mapBits.Count() == 0` (`0x00811BEE`), and only the init pair allocates that array, so `smsgsweep`'s 108 prior sends of `0x008C` measured nothing at all. The arm is therefore a **three-message sequence**: `0x008B` with `(dims.x, dims.y, byteCount)` where `dims.x % 32 == 0`, then `0x008A` carrying `ceil(byteCount/4)` dwords of a valid RLE stream (16-row bands, `u16` band length, `0xFF`-continuation runs, alternating colour — rung S8 re-derived it and closed it 8/8 on ArenaNet's own payloads), **then** `0x008C` with `(blockX, blockY, blockRadius)` inside the footprint at `>>5`. **The init pair posts no frame message**, so expect no repaint until the `0x008C` lands — a run that sends only the pair and stops is expected to look dead. Two things can make the whole arm fail for reasons unrelated to the opcodes: `0x008C`'s bit writes are gated by a **per-map explorable mask** from `Engine\Map\Map.cpp`, which nothing here has read, and the mark coordinates are in **continent-absolute blocks**, not map-local ones.
**`0x0091` (COMPASS_DRAW_BROADCAST): knots are two SIGNED int16 in one dword, low half = x, in ABSOLUTE world units divided by 96.0** — so a knot must be near the player's own position to be inside the compass at all (the clickable radius is exactly 4,500 world units). Field 1 must be **NON-ZERO** (zero is the drawing client's own owner tag, and a zero echo makes the client merge the broadcast into its own line and double the stroke). **The `knotCount > 16` process-kill caution is RETRACTED** — `MsgConn.cpp`'s generic unpacker refuses the message before dispatch with a tight bound, so an over-range send is not dangerous, it is **inert** and measures nothing. **Cheapest possible demonstration: a single `0x0091` with `knotCount = 1`, a non-zero owner tag and a world position beside the player** draws a red ping ripple without needing `0x0092` at all (`0x008BE6B3 cmp edi,1`).
**Procedure:** `smsgsweep --plan --only <op> --set <field>:<value>` → `sweeploop`/`shotloop` → `shotlabel`. Four paid-for cautions: (i) verify the server's own `plain=` hexdump matches intent — `--set` silently shipped the degenerate payload for an hour (`studies/smsgsweep/FINDINGS.md` §5c retractions); (ii) the ledger is keyed by regime (all-zero vs filled are different rows); (iii) `SILENT` = no c2s reply and is blind to anything drawn — the screen is the instrument; (iv) `--enemy` is opt-in, a combat-free run must warn if no hostile.
**Cost:** one launch per opcode, ~40 s each. Budget ~2 h for a dozen. **This is where quest markers and the exploration reveal get their first OBSERVED screen result — and where §4.2's `0x008C`-vs-`0x0089/0x008B` contest can be settled by which opcode actually unfogs a block.**

---

## Tier 3 — the expensive half, only if the owner wants the picture to be ours

### A1. Author one atlas tile — **DONE 2026-08-15. FINDINGS §6g.**

**The compass picture is OURS.** A 512×512 DXT1 ATEX we authored (10 levels, "closes exactly", round-tripped at 0 of 5,476 sampled pixels off by >60) was installed with `datmove` into a throwaway archive — 174,868 B against ArenaNet's 54,192 stored, exactly the misfit this rung predicted, so `--replace` was never an option. It reads back byte-identical, the archive still passes all three checksum rules, and on screen it is **38.0 % of the disc** (9,246 of 24,313 px in colours no Pre-Searing art carries).

**Not established:** the tile is a PATTERN, not our terrain — generating it from the authored heightfield is a further step, and as of 2026-08-17 it is **the only open item left in Tier 3** (see A3 below). **The collateral this rung warned about is CLOSED and needed no run**: sweeping all 888 rows for world-1 footprints overlapping tile (1,0) returns exactly **6** — maps 143, 147, 149, 150, 151, 779 — with 149/150/151 carrying byte-identical rects to 143's, so the blast radius was five extra maps in one throwaway archive. FINDINGS §6g.4.

---

### A1-old. The rung as written
**Prediction:** replacing the 512×512 ATEX at the tile covering map 143's footprint changes the compass, mission map **and** world map together (all read the same table, FINDINGS §3.2). If only one changes, §3.2 is wrong.
**Procedure:** `png.py` → `dxt1.encode` → `atex` container → `datwrite --replace` if it fits (it will not — ours writes uncompressed, median tile is 110,608 B stored), else `datmove`, into a **throwaway run-directory archive only**. Follow `iconset.py`'s scars: `file_id_table` returns one-based row numbers while `Archive.entries` is positional, so `entries[row]` reads the row before the one named; check the whole plan before the Writer opens. Prefer `datwrite --restore` from a donor over `--revert` (a journal expires the moment a client runs, `studies/crossbuild/FINDINGS.md` §4c; no journal exists for the icon rows left armed in `vault/run/reskin-roster/`).
**Cost:** 1-2 days + runs. **Collateral to state up front:** a tile spans up to four of map 143's footprint chunks, all shared with other Pre-Searing maps — arming them changes every neighbour's compass in that archive copy.


### A2. Repoint the area row's footprint — **DONE 2026-08-15, and its own prediction is half REFUTED. FINDINGS §6g.**

**We choose which part of the atlas a map crops.** `toolkit/clientpatch/footprint.py` moved map 143's two rects from (960,448,1280,992) to (1100,448,1420,992) — **6 bytes**, located structurally, written out of place, containment-checked and read back by re-locating the table in the patched file. Same archive, same authored tile, **only the origin moved**: authored colour falls from 38.0 % to **0.1 % with zero magenta** and retail terrain fills the disc.

**REFUTED half:** this rung predicted that making the rect's SIZE equal our authored dims "makes the compass crop that size". The size stayed 320×544 and the crop moved anyway — the window is clamped by `[CompassMap+0x50..0x5c]`, latched from the LOADED MAP FILE's dims, not from this rect. **The ORIGIN is the lever; the SIZE is not.** The module docstring carries this so the flag's existence cannot be read as the prediction having been confirmed.

**H3b is OBSERVED at last**, incidentally, in both frames: the authored region is a bounded square with fallback tiled around it. It went unseen this long because in the 38797 archive **all four** of map 143's footprint tiles are unaddressable — the customarea "brown disc" was the archive generation, not the authored map.

---

### A2-old. The rung as written
**Prediction:** editing `s_missionClientData[143]`'s `+0x48..+0x54` (and `+0x58..+0x64`) to a rect whose size equals our authored dims makes the compass crop that size (32/64/96 cells instead of 320×544), and the player marker projection becomes self-consistent for the first time.
**Procedure:** extend `toolkit/clientpatch/reskin.py`. The table is `.rdata`; the same three constraints as its four existing tables apply — locate structurally (never by address), require anchor AND shape to agree, write out of place, assert containment (every changed byte inside the intended dword) plus a read-back (`test_reskin.py`'s first version wrongly assumed "4 bytes changed" when only two moved).
**Cost:** 1-2 days + runs. **A1 makes the compass the right *picture*; A2 makes it the right *shape*. Neither alone is enough** — which is the cost the owner should see before Tier 3 starts: making an authored map's compass right means both an archive write and a client patch.

---

### A3. Generate the atlas tile FROM our terrain — **OFFLINE HALF DONE 2026-08-17; the run is all that is left**

**`toolkit/mapdata/tilerender.py` renders the authored heightfield into the tile, and the generator is the oracle.** The sculpt area's 64×64 plaza shades to a dip on −x, a rise on +x and a hard seam at `gx == mid` — which is exactly where `deploy.gen_plaza`'s own comment puts its 61° cliff. Measured rather than eyeballed: the heightfield's largest column step is at gx=33, **8,748 against the runner-up's 488**, and the shaded seam peaks within one column of it (a 3-tap central difference smears a step by one either side). The **sign flip is checked in both directions** — flipped, the rise reads +48.7 luma brighter; unflipped it inverts to −51.1 — so a renderer ignoring the archive's negated heights cannot pass.

**Placement is derived, and it is the half most likely to be silently wrong.** The atlas coordinate is `local + footprint_origin`, so map 143's render belongs at texel **(448, 448) of tile (1, 0)** — the corner, fitting 64×64 exactly. Putting it at the tile's own corner would be off by 448 and would read as the shading being broken. A render that would straddle two tiles is refused rather than truncated.

**It also shrinks A1's collateral by construction.** A1 replaced the whole 512×512, which is why five other maps got our checkerboard. This decodes ArenaNet's own tile and paints only the authored map's cells: **4,096 of 262,144 texels, 1.56%**. Verified against the real 38833 archive (tile 116842, row 177375) and round-tripped through the ATEX reader at 10 levels, worst channel delta 1. `test_tilerender.py`, 17 checks.

**A real bug the test caught, worth recording because it fails only when it matters:** `atex.decode_rgba` returns `(rgba, w, h)`, and the first `decode_tile` indexed that tuple as if it were the pixel buffer — inert until a real `--dat` is supplied, i.e. exactly when the tool is doing its job.

**WHAT IS LEFT: the run.** `datmove` the blob into a throwaway copy of a **38833** archive, cage it, and look at map 143's compass — the prediction below is unchanged and unverified. Everything up to the archive write is done and green.

---

### A3-old. The rung as written
**Prediction:** a 512×512 tile whose 64×64 sub-rectangle at map 143's footprint origin is a shaded render of the authored heightfield makes the compass show a recognisable picture OF OUR MAP — ridges and valleys matching the terrain the player walks — rather than art we merely chose. **Refutation:** if the compass shows the render but it does not correspond to the ground (features in the wrong place, or mirrored), the projection between heightfield cells and atlas texels is wrong, and §6b.2's "one texel per terrain cell" is the first thing to re-check.
**Why it is cheap now, and what is missing:** `mapexport.py` already emits `heights.f32` at exactly one sample per cell — the compass's own scale — and `atex.build_image` + `datmove` are proven end to end by A1. **What does not exist anywhere in `toolkit/` is a renderer**: no hillshade, no top-down. That is the whole of the new code, and it is pure-stdlib arithmetic over a float32 grid (a slope/aspect shade is a few lines), not a Blender dependency.
**Procedure:** render → `atex.build_image` → `datmove` into a throwaway copy of a **38833** archive (the 38797 generation cannot address world 1's tiles at all, §6g), then one short run at map 143. Place the render at the footprint origin's offset WITHIN the tile, not at the tile's corner — A2 established the origin is the lever and the size is not.
**Cost:** ~2 h for the renderer offline, then a 4 GB copy, one elevated cage step and one short run.

---

## Risks that could moot a rung

1. **The model may be incomplete.** Only `CompassMap.cpp` was disassembled; `CompassCanvas/Marker/Target/QuestEffect/Model` were read as assert lists, and `CompassAIControl`/`CompassModel` carry zero visible asserts. If a second ground layer is drawn from map data by one of those, FINDINGS §3 is incomplete — **C2 is early precisely because it can refute the whole model cheaply.**
2. **`asserts.py` is a floor** (370 sites short); every "no assert names X" inherits it.
3. ~~**`OFF_CONTINENT = 0x04` is UPSTREAM.**~~ **RETIRED 2026-08-14 by rung S4** — `+0x04` is CORROBORATED from the client's own code (`0x0084DE50` reads it out of `s_missionClientData[missionContext+0x230]` and hands it, unmodified, to the tier getter that asserts `ConstWorldMap:1313 world < WORLDS`). **What replaced it is a bigger risk and it is not hypothetical:** the `continent → world` map is the identity, but the seven values the area table uses `{0,1,2,3,4,5,9}` are **not** the seven worlds carrying tile arrays `{0,1,2,3,4,5,6}`. World 6 has 47 non-NULL tiles no area row asks for, and **world 9 has five area rows (883-887) and no record in any tier**, so those five maps take the tiled fallback unconditionally. An atlas emitter that drops "unreferenced" worlds loses world 6; one with no defined answer for a missing world is wrong on five map ids.
4. **The world map may have no server lever at all** (`map_open` silent, exploration per-character/client-local) — surface 1e may not be a gap we can close.
5. **Harness contention and the two-run rule:** `--install` arms the head to zero and the client locks the archive, so a navmesh readback needs `--serve` as a second launch (`studies/customarea/FINDINGS.md` §59.3). A run that "completes" is not evidence a hazard is absent.
6. **Archive safety:** every write goes to a run-directory copy; `C:\gw` and `vault/dat_study` are refused by the `datwrite`/`atex`/`iconset` guards; a new client under `vault/run/` costs a UAC prompt (the cage is per-binary).

7. ~~**The compass may never draw the atlas under our server at all.**~~ **RETIRED 2026-08-14 by rung C1 (FINDINGS §6d).** The compass draws the atlas crop on retail map 148 under our server — χ +0.250..+0.279 against the crop's predicted +0.286, control frames at +1.154..+1.160. The fallback is **not** structural, C2 and C3 have their instrument, and Tier 3's premise survives. **What replaces this risk is smaller and is an attribution question**, carried as rung S13: three variables moved together (client build, archive generation, `0x0199` id form) and C1 separates none of them, so *which* condition restores the atlas is unproven — an authored-map arm that assumes 38797 behaviour may still see a fallback. The historical text follows, since the static work under it stands. **Updated 2026-08-14 (rung S9): both of the hypotheses this risk was written against are REFUTED**, and the surviving one — a latch-once copy of the loaded map's cell dims at `[CompassMap+0x50..0x5c]` — is *structural in the bad direction for Tier 3*: under H3b an authored map's compass can only ever crop as many cells as the authored map itself has, whatever A2 does to the footprint. If H3b holds, **A2 stops being "make the compass the right shape" and becomes a prerequisite for the crop existing at all**, and A1's tile authoring buys nothing until the dims and the footprint agree. Re-cost Tier 3 after C1 reads the three dwords.
8. **Two locators in this repo answer confidently and wrongly when asked outside their range, and both bit this arc.** *(existing risk 8 text follows unchanged)* **A third trap, added 2026-08-14: two unrelated fields at the same offset in one function.** `[CompassMap+0x68]` is the loaded map's rect x1 (`0x008C294C`) and `s_missionClientData[map]+0x68` is the per-area compass texture (`0x008C2513`); they sit forty instructions apart on different base registers and `--field 0x68` returns both. `s_missionClientData` is `.rdata`, which is the discriminator FINDINGS §3.3 already uses from the other side.
8. **Two locators in this repo answer confidently and wrongly when asked outside their range, and both bit this arc.** `asserts.py --at` is a fixed 2,000-byte window and manufactured the whole fog contest (rung S8); `codescan --in <module>` bounds from assert VAs and silently truncates a module with a long assert-free tail, hiding the very branch rung S2 was run to find. Neither can warn. Prefer `--dis` to the `ret`+`int3` run for a bound and `--xrefs` for ownership, and pass `--in` a **directory** prefix, never a bare module name.
