# The DESKWORK client-confirmation session, 2026-09-24 (CONFIRM-2)

**What this is.** The loopback session that ran, on our own retail client, the runsheets the
three DESKWORK-D1 pass-4 lanes left as `final-confirmation-needs-run` (studies/cmsg "The town
weapon", "The party family", "World-map travel") and the display mode's field step ("The display
mode"). Every launch was harness-driven, hands off, one variable at a time, with the arm's revert
flag as the A/B where the question had one. 16 launches, all loopback, build 38797
(`vault/run/2026-07-29_221c13772c7a`), main at `09126d4a` — the first session read under the
harness verdict fix of that commit, so every verdict below is `checkpoints_passed` with the run
folder's `crash-dialog.txt` checked (one run has one, §3). Nothing touched ArenaNet.

Labels: everything below is **OBSERVED on our own client against our own server**. It confirms
what the client ACCEPTS and DRAWS from what we send; where what we send is itself a
RECONSTRUCTION, this session does not make it retail's.

Run folders are `vault/captures/harness/<stamp>` (`gamesrv.log`, `report.json`, the frames).
The loopback store (`vault/state/characters/loopback_rurik.invalid.json`) was backed up before
the first launch and was byte-identical afterwards (sha256 `e3ad6146…`); nothing to restore.

**Two runsheet defects, for whoever writes the next one.** (1) `--walk` takes keys as
`i:0.4` and has no `vk:` step; F-keys go in `--actions` (`40:vk:0x71`, timed from the Play
click, so they fire BEFORE the walk). The town-weapon runsheet's single `--walk` with `vk:0x49`
is refused by the parser, so its one launch became four. (2) The travel runsheet chose Kamadan
(449), whose continent has no fog init — see §3.

## 1. The town weapon (studies/cmsg "The town weapon")

| Arm | Run | The wire (gamesrv.log) | The screen |
|---|---|---|---|
| A1: outpost, default | `20260924T084418` | `TOWN WEAPON: the body's 0x006E leaves out the lead hand (item 1, visual 0)`, then `UPDATE_AGENT_VISUAL_EQUIPMENT(+armour) [hands empty: a town]` — as predicted | **the body HOLDS THE HAMMER** (head in the right hand, handle at the left hip; frame `walk2-shot.png`); the doll holds it too; the F-strip shows two sets |
| A4: outpost, `--no-town-weapon-strip` (KNOWN-BAD) | `20260924T084637` | `UPDATE_AGENT_VISUAL_EQUIPMENT(weapon+armour)` | the same picture — 208 of 52,800 body-crop pixels differ by > 24 (idle sway), the same hammer |
| A2: outpost, default, F2 | `20260924T084811` | `SET_ACTIVE_WEAPON_SET(1)`, `ITEM_CHANGE_LOCATION(off hand 12 -> equipped 1)`, `ITEM_SWAP_LOCATIONS(1 <-> 11)`, `TOWN WEAPON: the body's 0x006F for the lead hand (item 11), the off hand (item 12) is not sent` — as predicted | the doll shows the **sword and shield**, set 2 active, the hammer in the backpack — and **the world body still holds the HAMMER** |
| A4b: outpost, `--no-town-weapon-strip`, F2 | `20260924T085033` | the same batch plus `0x006F (lead hand = item 11)` and `(off hand = item 12)` | the body holds the **sword and shield**, matching the doll |

**Prediction (1) is REFUTED on the client, and the study's rival wins:** "the client draws the
bag's weapon regardless — would show it and refute (e)". Leaving the hands out of the town
`0x006E` changes nothing on screen (A1 = A4). The body's weapon comes from another carrier; the
likeliest is the player's own `0x006D` (`GAME_SMSG_NPC_UPDATE_WEAPONS`), which our load builds
from the item store (studies/cmsg "Inventory", the store paragraph) — UNVERIFIED, one desk read
settles it.

**And the switch half is a VISIBLE REGRESSION the shipped default introduced:** with the strip
ON, an outpost weapon-set switch leaves the body holding the OLD weapon while the doll shows the
new one (A2); the pre-pass-4 behaviour the revert flag restores redraws it (A4b). Retail sends no
hand `0x006F` on its 14 outpost hand changes (the study's (b)), so retail's own client must redraw
through something else — what retail's outpost switch sends that ours does not is the open desk
question (the study's (b) lists retail's switch bursts). Until it is read, `--no-town-weapon-strip`
is the workaround for a consistent body, at the cost of the load's hand bytes differing from
retail's. The explorable control (runsheet steps 5–6) was not run: the strip does not touch the
field path, which WEAPONS-W9 confirmed on 2026-09-19.

## 2. The party family (studies/cmsg "The party family")

| Step | Run | Result | Prediction |
|---|---|---|---|
| 1–2: the outpost, the list | `20260924T085250`, `20260924T085635` | the log: for each of agents 33, 32, 31 `level 3 on hireable agent N (prop 36, before its create)` then `PARTY_HENCHMAN_HIREABLE(agent N)`; Party Formation **(2/4)** (the player and Tahlkora); Party Search's **Henchmen tab lists W3 Lukas, R3 Yuun, A3 Mai** (level 3, W/R/A) with an Add Henchman button | the three listed — **held** |
| 3: add one | `20260924T085443` | c2s `0x009F`; `PLAYER_PARTY_SIZE(3)` THEN `PARTY_HENCHMAN_ADD(party 1, agent 31, …)` (size before row); `HENCHMAN_ADD: … (agent 31) joined the party; size now 3`; **(3/4)** with the henchman's row | held |
| 4: to the cap | `20260924T085818` | two adds (31, 32) → **(4/4)**; the list marks both `(Added)`; the third (Mai selected, Add pressed) sends **nothing** — the client refuses at its own max_party, and the Add button is NOT greyed | the server's refusal line was predicted; the client never reached it — **the panel guards itself** |
| 5: the hero and the count | `20260924T090058` | 4/4 → kick the hero: `HERO_KICK … size now 3` **(3/4)**; re-add: `size now 4` **(4/4)**; kick again (3), add Mai (4); re-add the hero at 4/4: the client DOES send `0x001E` and the server answers `HERO_ADD(3) refused: the party already holds 4 members (heroes and henchmen), the served cap …; nothing sent`; the panel stays (4/4) without the hero; no assert | held — the fix pass's shared count is a real guard: the client self-guards a henchman add at the cap, NOT a hero add |
| 6: the henchman kick (deliberate, its own launch) | `20260924T090622` | the Henchmen tab's Kick on the hired Lukas sends **c2s `0x00A8` [agent 31]** — plaintext `a8 80 1f 00`, one word, the henchman's agent id — `UNHANDLED GAME_CMSG 0x80a8`; the client **keeps the row** (3/4) until a reply | names the opcode: the UPSTREAM `0xA8` reading CORROBORATED on our client; the reply (retail's `0x01C0`?) is still on no tape |
| 7: control, `--no-henchman-add` | `20260924T090437` | no level or hireable lines; **the Henchmen tab is greyed out** and its click does nothing | held — `0x0071` is what enables the client's henchmen list |
| 8: into the explorable | not run | the field carry is deferred (studies/cmsg, PLAN.md §8.1) | — |

**CORRECTED 2026-09-25 (steps 4 and 5):** step 4's "sends **nothing** — the client refuses at
its own max_party" is WRONG. Its own run's log (`20260924T085818`, gamesrv.log line 282) has the
client's third `c2s 0x809f HENCHMAN_ADD` and our `HENCHMAN_ADD(33) refused: the party already
holds 4 members … nothing sent`; pass 6's K3 (`20260924T205051`) shows the same. The client
SENDS a henchman add at its own max_party exactly as it sends a hero add (step 5); the server's
cap is the only guard, and it refuses silently (retail's refusal reply NOT FOUND). Step 5's
conclusion that "the client self-guards a henchman add at the cap, NOT a hero add" falls with
it: it guards neither.

**The runsheet's two UNVERIFIED points are answered:** the list DOES show on type-10 map 148
(retail offers no henchmen there, yet the client lists ours), and the `noncombatant` allegiance
blocks neither the list nor the add. The name the client draws is its own (`Lukas [Guardian
Henchman]` for the row our content labels the Fighter) — resolved from the name id, as designed.

## 3. World-map travel (studies/cmsg "World-map travel") — BLOCKED on content

| Run | What happened |
|---|---|
| `20260924T090842` — the runsheet as written: `--map 449 --persist`, `M` | startup as predicted (the prewarm lines, `WITHHELD (unwarmed): [165, 166, 167]`), `MAP_TRAVEL_UNLOCK [7 destination(s) in arr4]` after `INSTANCE_LOAD_INFO`; then `M` **asserted the client**: `worldMapDims.x == mapDims.x * DXT_BLOCK_SIZE`, `GmMapView.cpp(1731)` (a single assert, quoted as the evidence). The same log says why, 2 lines later: `FOG INIT SKIPPED: continent 4 has no fog_dims row … an M press kills the client on GmMapView.cpp(1731) … Do not script key:m against this map.` The runsheet warned against exactly this and then chose the map it applies to |
| `20260924T091054` — `--map 148 --persist`, `M` | `MAP_EXPLORATION_INIT_BEGIN [continent 1, fogged …; the M key is safe]`; the world map opens — the pre-Searing view, **one pin: Ascalon City** ("Currently in Ascalon City") |
| `20260924T091253` — the same, zoomed out | no change: that view is the whole pre-Searing map |

**Why it is blocked.** Every destination `0x0094` offers is either on another continent's view
— 242, 248 and 310 have NO `map_continent` row in `content/fog.toml`, so the fog init is skipped
there too and runsheet step 2's "press `M` there" would assert the same way — or withheld as
unwarmed (165 and 166, the pre-Searing slots, whose authored files are not in this archive). So
no fog-initialised view carries a second pin, and neither the sufficiency question (step 1) nor
the click (step 2) can run. What would unblock it, cheapest first: two warmed outposts on one
continent-1 view (install the authored 165/166, or give a real continent-1 outpost a content
row); or fog dims for another continent, which `content/fog.toml` says one retail capture on that
continent settles. The arm itself (`0x00B1` → `0x01D9` + the transfer pair) stays confirmed only
by its tests.

## 4. The display mode's field step (studies/cmsg "The display mode")

`20260924T091443`, `20260924T091636` (`--map 148 --persist`, `I`, then a click on the headgear's
eye, then on its corner arrow): each click raised only the tooltip `Helm Status: Always Show`;
the harness's synthetic click does not open that drop-down. **The owner's hands** — the steps
are the study's paragraph "The field step needs `--persist`", unchanged.

## 5. What is owed after this session

- **Fix the town-weapon switch regression (desk):** read what retail's outpost weapon-set switch
  carries (the study's (b) bursts) and the player's own `0x006D` at load; send the redraw retail
  sends, or, if nothing retail sends explains it, restore the hand `0x006F` on a town switch as a
  labelled RECONSTRUCTION. Correct (e) and the runsheet's prediction (1) in the study.
  **DONE at the desk the same day** (studies/cmsg "The town weapon" (f), PLAN-LOG): the "likeliest"
  carrier above is withheld — our load's player `0x006D`, which retail never sends the own body
  (0 of 46 outpost connections with a controlled agent, 0 of 44 field); retail sends NO redraw
  on its outpost switches because nothing drew the body. That it WAS the carrier is
  CORROBORATED (the binary's one store, last writer wins; a field frame, run 20260923T154229,
  where its zero off hand erased the `0x006E`'s shield), not yet OBSERVED: no run above withheld
  it, and a client arming the body from the equipped bag once at load gives the same four
  frames — the fix's runsheet ARM 1 is the observation (the fix pass's relabel). Withheld in a
  town; `--town-player-weapons` restores it; the runsheet predicts the body EMPTY-HANDED after
  F2, not sword and shield.
- **Arm the henchman kick (desk):** c2s `0x00A8 [agent]` is now named from our client; its reply
  is still on no tape (RECONSTRUCTION, or LIVE-4's capture).
- **Unblock travel's confirmation (content):** see §3.
- **The display mode's field step (owner):** §4.

## 6. Quest gold and the purse (DESKWORK-D9, landed `3bd7de5b`; studies/quests §12.1's runsheet)

Seven more launches after pass 5 landed (main `05bd36a4`), the store backed up first and restored
after (sha256 `e3ad6146…` again). `--map 449 --persist` with the `quest_objective` probe (the
giver, agent 99, offering quest 1463 = `rurik_first_errand` and 1464; the scout, agent 98). The
runsheet's clicks do not survive as written: the giver's list carries TWO quests, and its
(0.480, 0.545) is row 2 (1464); a world click on the giver only walks there; the working
sequence drives the two interactions through the harness's `interact:AGENT` mailbox (the SERVER
runs its INTERACT arm; the log says so on every use) and clicks the fixed dialog: row 1
(0.48, 0.522), Accept (0.4649, 0.55). No `M` press (449 has no fog init, §3).

| Step | Run | Result | Prediction |
|---|---|---|---|
| 1: a zero purse at load | `20260924T111639`, `112623` | no `GOLD_CREDIT(load purse` on either load; the counter reads 0 (the §1 frames) | held |
| 2: the offer | `20260924T112111` | the offer screen reads **Reward: 100 Experience / 10 Gold** — the first gold line on our client, reward slot B drawn as gold | held |
| 2: the hand-in | `20260924T112623` | accept (`QUEST_ADD[1463]`), `objective for quest 1463 met at agent 98` (`0x0054`), the giver's list offers the bag (code 0x07, kind 23), the turn-in: `QUEST_REMOVE[1463] (turn-in, 1 of 1)`, `experience +100`, **`gold +10` (`0x0140`)**, `PERSIST: … purse 10`, `QUEST_REMOVE_AND_UNLIST[1463]`, the dialog closes; the inventory's counter reads **10** | held — Q3: the counter rises by exactly the row's gold; the reward rides INSIDE the `0x0052 · 0x004A` pair with no assert |
| 3: relaunch under `--persist` | `20260924T112953` | `GOLD_CREDIT(load purse 10)` right after the load's `0x0144`; the counter reads **10** — not 0, not 20 | held (the relaunch half of Q1) |
| 4: control, `--no-quest-gold` | `20260924T112953`, `113244` | the offer reads **Reward: 100 Experience** with NO gold line (the dialog text 180 B against the default's 194 B); the hand-in prints `reward_gold = 10 is NOT GRANTED`; the counter **stays 10**; the quest log's Active Quests is **empty** after the turn-in (Q2) | held |

**Not run:** Q1's ZONE half (the purse read once after an in-session transfer, the client's
re-sent `0x0144 [1, 0]` resetting its purse) — no reachable zone on this content (§3's travel
block); the optional `--no-reward-in-frame` A/B (the default order worked, so it has nothing to
separate); the merchant buy/sell (optional).

## 7. The town weapon after the carrier fix (merge `2762740d`)

Three launches on the merged tree, outpost, `--weapon-set 1=starter_sword+starter_shield`, no
`--persist`. The fix (studies/cmsg "The town weapon" (f)) withholds the player's own `0x006D`
in a town — retail sends the own body none — and `--town-player-weapons` puts it back alone.

| Arm | Run | The wire | The screen |
|---|---|---|---|
| T1: default, no input | `20260924T123930` | the empty-handed `0x006E`, then `TOWN WEAPON: the player's 0x006D (lead item 1) is not sent`; no `NPC_UPDATE_WEAPONS` on c1 | the body stands **EMPTY-HANDED** (both hands open); the doll holds the hammer |
| T2: default, F2 | `20260924T124139` | `SET_ACTIVE_WEAPON_SET(1)` and the town switch's withheld hand `0x006F`, as in A2; no `0x006D` | the body is **still empty-handed**; the doll shows the sword and shield — no stale weapon |
| T3: `--town-player-weapons`, F2 (KNOWN-BAD) | `20260924T124357` | `NPC_UPDATE_WEAPONS(leadhand = item 1)` at load | the body **holds the HAMMER** after F2 — CONFIRM-2's A2 reproduced |

**The carrier is OBSERVED:** one message separates T1/T2 from A1/A2 and T3, and it is the whole
difference on screen. §1's regression is closed by removing our invention rather than adding a
redraw: retail's town body is bare, so there is nothing to go stale. **The owner's answer
(2026-09-24): on retail no weapon is shown in a town other than on the doll** — the same
picture. The FIELD's player `0x006D` erasing a shield at load was fixed the same day and is §8.
Town combat with no hand declared is out of scope — the owner, 2026-09-24: combat in a town is
non-stock behaviour, so there is no retail picture to match.

## 8. The field shield (merge `e7893067`, main `41988b1d`)

Three launches on the landed tree, explorable (`--explorable`, the default map), `--player-weapon
starter_sword --player-offhand starter_shield --practice-target`, `--enemy`, no `--persist`. The fix
withholds the player's own `0x006D` in a FIELD as well — retail sends the own body none (0 of 44) —
and `--field-player-weapons` puts back the pre-fix `[player, lead, 0]` alone. Predictions were
written before the first launch. Before it, an orphaned sandbox stack held 6112 (the orchestrator
window closed at ~14:35 leaves its servers up); the owner cleared it, and `test_handshake` ran
green (24) on the landed tree.

| Arm | Run | The wire | The screen |
|---|---|---|---|
| F1: default, no input | `20260924T153739` | the `0x006E` `(weapon+armour)` with the shield at visual 1, then `FIELD SHIELD: the player's 0x006D (lead item 1) is not sent`; no `NPC_UPDATE_WEAPONS` for the player | the body holds the **sword in the right hand and the SHIELD on the left arm** (`walk2-shot.png`) |
| F2: default, `C` + `Space` at the practice target | `20260924T153945` | the same load; c2s `0x8026 ATTACK`, then 37 `damage 2`/`3 to agent 10` in about 50 s | the body **swings the sword** — four frames at four phases of the swing (`walk1/3/5/7-shot.png`), the shield on the arm in every one, `-2` over the target; no crash dialog |
| F3: `--field-player-weapons` (KNOWN-BAD) | `20260924T154209` | the same `0x006E`, then `NPC_UPDATE_WEAPONS(leadhand = item 1)` and the flag's KNOWN-BAD line | the body holds the sword and **NO shield** — run `20260923T154229`'s picture, now on the same map and frame as F1 |

**The field shield is OBSERVED on our client:** one message is the whole difference between F1 and
F3, and withholding it leaves the body what the `0x006E` drew. The town fix's "swing path
UNVERIFIED" is closed as far as this client shows it: with no `0x006D` for the player at all, the
sword swings, the hits land and nothing asserts (F2), as the desk read predicted (the two workers
are one code body; the lead's type byte is the setter's own write). None of the three runs left a
`crash-dialog.txt`.

## 9. Pass 6's kick and leash runs (main `bb84484e`)

The owner's go-ahead: "go ahead with the kick and leash runs". Harness-driven, hands off, loopback,
build 38797, the landed pass-6 tree (`--persist` never passed, so the store was not touched). The
runsheet is the lanes' (CLEANUP-3 round 2's K0–K5; D8's ARMs A–D with the review's fixes), with the
deviations named below. Frames and logs under `vault/captures/harness/<stamp>`.

**The henchman kick (CLEANUP-3), map 148, `--area outpost_henchmen --party slice`:**

| Arm | Run | The wire | The screen |
|---|---|---|---|
| K0 discovery | `20260924T204559` (a first launch, `204350`, never delivered the Play click: focus refused, the machine in use) | `0x009F` add, `0x00B0 (3)`, `0x01BF` | Party Formation 3/4 with Lukas; **no X on the henchman's row in the party panel** — the Henchmen tab's own **Kick** (0.597, 0.67) is the route, CONFIRM-2 step 6's |
| K1 + K2, kick then re-add | `20260924T204808` | c2s `0x00A8`, then `PARTY_HENCHMAN_REMOVE(party 1, agent 31)` `0x01C0`, `PLAYER_PARTY_SIZE(2)` `0x00B0`, the `HENCHMAN_KICK … size now 2` line; then the re-add's `0x00B0 (3)` + `0x01BF` | **3/4 → 2/4, the row gone** (the client then offers Lukas in the panel's invite slot) → 3/4 again, the row back; the NPC standing throughout, no `0x0021`, no dialog |
| K3, the count with the hero | `20260924T205051` | two adds → size 4; a third `0x009F` **sent by the client at 4/4** and refused by our cap (`OUTPOST_PARTY_CAP=4`, nothing sent); the kick → size 3; the third add → size 4 | 4/4 (Lukas, Yuun) → 4/4 unchanged → 3/4 → 4/4 (Yuun, Mai). The runsheet predicted the client would refuse the 4/4 add itself (CONFIRM-2 step 4); it sent it, and the server's refusal is what held the count |
| K4, `--no-henchman-kick` | `20260924T205355` | c2s `0x00A8`, `HENCHMAN_KICK ignored (--no-henchman-kick)`, nothing sent | the row stays, 3/4 |
| K5, the launch row (`--henchman hatcher`) | `20260924T205640` (the runsheet's command omitted `--henchman`'s NPC key; `205614` was refused by the parser) | no `0x00A8` | the launch row draws as "Lvl 255 …" (the hatcher's name ids on a `0x01BF`); after a click on it the party panel's **Kick stays greyed** and nothing is sent. Whether the click SELECTED the row is not visible in the frame, so this is "not offered on that route", weak |

**The kick is CONFIRMED on our client:** the RECONSTRUCTION reply (`0x01C0` row, then `0x00B0`
size) removes the row and drops the count, a re-add restores both, and the flag's control keeps
CONFIRM-2's picture. The launch row's Kick stays UNVERIFIED-leaning-not-offered.

**The leash and the caster (D8 steps 3 and 4), `--explorable`, `--enemy`:**

| Arm | Run | The wire | Verdict |
|---|---|---|---|
| A as written (`--no-enemy-skills --enemy-leash 600`, `S:9`) | `20260924T205858` | ONE follow and halt; the client sent **no movement message at all** during the 9 s `S` hold (heartbeats, one `ROTATE_PLAYER`, a `TARGET_SELECT`), so the server's player never moved and the Hatcher swung it to death (`--enemy-hit` default 0.10) | VOID — a run-shape defect, not a leash result; the backpedal is not a usable driver here |
| A re-shaped (the Hatcher at `--enemy-offset -300,0` behind, `W:8` away, `--enemy-hit 0.02`) | `20260924T210317` | the chase (follows re-pathing at 0.5 s), then `GIVES UP: the player 1014 u from its anchor for 3.0 s (leash 600 u, 3.0 s)`, `RETURN leg 1` (896 u out) at t=18.48 s, `RETURN leg 2` (396 u) at 20.23 s — **1.75 s apart, retail's p50** — `is HOME: 0 u from its anchor after 2 legs, 3.1 s`; no `0x0028` at the give-up, between the legs or at the end; no death, no dialog | **HELD** (the frames face away up the stairs; the wire is the evidence) |
| A control, `--no-leash-return` | `20260924T210543` | the same chase; no GIVES UP / RETURN / HOME; the Hatcher keeps following, since the old rule halts only when the target is 1,012 u from IT | **HELD** — the flag is the whole difference |
| B, the caster inside range (the default Hatcher, the player standing) | `20260924T210744` | `stands and casts: 300 u from the player`, skill 253, no follow; 312 (touch) NOT cast from 300 u (the re-emitted `touch_range`); then **nothing for ~57 s** although 253 recharges in 5 s | the opening HELD; **a DEFECT: the caster stalls after its first cast** |
| B control, `--no-caster-opening` | `20260924T210957` | the old walk-in to 80 u; 253, 312, 289 cycling all run | the stall is the caster branch's |
| C, the leg to range (`--enemy-cast-range 450 --enemy-offset 700,0`) | `20260924T211150` | `CASTER leg: agent 10 -> range point (9380,8077) … 450 u short` (a `0x0029`, never a `0x002A`), `halts … parked, 446 u from the player`, `casts skill 253`, `stands and casts: 446 u` — then the same stall | the opening HELD; the stall again |
| D, the notice gate (`--enemy-offset 1100,0`) | `20260924T211323` | the Hatcher created at 1,100 u; no target, no cast, no follow for the whole run | **HELD** — a caster makes no notice beyond `AGGRO_RANGE` |

**The stall, read at the desk:** `pick_skill` is round robin from `last_slot + 1`, and `last_slot`
advances only on a cast. The EV-1 per-skill reach gate HOLDS a touch slot out of reach (`slot =
None`) without stepping past it, so once the re-emitted `skills.toml` marked 312 touch, 312 was
re-picked and held on every tick and the bar never reached 289 or 253 again. The heal gate above
it already steps past a held slot and re-picks, bounded by what was held; the fix gives the reach
gate the same shape (branch `desk-casterfix`). The desk tests could not see it: they never ran a
caster against a table carrying the touch bit, which only the landing's re-emit introduced.

**The stall's fix, re-run on the client (main `1926a31f`, 2026-09-25, the same go-ahead):**

| Arm | Run | The wire | Verdict |
|---|---|---|---|
| B again (the default caster, 300 u, the player standing) | `20260925T012823` | 16 casts cycling 253 / 289 from 300 u (`stands and casts` each); `holds skill 312: 300 u from the player, its reach 92 u` on every pass; 276 held (nobody under 90 %); `cannot cast … needs N energy` while the pool refills; no follow, no dialog | **HELD** — the bar cycles past the held touch skill |
| C again (`--enemy-cast-range 450 --enemy-offset 700,0`) | `20260925T013011` | the one `CASTER leg` `0x0029` to 446 u and its halt, then 13 casts (253 ×6, 289 ×7) from there; no `0x002A` | **HELD** |

The stall of `20260924T210744` / `211150` is gone on the client: one held touch slot no longer
freezes the bar, and the caster keeps casting from where it stands.

**CORRECTED 2026-09-25 — two of section 9's readings were the orchestrator's, and wrong**
(the owner asked for "those 3 issues" to be fixed; re-reading the captures and four more
launches on `069635de`, the same go-ahead, show two were never defects):

* **ARM A as written was a DEAD PLAYER, not a silent backpedal.** The capture (`205858`) has
  `KILL the player` at wall 1790297986.79 and the `S` hold starting at 1790297987.71 — the
  Hatcher (`--enemy-hit` default 0.10) killed the player during the plan's opening `wait:4`,
  before the key went down; a corpse sends no movement. (The memory "--walk number-key trap was
  a DEAD PLAYER" is this trap.) On a live player (`20260925T083808`, no enemy) the `S` keydown
  sends `MOVE_SET_HEADING` with a heading of (−766.8, 0) at once and the server leads the body
  backwards (`KBD LEAD (9306,8077) from (9826,8077)`). The backpedal works. That run then backed
  into the `ascalon_to_corridor` portal 120 u behind the spawn, zoned to map 168 (an authored
  map with no navmesh in the archive), and its `W` leg walked past the map's rect (x > 3,072)
  on server re-grants marked `[no-mesh]` until the client asserted
  `MapQueryAltitude() invalid params` (crash-dialog.txt) — a defect of its own, not this item's.
  (`20260925T083956`, the next launch, never left the character screen: after a crash the client
  asks "did not cleanly disconnect … reconnect?" and eats the Play click; `--actions
  "0:click:0.5723,0.5817 2:play"` presses No first.)
* **The party panel's Kick WORKS; it needs a selected row.** K0–K3's frames show it greyed
  because nothing was selected. Hovering Lukas's row (`20260925T084210`, `hover:0.936,0.352,2`)
  lights the row and shows no X (none exists on hover either); CLICKING the row
  (`20260925T084415`) selects it, the panel's Kick lights, and its click (0.936, 0.417) sends
  `c2s 0x00A8 [31]` 0.17 s later — our reply removes the row, 3/4 → 2/4. Both routes (the
  Henchmen tab's Kick, the party panel's) send the same word and both are CONFIRMED.
* **K5 firms up:** the same click that enables Kick on a hired row left it greyed on the LAUNCH
  henchman's row, so the client does not offer that kick (OBSERVED, one launch each).
* The third item — the full-party add — is section 2's correction above: the client sends it,
  our cap refuses it silently. How retail answers is a desk study (branch `desk-partyfull`).
