# Placement and player defaults: what stands where, and what the player starts with.
#
# The spawn table is the one that will grow. Today it holds a single test enemy placed
# relative to the player so that combat is exercisable at all; R4c's real spawns are
# server-only data that has to be captured, and none of it exists yet
# (PLAN.md section 1.7 names spawn placement as one of the four genuinely server-only
# things). When those arrive they are rows here, not code.

[spawn.test_enemy]
npc = "hatcher"
agent_id = 10
definition = 3
max_health = 100
# ARMOUR RATING is DERIVED, not declared -- see creature_armor_rating() in
# authsrv.py. It was `60` for one commit, chosen because that is the Isle's own
# Suit baseline, and 60 is level-20 armour on a level-1 creature: the wrong
# SHAPE, which is exactly the failure studies/monsterai section 3.3 already paid
# for on reach. WIKI (GWW, "Armor rating", rev. 2026) gives the formula --
# `creature AR = 3 * Level + armor bonus`, the bonus being profession-specific
# -- so this row already carries everything needed: the npc's `level` and
# `profession`. A number derived from two facts we hold beats a third fact
# invented beside them.
#
# Uncomment to OVERRIDE, which the same page says is often right: "Many
# creatures in PvE do not follow this formula and may be significantly
# resistant or vulnerable to certain types of damage." An override is a
# measurement about one creature and should cite where it came from.
# armor_rating = 60
# Placed relative to the player's arrival point rather than at an absolute coordinate,
# because the player's arrival point differs per map and this enemy is meant to be
# reachable in whichever one is loaded.
offset_x = 300.0
offset_y = 0.0
enabled = true
# Burrowing: the Plague Worm cycle, off by default. See [spawn.test_enemy.burrow].
burrow = false
burrow_out_seconds = 5.0
burrow_hidden_seconds = 4.0

[spawn.test_enemy.provenance]
source = "invented"
verified = ""
note = """
Ours, chosen rather than observed, and the row says so. There is no evidence that
anything stands 300 units east of the arrival point in any Guild Wars map. This exists
so that a hostile body can be hit, killed and revived on demand -- studies/enemy
records the repeatable cycle it supports.

`agent_id = 10` and `definition = 3` are allocator choices, not facts about the game.
studies/enemy/PLAN.md section 7.2 asks for a real agent table with allocated ids; when
that lands these two fields stop being constants and this row keeps only the placement.

max_health 100 is a placeholder matching the player's, so that damage arithmetic is
easy to read by eye during a fight. Absolute monster health is server-only data
(PLAN.md section 1.7) and nothing here has measured a Hatcher's.

BURROWING, added 2026-08-10, and the split of what is measured from what is chosen
matters more than the numbers.

`burrow_out_seconds` and `burrow_hidden_seconds` are INVENTED, like the rest of this
row, and they are the honest part of a mechanic whose other half is not. Live, these
are NOT periods: a Plague Worm stayed out 0.48-7.48 s (median 1.46) and hidden 1.6-6.9 s,
and two families sharing one model ran ~8-10 s and ~33 s cycles with distance from the
player failing to explain the split. 5.0 and 4.0 sit inside the observed ranges and are
otherwise ours. Do not read them as the game's.

The two numbers that ARE measured -- 2.00 s to emerge and 2.00 s to submerge, over 140
worm creates, with every sample inside +/-40 ms (emerge n=137 in [1.976, 2.021],
submerge n=132 in [1.973, 2.037]; the two n differ because a capture starts and ends
mid-cycle) -- deliberately did NOT come here. They are not
authorable: they are how long ArenaNet's server holds the transition effect bit before
and after the create/remove, which is protocol timing rather than a fact about this
particular creature, and this file's own header calls moving that kind of value here a
category error. They live in authsrv.py beside the opcode constants, cited.

A Hatcher is standing in for a worm. Nothing in Guild Wars says a collector burrows;
this row exercises the CYCLE, not the creature. The real worms' definition (1442, file
116366) was observed in our live capture and is deliberately not transcribed here --
importing an ArenaNet creature into tracked content is a judgement for the owner, and
the cycle is testable without it.
"""

# ------------------------------------------------- what a skill's scale MEANS
#
# THE CLIENT'S TABLE DOES NOT SAY. `scale0`/`scale15` is a generic magnitude
# pair; nothing in the 164-byte record marks it as damage, healing or a health
# bonus, and `type_code` is the skill's TYPE (spell/stance/shout/attack), which
# does not discriminate either -- a Spell can heal or harm.
#
# Reading the endpoints as damage would have been an invention wearing a
# measurement's clothes, and a bad one: three of the four skills on our own
# enemy's bar are NOT damage. The enemy would have "damaged" the player with a
# heal for 10-70 and an enchantment for 40-200.
#
# So the magnitude is measured and the MEANING is sourced here, from GWW's own
# `{{Skill progression}}` variable names -- the wiki's word, quoted verbatim,
# not our classification. Ids are the client's; every row's number was checked
# against the client table and matches endpoint for endpoint.
#
# `toolkit/authsrv/authsrv.py` models only the labels it names in
# SCALE_MEANS_DAMAGE. Everything else lands as "no modelled effect" and says so
# in the log rather than being quietly treated as damage.

# ---------------------------------------------------------------------------
# R4b'S NINE EXEMPLARS, added 2026-08-20.
#
# PLAN.md 3.2 grades R4b as "n of 9": one named skill id per `type_code` family
# Pre-Searing reaches. These are the rows that let the server resolve them. All
# eight progressions below were read from GWW in one pass on 2026-08-20 and
# **all eight agree with the client's own endpoints**, which is worth stating as
# a number because two of them agree in a way that could not be luck:
#
#   * Sever Artery -- GWW gives ONE variable, `Bleeding` 5..25, and the client
#     carries 5..25 in the BONUS slot with `skill_arguments = 4` (bonus only).
#     The bitfield said which slot before the wiki was read.
#   * Faintheartedness -- GWW gives TWO, `Duration` 3..16 and `Health
#     degeneration` 0..3, and the client carries 3..16 in the duration slot and
#     0..3 in the bonus slot with `skill_arguments = 5` (duration | bonus). Two
#     variables, two enabled bits, two matching pairs, and the SCALE slot's 50
#     sits there with its bit clear -- the "you attack 50% slower" from the
#     description, which is not a progression.
#
# That is the wiki and the client agreeing on WHICH SLOT as well as on the
# numbers, from sources that share no author or ancestry.

[skill_effect.194]
scale_means = "Fire damage"
[skill_effect.194.provenance]
source = "wiki"
page = 'GWW, "Flare", {{Skill progression}}, rev. as fetched 2026-08-20'
verified = """Client scale 20->65, args = 2 (scale). Wiki var1 `Fire damage`
20->65. The bonus slot holds 1800 with its bit CLEAR -- another Rush-shaped
constant, and the reason the bitfield is honoured rather than the endpoints
read."""

[skill_effect.1]
scale_means = "Heal"
[skill_effect.1.provenance]
source = "wiki"
page = 'GWW, "Healing Signet", {{Skill progression}}, rev. 2026-04-01'
verified = """Client scale 82->172, args = 2. Wiki var1 `Heal` 82->172. Goes out
on property 55 positive -- the health-GAIN direction, measured from the corpus
(agents.GV_HEALTH_GAIN), not a negative damage."""

[skill_effect.307]
scale_means = "Maximum heal"
[skill_effect.307.provenance]
source = "wiki"
page = 'GWW, "Reversal of Fortune", {{Skill progression}}, rev. 2023-05-02'
verified = """Client scale 15->80, args = 2. Wiki var1 `Maximum heal` 15->80.
KNOWN SIMPLIFICATION, carried in skill_heal's docstring: the skill heals for the
damage it prevents, capped at this number, and this server models no damage
prevention -- so it heals the cap. Right at the ceiling, wrong below it."""

[skill_effect.135]
scale_means = "none"
bonus_scale_means = "Health degeneration"
[skill_effect.135.provenance]
source = "wiki"
page = 'GWW, "Faintheartedness", {{Skill progression}}, rev. 2024-05-16'
verified = """args = 5 (duration | bonus). Wiki gives exactly two variables and
the client carries exactly those two slots: `Duration` 3->16 in duration0/15 and
`Health degeneration` 0->3 in bonus_scale0/15. The scale slot's 50 has its bit
CLEAR and is the "attack 50% slower" from the description. Degeneration is not
modelled -- the row records what the number IS, not that we send it."""

[skill_effect.431]
scale_means = "Fire damage"
[skill_effect.431.provenance]
source = "wiki"
page = 'GWW, "Ignite Arrows", {{Skill progression}}, rev. 2024-12-05'
verified = """Client scale 3->18, args = 2. Wiki var1 `Fire damage` 3->18 -- the
SAME LABEL as Flare's, and it does not mean the same thing. This is a
Preparation: WIKI (GWW, "Preparation") "preparations generally alter bow
attacks, allowing the fired arrows to cause additional effects", so the fire
damage rides the next arrows rather than landing on cast. The server refuses to
resolve damage for any episode-opening skill for exactly this reason."""

[skill_effect.200]
scale_means = "Energy"
[skill_effect.200.provenance]
source = "wiki"
page = 'GWW, "Glyph of Lesser Energy", {{Skill progression}}, rev. 2024-02-27'
verified = """Client scale 10->18, args = 0 -- BOTH endpoints present with the
bit clear, and the wiki lists the progression, so this is one of the rows where
the two disagree about whether it scales. Not resolved here: the server sends
the 15 s glyph episode and models no energy, so nothing reads this number yet.
Recorded so the disagreement is on file rather than discovered later."""

[skill_effect.382]
bonus_scale_means = "Bleeding"
[skill_effect.382.provenance]
source = "wiki"
page = 'GWW, "Sever Artery", {{Skill progression}}, rev. 2023-02-24'
verified = """args = 4 (bonus only), bonus 5->25, scale 0->0. Wiki gives one
variable, `Bleeding` 5->25. The bitfield picked the slot before the wiki was
read. Costs 4 adrenaline, which this server does not model."""

[skill_effect.346]
scale_means = "none"
[skill_effect.346.provenance]
source = "wiki"
page = 'GWW, "Frenzy", rev. 2023-02-17'
verified = """NO {{Skill progression}} template on the page, and the client
agrees: args = 0. The scale slot's 33 is the "attack 33% faster" of the
description and the duration slot's flat 8 is the real duration -- which is the
row that shows the duration bit means SCALES rather than MEANINGFUL, since the
server sends 8 s and the client draws an 8 s bar."""

[skill_effect.312]
scale_means = "Holy damage"
bonus_scale_means = "+ Holy damage"
[skill_effect.312.provenance]
source = "wiki"
page = 'GWW, "Holy Strike", {{Skill progression}}, rev. 2024-05-16'
verified = """Client scale 10->55 and bonus_scale 10->55, skill_arguments = 6
(scale|bonus). The wiki lists exactly those two vars at exactly those endpoints,
and its concise description reads "Deals 10...55 holy damage. Deals 10...55 more
holy damage if target is knocked down." """

[skill_effect.322]
scale_means = "+ Damage"
[skill_effect.322.provenance]
source = "wiki"
page = 'GWW, "Power Attack", {{Skill progression}}, rev. 2026-07-05'
verified = "Client scale 10->40, args = 2 (scale). Wiki var1 `+ Damage` 10->40."

[skill_effect.323]
scale_means = "+ Damage"
[skill_effect.323.provenance]
source = "wiki"
page = 'GWW, "Desperation Blow", {{Skill progression}}, rev. 2026-07-05'
verified = "Client scale 10->40, args = 2 (scale). Wiki var1 `+ Damage` 10->40."

# The rest of the two bars, declared so "not damage" is a RECORDED FACT with a
# citation rather than a silent default. The server models none of these.
[skill_effect.276]
scale_means = "Healing"
[skill_effect.276.provenance]
source = "wiki"
page = 'GWW, "Restore Condition", {{Skill progression}}, rev. 2024-05-16'
verified = "Client scale 10->70, args = 2. Wiki var1 `Healing` 10->70."

[skill_effect.253]
scale_means = "Duration"
[skill_effect.253.provenance]
source = "wiki"
page = 'GWW, "Scourge Sacrifice", {{Skill progression}}, rev. 2026-02-05'
verified = """Client duration 8->20 with args = 1 (duration only); the scale
slot holds a constant 100 with its bit CLEAR, which is the same shape as Rush's
25 and is why the bitfield has to be honoured."""

[skill_effect.289]
scale_means = "+ Maximum health"
[skill_effect.289.provenance]
source = "wiki"
page = 'GWW, "Vital Blessing", {{Skill progression}}, rev. 2026-07-05'
verified = "Client scale 40->200, args = 2. Wiki var1 `+ Maximum health` 40->200."

[skill_effect.316]
scale_means = "+ Max health"
[skill_effect.316.provenance]
source = "wiki"
page = 'GWW, "To the Limit!", {{Skill progression}} (via studies/skills/FINDINGS.md section 4, crawled 2026-08-14)'
verified = """Client scale 10->60, args = 7. FINDINGS section 4 records the GWW
progression as Max foes 1->6, Duration 10->20, + Max health 10->60. Cited from
there rather than re-fetched: the plain title redirects on the API."""

[skill_effect.317]
scale_means = "Duration"
[skill_effect.317.provenance]
source = "wiki"
page = 'GWW, "Battle Rage", {{Skill progression}}, rev. 2026-07-31'
verified = "args = 1 (duration only); the scale slot holds a constant 33."

[skill_effect.318]
scale_means = "+ Maximum health"
bonus_scale_means = "Damage reduction"
[skill_effect.318.provenance]
source = "wiki"
page = 'GWW, "Defy Pain", {{Skill progression}}, rev. 2026-07-05'
verified = "Client scale 90->300, bonus 1->10, args = 7. Wiki lists both."

[skill_effect.319]
scale_means = "Duration"
[skill_effect.319.provenance]
source = "wiki"
page = 'GWW, "Rush", {{Skill progression}}, rev. 2023-03-03'
verified = """args = 1; the scale slot holds a constant 25 -- the "move 25%
faster" in the description -- with its bit clear. THE row that proves reading
endpoints without the bitfield invents scaling the game does not draw."""

[skill_effect.320]
scale_means = "Crippled duration"
[skill_effect.320.provenance]
source = "wiki"
page = 'GWW, "Hamstring", {{Skill progression}}, rev. 2026-06-25'
verified = "args = 4 (bonus only); the scale slot is 0->0."

[skill_effect.321]
scale_means = "none"
[skill_effect.321.provenance]
source = "wiki"
page = 'GWW, "Wild Blow", rev. 2026-03-08'
verified = """No {{Skill progression}} template on the page at all, and the
client agrees: args = 0, every set disabled. Two independent ways of saying
this skill does not scale."""

# ---------------------------------------------------------------- the player

# The player's attribute ranks, as [attribute_id, rank] pairs.
#
# The IDS are measured -- they are the client's own `s_attrib` indices, read by
# toolkit/clientscan/attribtable.py, and 17..21 are exactly profession 1's
# (Warrior): Strength 17 (its primary), Axe Mastery 18, Hammer Mastery 19,
# Swordsmanship 20, Tactics 21. See studies/combat/PLAN.md section 10.
#
# The RANKS are ours. Nothing in the vault can source them: 0x003A appears 0
# times in 21,543 live GAME_SMSG, no c2s attribute-spend opcode was found in
# 52,778 decoded client messages, and there is no character-creation flow to
# take them from. So they are chosen, and the row says so.
#
# CHOSEN DISTINCT, and that is the one design decision worth keeping: with
# 12/9/6/3/1 no two attributes share a rank, so if the wire's triple slots were
# ever mis-ordered the attribute panel would show the numbers against the wrong
# names and a person could SEE it. A uniform spread would hide exactly that.
# Every value is <= 12, which is ArenaNet's own cap (AcctTemplate:441
# `data.attribValue[index] <= 12`, and CharData:202's bound of 13 on the
# s_attribPoints lookup).
#
# THE POINT BUDGET IS NOT MODELLED -- said out loud rather than left implied.
# We do not read the client's s_attribPoints cost table, we do not track spend,
# and 0x0037 goes out as [0, 0] because that is what all 8 live samples carry.
# Whether this spread is affordable at any level is a question this server
# cannot currently ask.
[player.attributes]
ranks = [[17, 12], [18, 9], [19, 6], [20, 3], [21, 1]]
points_total = 200

[player.attributes.provenance]
source = "invented"
note = """
Ids measured from the client's s_attrib table (attribtable.py, build 38797);
ranks chosen. Replace the ranks the day a capture shows a real character's --
capture shopping-list item 1, studies/combat/PLAN.md section 3 -- and the ids
should survive that, because they are the client's own.

points_total is the character's LIFETIME attribute-point budget, which
0x0037's fourth field carries and the panel spends down; the third field is
what is left (studies/pvpui/FINDINGS.md 32.5). The NUMBER 200 is observed
rather than invented -- it is the level-20 maximum, and 0x0037's fourth field
reads exactly 200 in 34 of the corpus's 48 sightings, with 0, 5 and 10 on
lower-level characters -- but GIVING IT TO THIS CHARACTER is a choice, which
is why the whole row stays `invented`. The ranks above already sink 173 of it
(97+48+21+6+1), leaving 27 unspent, so the panel opens with points to spend
and room to refund; change the ranks and that follows automatically, because
the server computes spent from the cost table rather than storing it.
"""

[player.defaults]
energy = 25
health = 100
float_43 = 0.0396

[player.defaults.provenance]
source = "gw-preservation"
verified = """The PROPERTY IDS these values ride on were verified against our own \
binary, which is the part that mattered: our read of the int-record dispatch found \
cases for {32, 41, 42} and nothing else (studies/agentprops/FINDINGS.md section 3b), \
we had already MEASURED 42 as maximum health, and 41 is the only remaining slot in \
that dispatch and the one upstream calls energy. The float channel does have a real \
case for 43."""
note = """
The VALUES are weaker than the ids and should not be read as measured. 25 energy and
100 health are a starting character's, taken from gw-preservation's
gameservice/player.go:268-269, not observed on our wire.

float_43 = 0.0396 is worse still and is kept only because it travels with the pair
above in a server that works. gw-preservation's own comment next to it says "REVERSE
THIS MORE", so nobody upstream knows what it is either. Energy regeneration is the
obvious reading and it is a GUESS. If something ever depends on this number, measure
it first.

UPDATE 2026-08-20 -- MEASURED, and the guess was right. Property 43 is energy
regeneration as a FRACTION OF MAXIMUM ENERGY PER SECOND, read off the one death in
the live corpus: the same character carried 0.0528f at 25 max energy and 0.0600f at
22, and 0.0528 x 25 = 0.0600 x 22 = 1.32 energy/s exactly -- the pool shrank under a
death penalty and the absolute rate did not move. GWW ("Energy", "Regeneration and
degeneration") gives one pip as 1 energy per 3 seconds, so 1.32/s is four pips at
0.33 each, and THIS row's 0.0396 is 3 x 0.33 / 25: three pips on a 25-energy
character. Nobody upstream knew what it was; it was never mysterious, only
unlabelled. studies/morale/FINDINGS.md section 2.3.
"""

# ------------------------------------------------- morale, and the death penalty
#
# Morale is a percentage with 100 as neutral. It scales the character's BASE health and
# energy -- not the totals -- and a death is -15 of it. Everything here is the RULE
# layer; the wire layer (which opcodes carry it) is in authsrv.py and the evidence for
# both is studies/morale/FINDINGS.md.

[player.morale]
baseline = 100
floor = 40
ceiling = 110
death_step = 15
xp_per_percent = 75
base_energy = 20
base_health_level_1 = 100
health_per_level = 20

[player.morale.provenance]
source = "wiki"
page = """GWW "Death Penalty" (sections Definition, Acquisition, Counters), "Morale \
Boost" (Definition, Notes), "Health" (Maximum health) and "Energy" (Maximum energy, \
Regeneration and degeneration), all read 2026-08-20."""
verified = """OBSERVED against ArenaNet's own wire, and the check could have failed: \
the one player death in the live corpus (20260817T183756, agent 27, t=78.813) carries \
morale 100 -> 85 on two channels at once, and BOTH maxima move by exactly what these \
numbers predict. Health 120 -> 102 is level 2's base 120 (= base_health_level_1 + \
health_per_level) less 15%. Energy 25 -> 22 is base_energy 20 less 15%, subtracted \
from a 25 total -- which is the discriminating one, because scaling the TOTAL gives \
21.25 and no rounding rule reaches 22 from there. The floor and ceiling are corroborated \
from a second lineage that shares no ancestry with the wiki: GWCA's PlayerAttrSet \
annotates morale_percent "Ranges from 40 to 110"."""
note = """
base_health_level_1 and health_per_level are GWW's published progression ("start at
level 1 with 100 health. This increases at 20 health per level up to a maximum of 480
health at level 20"), so base health is 100 + 20 x (level - 1). base_energy is the
innate 20 every profession has regardless of level; the +5/+10 from basic armor is
real total energy but is NOT scaled by morale, which is what the wire measurement above
shows and what GWW says outright: a boost "is not applied to energy or health from
equipped armor or weapon sets".

xp_per_percent is the PvE counter -- 75 experience removes 1% of death penalty. The PvP
counter (killing an enemy player removes 2% per living teammate) is deliberately absent:
this server has no PvP and inventing the row would put an unreachable rule in the world.

WHAT THIS ROW DOES NOT DECIDE: whether a death costs anything at all. That is the map's
question, and pre-Searing -- every map this server ships -- never charges one. See the
map_rule rows in content/maps.toml.

CORROBORATED the same day from the opposite direction (the energy arc, blind to
the death-tick derivation above): ALL FIVE distinct property-43 values in the
live corpus fit f32(0.33) * pips / max BIT-EXACTLY while the wiki's nominal 1/3
energy per second fits none of them, the (pips, max) pairs are exactly GWW's
basic-armour rows, and this row's 0.0396 (f32 0.03959999978542328) appears on
ArenaNet's own wire in capture 20260810T235916 on four connections -- three
pips, a Ranger's. Two derivations, no shared code path, one constant.
studies/skills/FINDINGS.md section 23. The pip count 3 rides with max 25 as a
pair; change either only together.
"""

# ------------------------------------------------- weapon and creature swing rates
#
# One row per weapon class. The client multiplies this by an attack-speed MODIFIER at
# 0x007F837E (`fld [esi+0xf0]; fmul [esi+0xec]`), and "unmodified" is 1.0 -- never 0,
# because the client asserts on a zero at both ends.

[attack_speed.rates]
axe = 1.33
daggers = 1.33
sword = 1.33
scythe = 1.5
spear = 1.5
hammer = 1.75
staff = 1.75
wand = 1.75
flatbow = 2.025
shortbow = 2.025
longbow = 2.475
recurve = 2.475
hornbow = 2.7
pet = 2.0
melee_minion = 3.1
bone_fiend = 1.86
unmodified_modifier = 1.0

[attack_speed.rates.provenance]
source = "wiki"
page = """GWW "Attack speed", section "Attack durations and effect of IAS and DAS", \
read 2026-08-06. The table says outright "These are the exact values used by the game"."""
verified = """A check that could have failed and did not: the client's own product \
reproduces the wiki's published IAS table exactly for three weapon classes and two \
modifiers -- 1.33/1.5/1.75 against 0.67 and 0.75 give 0.8911/1.005/1.1725 and \
0.9975/1.125/1.3125, six for six to four decimals. GWCA's Agent.h:181-182 \
independently names the same two offsets weapon_attack_speed and \
attack_speed_modifier and gives 1.33 for axe/sword/daggers, agreeing on both."""
note = """
WIKI (GWW, "Attack speed", section "Attack durations and effect of IAS and DAS", read
2026-08-06), whose table says outright "These are the exact values used by the game".

These are the rates the game uses, not rates we chose. A Hatcher's real swing rate is
not among them -- nothing has measured what a collector swings at -- so any code
standing an axe's 1.33 in for a monster is using a labelled placeholder.
"""

# ---------------------------------------------------------------------------
# AREA POPULATIONS -- what lives in a zone this toolkit authored.
#
# A row carrying `area` belongs to that authored area and is served by
# `authsrv --area NAME`, which REPLACES the single global test enemy above
# rather than adding to it: that one is placed by offset from the player, so in
# a zone with its own idea of what stands where it would appear in the middle
# of the arrangement.
#
# THE COORDINATES CAME OUT OF THE COMPILED NAVMESH, not off a screenshot, and
# that is the whole reason this could not have been written before rung (I).
# The sculpt map is **1.2% walkable by area** -- 13 trapezoids over a 64x64
# map, because a Blender basin and a hard-crested ridge leave most of the
# terrain steeper than the client's own walkable band. Picking a spot by eye
# gives ground about one time in eighty. Each position below is the CENTRE of a
# trapezoid the client itself built from our terrain, and the server re-checks
# every one against the mesh before a body goes out -- a position that is not
# ground is refused rather than placed, because a body standing where the
# server's own collision says nothing exists makes everything downstream reason
# about it wrongly.
#
# `definition` may be shared by rows naming the SAME npc and never by rows
# naming different ones; `agent_id` is unique always. Both are allocator
# choices, like the ones above, and are deliberately clear of the probes
# (agents 2..7, definition 2) and of `test_enemy` (agent 10, definition 3).

[spawn.sculpt_watcher]
area = "sculpt"
npc = "hatcher"
agent_id = 20
definition = 5
max_health = 100
# Trapezoid 10's centre, 140 units from the arrival point -- the body you see
# without turning round. Probe bodies placed far out read as "nothing appeared".
x = 2934.0
y = 3046.5
allegiance = "noncombatant"
attacks_back = false
enabled = true

[spawn.sculpt_watcher.provenance]
source = "invented"
verified = """Ours, chosen rather than observed, like every placement in an \
authored area. What is NOT invented is that the spot is standable: (2934, \
3046.5) is the centre of trapezoid 10 of the navmesh the retail client compiled \
from our own terrain, read back out of the archive after the run that built it, \
and the server re-checks it against that same mesh before creating the body. \
The npc template it names carries its own provenance in npcs.toml."""

[spawn.sculpt_hostile]
area = "sculpt"
npc = "hatcher"
agent_id = 21
definition = 5              # same template as the watcher, so one definition
max_health = 100
# Trapezoid 1's centre, ~478 units out: something to walk toward across ground
# that is walkable at both ends and mostly not in between.
x = 2618.6
y = 2921.0
allegiance = "hostile"
attacks_back = true
enabled = true

[spawn.sculpt_hostile.provenance]
source = "invented"
verified = """Ours. The position is trapezoid 1's centre in the client-compiled \
navmesh, chosen because it is walkable, roughly 478 units from the arrival \
point, and separated from it by terrain that is not -- which is what makes it a \
test of the ground rather than of the spawn. Shares `definition` with \
sculpt_watcher deliberately: both are hatchers, a definition is per-instance and \
outlives the agents using it (ArenaNet sends one for 140 re-creates of one \
worm), and this exercises the rule that sharing is allowed within a template."""

[spawn.sculpt_farside]
area = "sculpt"
npc = "hatcher"
agent_id = 22
definition = 5
max_health = 100
# Trapezoid 7, ~654 units. Three bodies rather than two so the population is
# visibly a SET -- a pair can be produced by code that handles exactly two.
x = 2595.5
y = 3520.0
allegiance = "hostile"
attacks_back = false
enabled = true

[spawn.sculpt_farside.provenance]
source = "invented"
verified = """Ours; trapezoid 7's centre in the client-compiled navmesh. It \
exists so the area holds THREE bodies rather than two -- a population of two is \
satisfied by code that handles exactly two, and the count is the claim here. \
`attacks_back = false` so that one of the two hostiles is passive, which keeps \
a run readable when both are in aggro range at once."""
