# NPC templates: what the client must be told before an agent of this type exists.
#
# The fields are exactly the ones agents.npc_properties() and agents.npc_model() take.
# Not optional and not decoration: the definition index is a raw array index on the
# client, and creating an agent whose definition was never sent takes the client down
# on `index < m_count` in Base\rtl\Array.h. OBSERVED.
#
# `enc_name` is a list of 16-bit GW string ids, not text. The client resolves them
# against its own string table and renders the result in whatever language it is
# running -- we sent four opaque words and got "Hatcher [Collector]" in English. These
# can be copied and cannot be invented.

[npc.hatcher]
name = "Hatcher [Collector]"
file_id = 116228
model_id = 116703
profession = 3
level = 1
enc_name = [0x328A, 0xE3B9, 0xAA36, 0x2E69]
scale = 0x64000000          # hue 0, saturation 0, lightness 0, scale 100%
flags = 0x20C

[npc.hatcher.provenance]
source = "gw-preservation"
verified = """OBSERVED working against our own client on build 38797, which is stronger \
than any cross-reference: it stood in the map wearing a collector's body with a gold \
nameplate, plainly different from the light-blue Trade-able player bodies section 6c \
produced, and the four EncString words resolved to "Hatcher [Collector]" in English. \
Independently, file id 116228 appears in BOTH gw-preservation (2026, hatcher_collector) \
and GWLP-R's mock NPC (2013, NPCFactory.java:109) -- thirteen years and two lineages \
apart. studies/enemy/PLAN.md sections E2 and 6."""
note = """
Two consequences drawn in studies/enemy/PLAN.md and worth keeping attached to the row:
gw-preservation's NPC table is a GOOD lead rather than a plausible one, and Gw.dat model
ids are far more stable across builds than this project's experience of opcode drift
would lead you to expect.

This is a borrowed Ascalon collector standing in for a monster. It is not a Pre-Searing
enemy and nothing about it has been checked against what actually spawns there.
"""

# ---------------------------------------------------------------------------
# The burrowing worm of Lakeside County. Every value below was read off
# ArenaNet's own server talking to ArenaNet's own client, in the owner's own
# capture -- not from any upstream table, and not from Gw.dat.
#
# PROVENANCE RULING, owner, 2026-08-10. This row was deliberately withheld when
# the burrow cycle landed, because copying a real ArenaNet creature out of a
# live capture sat close to the provenance gate. The owner's decision is that an
# NPC definition of this kind is DOCUMENTATION OF AN OBSERVED FACT -- the same
# character as a wiki entry recording a monster's name and level -- rather than
# a reproduction of binary data. It is nine integers describing what the service
# sent us, not an extracted asset. The gate itself is unchanged and unweakened:
# no client files, no Gw.dat, no extracted assets, no decompiled code.
#
# `enc_name` is FOUR OPAQUE STRING IDS, which is the clearest illustration of
# why this row is a description and not an asset: the words carry no text. The
# client resolves them against its own string table and renders the result in
# whatever language it runs. We cannot read them and did not need to.

[npc.lakeside_worm]
# NAME PROMOTED 2026-09-11 by the model parade -- see the note below, which
# asked for exactly this evidence and has been rewritten rather than deleted.
name = "Plague Worm"
file_id = 116366
profession = 1
level = 0
enc_name = [0x0F41, 0xBE66, 0xF22A, 0x04D4]
scale = 0x64000000          # identical to the hatcher's -- hue/sat/light 0, scale 100%
flags = 0x8
speed = 12.0                # its burrow-crawl speed, 151 of 151 creates
# NO model_id ON PURPOSE. ArenaNet declares this definition with 0x0056 and
# sends NO 0x0057 MONSTER_COMPOSITE for it at all -- 8 of the 44 definitions in
# the capture are 0x0056-only. Inventing a model id here is exactly the guess
# that made the first agent_removal run's negative meaningless, so the field is
# absent and the server skips the model message rather than filling it.

[npc.lakeside_worm.provenance]
source = "capture"
capture = "20260807T143055"
origin = "live"
verified = """OBSERVED, capture 20260807T143055, four chained live tapes of the owner's \
own session against the live service. Definition slot 1442 is declared by GAME_SMSG \
0x0056 NPC_UPDATE_PROPERTIES twice in the capture and the two declarations are \
byte-identical. 151 of the 166 mon1-class creates in the corpus name this definition \
through the create's tagged model field (tag 0x2, low 28 bits 1442), and those are the \
same agents that drive the burrow cycle measured in studies/tape/FINDINGS.md T3 -- the \
five-message create burst and the two 2.00 s +/-0.06 s transition windows, 148/148 and \
141/141. The field mapping is our own agents.npc_properties() read back against the \
wire, not a reconstruction: [definition, file_id, 0, scale, 0, flags, profession, \
level, enc_name] lines up field for field."""
note = """
THE NAME WAS OURS AND UNVERIFIED UNTIL 2026-09-11; IT IS NOW OBSERVED. This
paragraph used to read "burrowing worm is a label for a creature we have only ever
seen as an agent id and a definition slot... Do not promote it to a real name until
a client renders it." A client rendered it: the model parade
(studies/slice/RUN-PARADE.md, harness 20260911T231549) stood this definition in
Lakeside County and its nameplate read **Plague Worm** in English.

WHY THIS DOES NOT REST ON PICKING THE RIGHT BODY OUT OF A SCREENSHOT. The parade
stood fifteen bodies at once, so "which nameplate belongs to which row" is a real
question for most of them -- and it is not a question here. A nameplate is the
client resolving `enc_name` against its own string table, and the row the parade
spawned carries the four words below BYTE-IDENTICALLY (vault def_1442; checked
field by field, enc_name included). Same four words, same string, same plate. The
elimination argument that also points here is redundant.

CORROBORATION, and it was not used to reach the answer: studies/presearing's own
manifest lists "Plague Worm" among Pre-Searing's quest-only creature types, from a
wiki pass months earlier that had no connection to this definition slot.

`level = 0` is what the wire carries in the slot our sender calls level. That is recorded
rather than corrected: the field name is ours, and a worm whose level reads 0 is at least
as likely to mean the field is not a level as it is to mean the worm has none.
"""

# ---------------------------------------------------------------------------
# THE PARADE'S OUTPUT, PROMOTED. SLICE-B1, 2026-09-12.
#
# Both rows were emitted into the gitignored vault overlay by npcdefs.py months
# ago and neither had a NAME, because npcdefs deliberately does not write one:
# "a name comes from a rendered nameplate or it does not exist". The model
# parade (studies/slice/RUN-PARADE.md) is where they got one -- each of these
# two stood ALONE in an empty world, so the nameplate on screen belongs to this
# definition and to nothing else, with no projection or elimination involved.
#
# WHY THEY ARE TRACKED NOW AND THE OTHER FIFTY-FOUR ARE NOT. A spawn row in the
# tracked content/world.toml that names a vault-only template loads here and
# fails on a bare machine (project-rurik-bare-machine-defect-class). The quest
# giver and its objective NPC are the first content rows that need to be in git,
# so they are the first templates that have to come with them. The rest stay in
# the overlay until something tracked needs them.
#
# The `name` is ours to the extent that we chose to write down what the client
# drew; every other field is ArenaNet's, measured, and unchanged from the
# overlay row it was copied from.

[npc.lieutenant_fisk]
name = "Lieutenant Fisk"
definition = 1458
file_id = 116228
model_id = 116671
scale = 1677721600
flags = 524
profession = 1
level = 2
move_speed = 250.0
enc_name = [3914, 61387, 52282, 21632]

[npc.lieutenant_fisk.provenance]
source = "capture"
capture = "20260807T143055"
origin = "live"
extractor = "toolkit/authsrv/npcdefs.py"
verified = """OBSERVED on ArenaNet's own wire: definition 1458 declared by \
GAME_SMSG 0x0056 in 2 connections across 2 captures, payloads byte-identical, \
2 creates carried it. Every numeric field is read from that declaration and is \
unchanged from the vault overlay row npcdefs.py emitted.

THE NAME IS OURS ONLY IN THE SENSE THAT WE WROTE DOWN WHAT THE CLIENT DREW. \
Harness 20260911T233252 (build 38797, loopback): this definition stood ALONE in \
Lakeside County -- two creates in the whole run, the player and this body -- and \
its nameplate read "Lieutenant Fisk". One body in the world means one nameplate, \
so no projection, compass reading or elimination stands between the definition \
and the name. studies/slice/RUN-PARADE.md 4b."""

[npc.ascalonian_townsfolk]
name = "Ascalonian Townsfolk"
definition = 1490
file_id = 116225
model_id = 116754
scale = 1677721600
flags = 524
profession = 1
level = 1
move_speed = 200.0
enc_name = [3948, 39255, 51957, 11013]

[npc.ascalonian_townsfolk.provenance]
source = "capture"
capture = "20260807T143055"
origin = "live"
extractor = "toolkit/authsrv/npcdefs.py"
verified = """OBSERVED on ArenaNet's own wire: definition 1490 declared by \
GAME_SMSG 0x0056 in 6 connections across 2 captures, payloads byte-identical, \
36 creates carried it. Every numeric field is read from that declaration and is \
unchanged from the vault overlay row npcdefs.py emitted.

Harness 20260911T233623, same method as lieutenant_fisk above: alone in the \
world, nameplate "Ascalonian Townsfolk". studies/slice/RUN-PARADE.md 4b."""

# THE SLICE'S TWO MONSTER ARCHETYPES, promoted from the vault overlay exactly as
# lieutenant_fisk and ascalonian_townsfolk were: every numeric field is the
# overlay row npcdefs.py emitted from ArenaNet's own 0x0056, and the NAME is
# what the client drew when the definition stood alone in an empty world
# (studies/slice/RUN-PARADE.md 4b). They are tracked because content/world.toml's
# corridor rows name them, and a tracked spawn row naming a vault-only template
# dies on a bare machine (test_quests section 21).

[npc.bandit_raider]
name = "Bandit Raider"
definition = 1421
file_id = 141267
model_id = 116647
scale = 1677721600
flags = 12
profession = 1
level = 2
move_speed = 288.0
attack_interval = 1.75
attack_modifier = 1.0
enc_name = [3884, 43762, 40100, 4764]

[npc.bandit_raider.provenance]
source = "capture"
capture = "20260807T143055"
origin = "live"
extractor = "toolkit/authsrv/npcdefs.py"
verified = """OBSERVED on ArenaNet's own wire: definition 1421 declared by \
GAME_SMSG 0x0056 in 4 connections across 2 captures, payloads byte-identical, \
26 creates carried it -- a real Pre-Searing hostile, allegiance `band` in the \
captures. Every numeric field is read from that declaration and is unchanged \
from the vault overlay row npcdefs.py emitted.

Harness 20260911T234311 (build 38797, loopback), same method as \
lieutenant_fisk: this definition stood ALONE in Lakeside County -- two creates \
in the whole run, the player and this body -- and its nameplate read "Bandit \
Raider". studies/slice/RUN-PARADE.md 4b. It is the slice's WARRIOR archetype \
(profession 1)."""

[npc.academy_monk]
name = "Academy Monk"
definition = 1486
file_id = 116225
model_id = 116741
scale = 1677721600
flags = 524
profession = 3
level = 5
move_speed = 250.0
enc_name = [3944, 45501, 61520, 25395]

[npc.academy_monk.provenance]
source = "capture"
capture = "20260807T143055"
origin = "live"
extractor = "toolkit/authsrv/npcdefs.py"
verified = """OBSERVED on ArenaNet's own wire: definition 1486 declared by \
GAME_SMSG 0x0056 in 2 connections across 2 captures, payloads byte-identical, \
3 creates carried it. Every numeric field is read from that declaration and is \
unchanged from the vault overlay row npcdefs.py emitted.

Harness 20260911T233513, same method: alone in the world, nameplate "Academy \
Monk". It is the slice's MONK archetype (profession 3) -- and note the parade's \
finding that name-matches-profession is REFUTED as a join key: def_1470, the \
first guess for "the monk", drew "Outfitter". studies/slice/RUN-PARADE.md 4b. \
Spawned HOSTILE by the corridor rows; the template carries no allegiance."""

